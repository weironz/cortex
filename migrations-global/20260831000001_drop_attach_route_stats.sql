-- 直拨（灰度期的回退路）退役，量具跟着退役。
--
-- 这张表 2026-08-30 随 v0.1.28 建起来，用途只有一个：回答「直拨还能不能撤」。
-- 答案在第二天就齐了，而且比预想的干脆 —— 它一行都没写过：
--
--   * 表是空的。flusher 在零计数时主动不写空事务，所以空表就是「没人接入过」
--   * 生产上远程接入**一次都没被启用过**（两个 agent 都没带
--     `--allow-remote-attach`），而桌面端本来就从不传 `--attach-addr`，
--     于是它根本产生不出直拨路
--   * 隧道那条有全链路测试盯着，每次构建都跑
--
-- ⚠️ **前一条 migration 不能删，只能在这里 DROP。** 它已经在生产上应用过，
-- 而 sqlx 会核对「已应用的每一条都还在」—— 直接删文件的表现是服务端启动时
-- 报「某条 migration 已应用但找不到」，且那是在部署完成之后才发作。
DROP TABLE IF EXISTS cortex_auth.attach_route_stats;
