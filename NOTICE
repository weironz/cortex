Cortex
Copyright 2026 Cortex contributors

Licensed under the Apache License, Version 2.0. See LICENSE.

═══════════════════════════════════════════════════════════════════════
本项目取件（vendor / 借鉴）了以下 Apache-2.0 项目的成果。
每一处的具体出处也写在对应源文件的头部注释里 —— 这份文件是索引，
不是替代品。改动过的地方都在源文件里注明了「与上游的分歧」及理由。
═══════════════════════════════════════════════════════════════════════

───────────────────────────────────────────────────────────────────────
goose
https://github.com/block/goose
Copyright Block, Inc.
Licensed under the Apache License, Version 2.0
───────────────────────────────────────────────────────────────────────

**已取件，此后由 Cortex 自行维护，不再与上游同步。**（2026-08-19）

  crates/cortex-providers/     LLM 供应商适配层。类型层（Message / 格式转换 /
                               模型目录）与四个引擎（openai / anthropic /
                               google / ollama）的实现，含声明式 JSON 供应商
                               定义。已处理 prompt caching 与 thinking /
                               reasoning 不透明块。

原先是 crates.io 上的 `goose-providers = "0.1.0-alpha"`。改成取件的理由写在
`crates/cortex-providers/src/lib.rs` 的模块文档里，一句话是：它是 alpha，
而我们用不到它的大半 —— databricks / snowflake 那几套在本仓库一行都执行不到，
却一直在编。

砍掉的与保留的、以及当初是怎么取的，见 `scripts/vendor-providers.py`
（一次性脚本，留着当记录，不打算再跑）。

⚠️ **不与上游同步**是有意的：那份代码现在按本仓库的判据改，不必去看上游。
刻意不留「定期同步 upstream」的流程 —— 一个没人会执行的流程会让读代码的人
以为这里的改动需要先跟谁对齐。

保留署名是 Apache-2.0 第 4 条的要求（每个取件文件头部都写着出处），
与「这是不是我们的代码」无关。

与上游的分歧（都在源文件里注明了理由）：

  src/images.rs   `candidate_anchors` —— 上游只认 POSIX 绝对路径（只找 `/`），
                  于是 Windows 上粘一个 `D:\pics\shot.png` 图片**静默**不被
                  内联。补了盘符与 UNC 两种起点。上游那 6 条 images 测试在
                  Windows 上原本全红。

`cortex-llm` 直接复用其 `Message` / `MessageContent` 作为内部规范消息格式，
不再包一层有损转换 —— 中间抽象会丢掉 prompt caching 与 thinking 块，
那是整条链路上最贵的两样东西。

相关文件：
  crates/cortex-llm/src/{client,error,provider,vision}.rs

───────────────────────────────────────────────────────────────────────
codex
https://github.com/openai/codex
Copyright OpenAI
Licensed under the Apache License, Version 2.0
───────────────────────────────────────────────────────────────────────

未以 crate 依赖方式使用 —— codex 的 sandboxing crate 未发布到 crates.io，
且深度耦合其内部类型（codex-protocol / codex-network-proxy 等）。
因此取的是**策略与设计**，底座直接用上游 crate（landlock / seccompiler）。

  crates/cortex-agent/src/sandbox/seatbelt_base.sbpl
      macOS Seatbelt 基线策略，近乎原样取自
      codex-rs/sandboxing/src/seatbelt_base_policy.sbpl

  crates/cortex-agent/src/sandbox/linux.rs
      seccomp 拦截清单的语义取自 codex-rs/linux-sandbox/src/landlock.rs
      （socket / socketpair 按 AF_UNIX 分流；recvfrom 绝不拦截）。
      有意分歧两处，理由写在该文件头部：不用 bubblewrap；读也限而非只限写。

  crates/cortex-agent/src/sandbox/macos.rs
      sandbox-exec 的调用形状取自 codex-rs/sandboxing/src/seatbelt.rs

  app/assets/providers/*.png
      供应商品牌标，来自两处：

      * anthropic / google / minimax / ollama / openai / xai
        取自 goose 的 ui/desktop/src/components/settings/providers/modal/
        subcomponents/icons/（各家的 @3x 那份，128px）

      * deepseek / zhipu / moonshot / alibaba
        取自 lobehub/lobe-icons 的 packages/static-png/light/
        （deepseek-color / zhipu-color / kimi-color / qwen-color，640px）
        MIT License, Copyright (c) 2023 LobeHub

      文件名就是 provider id，与 `ProviderMark` 的约定一致。

      ⚠️ 这些图**本身是各厂商的商标**，不是 goose 的作品 —— Apache-2.0
      覆盖的是 goose 的代码，不是它打包的第三方标识。这里的用法是
      「用某商标指代该商标的产品」（指名性使用）：界面上每一个标都紧挨着
      那家供应商自己的名字，不用作 Cortex 的标识、不暗示任何背书或合作。
      任一厂商提出异议时，`ProviderMark` 的首字母圆标兜底就是退路 ——
      删掉对应的 png 即可，不用改一行代码。

═══════════════════════════════════════════════════════════════════════
明确未取件
═══════════════════════════════════════════════════════════════════════

  crush (charmbracelet/crush) 是 FSL-1.1-MIT，禁止竞品使用。
  GitHub 上显示为 NOASSERTION，极易被误认为宽松许可。
  本项目**一行代码都没有**借鉴自它，仅在 docs/references.md 里作调研记录。

  claude-code 是专有软件，仓库不含源码，无从借鉴。

═══════════════════════════════════════════════════════════════════════

Apache License 2.0 全文见 http://www.apache.org/licenses/LICENSE-2.0
