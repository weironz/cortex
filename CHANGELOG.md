# 变更日志

本文件记录**用户能感知到的**变化。设计上的「为什么」在
[docs/architecture.md](docs/architecture.md)，逐条进度在
[docs/roadmap.md](docs/roadmap.md) 与 [docs/roadmap-done.md](docs/roadmap-done.md)。

版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。**0.x 意味着
HTTP / SSE 契约与数据库 schema 都还会不兼容地变** —— 见下面「0.1.0
不做什么保证」。

---

## [未发布]

### 「登录很快过期」的诊断补上了最后一块盲区

桌面端的诊断文件（`agent-launch.jsonl`）此前只记录**用着用着被踢**的
那条链；「第二天打开又要登录」—— 更常见的抱怨形状 —— 什么都不留。
现在每次启动用记住的凭据续会话时，结果（成功也算）连同服务端的答复
落进同一份文件。于是「日志里没有被踢记录」第一次可以读成结论：
续期在跑，而且都成功了。

顺带说明：此前文件里那 124 条「被踢」记录全部是 08-25 当天测试跑出来
的残留（当天已修，测试不再写真实日志），不是真的发生过 124 次。

Windows 上的命令终于被沙箱管住了；顺带揪出一个「带引号路径的命令一直
跑不通」的老 bug。另外对着两家刚开源的 agent harness（OpenAI Codex、
DeepSeek Harness）核了一遍差距，补上两处真空。

### 需要跑构建的话，有第二档沙箱了（默认关）

上面那档（AppContainer）读得最严，但**跑不了完整构建** —— `cargo check`、
clippy、类型检查都正常，可 `cargo build` 生成 .exe 的最后一步会挂，而且是
这套机制本身的限制，改权限没用。

所以多了一档，用环境变量 `CORTEX_WIN_BACKEND=restricted` 打开：

- **`cargo build` 能出 .exe 了**（含拉取新依赖），`git`、`dir` 也全部正常，
  **不需要管理员**。
- **写还是只能写工作区**，写工作区以外会被拒。
- 沙箱里的程序跑在一个独立桌面上，看不见也动不了你桌面上的窗口。

⚠️ **但这一档不限制读取**：它读得到你自己能读的任何文件，包括 `~/.ssh`。
这不是没做完，是这套机制下**做不到** —— 沙箱进程的读身份就是你本人，
没有办法只拦沙箱而不拦你自己（试过，会把你自己也挡在 `~/.ssh` 外面）。

**`git` 联网也修好了**：`clone` / `fetch` / `push` 都能用。这一档下 Windows
的默认 TLS 拿不到凭据（证书存储在受限令牌下只能只读打开，是系统的既定规则），
沙箱会自动让 git 改用它自带的证书包绕开。

**`cargo` 拉新依赖也通了**（`cargo add` / 新 `Cargo.toml` 都能用）。下载
crate 那一步用的是 cargo 自己链进去的 TLS，换不掉 —— 所以改成让它不走 TLS：
Cortex 在本机回环上开一个 crates.io 镜像，TLS 由 Cortex 这一侧做。crate 的
校验和照常验，端到端没有变松。

⚠️ **这一档下 cargo 用的是它自己那份缓存目录**，不是你的 `~/.cargo` ——
沙箱写不进你的，而把你的授出去意味着沙箱里的代码能往你的 crate 缓存里塞
东西、下次在沙箱外构建时被照单编译。代价是**第一次构建会重新下载依赖**，
之后各工作区共用这一份。不要为此去改 `CARGO_HOME`。

**`curl https://` 也通了**：系统自带的 curl 用的 TLS 在这一档拿不到凭据，
沙箱会在首次使用时自动准备一份官方的 LibreSSL 构建（来自 curl.se，下载
地址与 SHA-256 校验值都固定在代码里，约 9 MB，一次性），之后沙箱里的
`curl` 命令自动用它。

⚠️ PowerShell 的 `Invoke-WebRequest` 仍然不通（.NET 的 TLS 没有可换的
后端）—— 模型已被告知改用 `curl` 或 `git`，不会当成网络故障反复重试。

所以两档的用途不一样，按需要选：

| 你要做的事 | 用哪档 |
|---|---|
| 让 agent 读代码、分析、跑 check / lint | 默认那档（读得严） |
| 让 agent 跑**本地**构建、本地 git 操作 | `CORTEX_WIN_BACKEND=restricted` |
| `git clone` / `fetch` / `push` | `CORTEX_WIN_BACKEND=restricted` 可用 |
| `cargo` 拉**新**依赖 | `CORTEX_WIN_BACKEND=restricted` 可用 |
| 跑**完全不信任**的代码 | 默认那档 —— 第二档挡不住偷读 |

两档都**还没有隔离网络**。

### Windows 上的命令终于被沙箱管住了

以前 Windows 上唯一的闸是「用户逐条确认」，而**远程接入的一方能自己按
那个确认**。现在 Windows 上的 `shell` 命令跑在 **AppContainer** 里：
工作区内照常读写，工作区以外**读不到也写不进** —— `~/.ssh`、浏览器
profile、文档，一律拿不到。不需要管理员权限。

每条命令的额外开销 60 毫秒。第一次绑定一个**已经建过**的大仓库要多等
一会（一个 79 万文件的仓库实测 2 分半），那是 Windows 给目录授权时必须
铺满整棵子树，**跨重启只付这一次**，之后每条命令 0.2 秒。给上级目录授权
则是瞬间的（同样的目录 0.4 毫秒）——那一步不需要触及子项，换了个不做
传播的接口。

**网络也管住了**：沙箱里的命令默认连域名都解析不出来。需要联网的命令
要显式放开，而放开是**全放**（这一层没有白名单，与云沙箱那侧不同）。
顺带一提，沙箱里的命令**连不上你本机跑着的开发服务器** —— 那是 Windows
这套机制一贯如此，不是我们设的。

**`git` 在沙箱里能用**，而且提交带的是你自己的身份。为此沙箱会给工作区的
上级目录加一点「能看见有哪些文件名」的权限（读不到内容），并**合成**一份
只带 `user.name` / `user.email` 的 git 配置 —— 你的 credential helper、
链接里嵌的令牌、签名密钥一个都不进沙箱。

⚠️ **有些位置还是不行**：工作区在用户主目录底下（`C:\Users\<你>\…`，含桌面、
文档、`%TEMP%`）时用不了；工作区所在盘的第一级目录名比较短时（`D:\codes\…`）
也需要一次管理员授权。放在名字长一点的目录下（`D:\my-projects\…`）零配置可用。
这不是我们挑的规则 —— Windows 只在目录名**可能是老式 8.3 短名**时才去上一级
查它，而那一次查询正是沙箱挡住的东西。

⚠️ **`dir` 与 `vol` 用不了**，会报「拒绝访问」。它们要磁盘卷的信息，而那
需要一次管理员授权 —— 那是整套里唯一需要提权的东西，且**只买这两个命令**。
看目录内容用 `list_dir` 工具（模型已经被告知了这一点）。

### 带引号路径的命令在 Windows 上一直是坏的（已修）

`type "C:\Program Files\x\a.txt"` 这种写法在 Windows 上**从来没跑通过** ——
报「文件名、目录名或卷标语法不正确」。而带空格的路径在 Windows 上是常态，
所以模型每次照着 `C:\Program Files\...` 写命令都会失败，然后换一种写法
再试，一轮就这么烧掉了。

原因是给 `cmd.exe` 传命令时用了错误的转义规则（那套规则是给别的程序用
的，cmd 不认）。hook 里跑的命令是同一条路，也一样。

> 这个 bug 是**被沙箱的逃逸测试逼出来的**：那条「命令读不到用户目录」的
> 测试，把沙箱整个关掉之后**照样是绿的** —— 因为命令压根没跑起来。
> 一个在保护失效时仍然全绿的安全测试，比没有那条测试更糟。

### agent 不再在原地打转到把一轮烧完

模型会卡住：同一条失败的命令重跑、同一个没变过的文件重读。以前那一轮
会烧完八次模型调用，全花在同一个 grep 上，然后以「我没能完成」收场。

现在它会被提醒：「你已经用完全相同的参数调了这个工具 N 次，而结果没有
变」。**只提醒，不阻断** —— 轮询后台任务这类合法的重复一次都不会被打扰。

### 一台卡住的 MCP server 不再把整轮挂死

第三方 MCP server 在某次调用上卡住时（对端等一个永远不来的响应、子进程
死锁），以前那一轮会**永久挂在那里**，而那台 server 在设置页里显示
「连接正常」。现在一次调用最多 120 秒，超时后模型会收到一句读得懂的
失败并可以换个做法。

### 一台慢的 MCP server 不再让整个本地 agent 起不来

以前 agent 的启动被「连上每一台 MCP server」挡着（每台最长 60 秒，
且是串行的），而桌面端只等 20 秒 —— 超时就报「本地 agent 启动失败」，
**而 agent 其实好好的，只是在等一台别人的进程**。装的 server 越多越容易
撞上。现在在后台连，agent 立刻就绪；还没连上的 server 只是这一轮暂时
没有它的工具。

### 「关于」页不再说一句它没验证过的话

装着新版本的人打开「关于」，多半会看到**「已是最新」**—— 而那句话在
「还没检查过」的情况下是没有依据的（24 小时节流让启动时那次检查跳过了）。
现在它老实说「还没检查过」。

### 其他

- `CORTEX_MCP_CONFIG` 可以指定 MCP 配置文件路径。多数人用不到；
  它是**一台 MCP server 让 agent 起不来时的逃生口** —— 指一个空文件
  就能先把 agent 拉起来看日志。
- 修掉技能说明里一处格式问题：那段话因为缩进被模型当成了代码块。

### Windows 上的模型不再拿 bash 的写法去撞墙

系统提示词里从来没有一句说「这台机器是 Windows、shell 是 `cmd`」。模型于是
按最常见的猜：Unix 的 `ls` / `cat` / `grep`，或者 Windows 上更眼熟的
PowerShell（`Get-ChildItem`、`$env:VAR`）—— 而 `cmd.exe` 两套都不认。
失败信息是「'ls' 不是内部或外部命令」，读起来像**这台机器没装那个工具**，
于是它去查、去装、去换写法，一轮就这么烧完。

现在提示词里直接讲清楚：跑的是 `cmd.exe /C`，不是 PowerShell 也不是 bash，
对应的命令是哪几个，路径和环境变量怎么写。有一条测试拴着它和真正起 shell
的那段代码 —— 哪天换了 shell 而说明没跟着改，测试会红。

### 这一版仍然不能

- **默认那档（AppContainer）跑不了 `dir` / `vol`**，工作区放在主目录底下时
  也跑不了 `git` —— 解开要一次管理员授权，**已决定不做**（「不要管理员」
  正是这一档的立身之本）。要完整工具链就切 `CORTEX_WIN_BACKEND=restricted`
  那档，`cargo` / `git` / `curl` 都通。
- **放开网络就是全放**：本机这一层没有白名单，**也不打算做**（零管理员
  做不到真封锁）—— 需要出网管控的场景用云沙箱，那侧有白名单。
- 上一版说的「Windows 上没有进程级沙箱」这一条**不再成立**。
- **远程接入不是「随处可用」**：那台机器得开着、Cortex 得跑着、
  开关得打开。
- **绑定是机器自陈的，不是密码学证明**：你名下一台被攻陷的机器可以
  申领你的任意会话并赢得路由。威胁模型在 `docs/controller-worker.md`。
- **直拨与隧道两条路并存**，为的是灰度期新旧版本混跑。

## [0.1.25] - 2026-08-27

两件大的：**远程接入**（你的机器终于能被 Web 端接进来）与**联网检索**
（四家服务商 + 网页抓取）。外加模型能力判定整块重做、对话区改版、
终端独立成栏。

### 远程接入：绑在这台机器上的会话，现在能在别处继续

以前一个绑了本机目录的会话只能在那台机器上打开 —— 换台设备就顶回来，
而那句话还不告诉你是哪一台。现在：

- **`设置 → 我的机器`** 列出你名下在线的机器、各自绑了几个会话、能不能
  接进去。那一页顶上是**这台机器**的远程接入开关。
- 打开之后，你在手机 / 网页 / 另一台电脑上的 Cortex 可以接进这台机器，
  继续那些绑在本机目录的会话 —— 文件、命令都在这台机器上跑。
- **不需要公网地址、不需要开端口、不怕 NAT**：可达性来自这台机器
  主动拨出去的一条长连接，绑 `127.0.0.1` 也照样能被接进来。
- 开着时**顶栏有一颗标记**，点它直达那一页。关着不画。

⚠️ **默认关，而且必须是你的一次显式决定。** 打开它等于同意
**远端可经模型在这台机器上执行命令、读写文件** —— 接入面里
「发起一轮」与「批准工具确认」是并存的，也就是说接进来的一方能自己
批准自己。开关旁边那段话把这件事说破了，它不许被写软成「允许远程查看」。

关掉是**当场生效**的：钥匙立刻作废、已经建立的连接当场断开、云端立刻
不再认为这台机器可接入。正在跑的那一轮仍会跑完，结果照样落进历史。

远程接入用的是一把**另铸的**钥匙，只在接入面上有效（发起对话、看进度、
批准确认、健康检查）。换凭据、绑目录、改 MCP、开终端一律拒绝 ——
**开放接入不等于交出机器**。

### 联网检索：四家服务商，key 在你手里

- `设置 → 联网检索`：**Tavily / 博查 / Exa / Brave** 任选，各自的 key
  与端点各存各的，检索深度可调、结果条数可调、可拉黑站点。
- **`web_fetch`**：模型能把搜到的网页正文读回来，而不只是看摘要。
  抓正文的只有 Tavily 与 Exa；博查与 Brave 只做搜索，界面与服务端两处
  都挡着，不会摆出一个做不到的能力。
- 检索结果**带上发布日期** —— 模型此前分不出昨天的新闻和 2019 年的。
- 「自动」档不再挑一个下一步必定被拦下的来源。

> 四家不是同一件事的四个牌子：地域、索引、语义检索各有强项，
> 那张对照表在 `docs/architecture.md` 的「服务商是一层抽象」一节。

### 模型能力：接口能问的先问，问不出来才落目录

「这个模型能不能看图 / 调工具 / 生图」以前只有两个源，而且都会漏 ——
新上线的模型永远慢一拍，自建中转站永远不在里面。现在是四级回落：

**你手工按的 > 接口实时探测 > 内置目录 > 供应商定义**

- **「编辑模型」面板**：视觉 / 工具 / 思考 / 生图四位可以手工按定，
  另外能改显示名与上下文长度。只写你**真按过**的那几位 —— 界面上预先
  选中的多半来自自动结论，一并写进去等于把此刻的结论冻成你的断言。
- **接口探测**：OpenRouter 与 Ollama 能直接问出能力，问了就用。
- 「部署提供」那条来源现在也真的去问供应商，不再只列硬编码的几个名字。
- **贴了图而模型看不懂图时，拦在发送之前**并告诉你换哪个 ——
  此前是发出去之后才失败，而图已经上传了。
- DeepSeek V4 全系上下文改回 **1M**。

### 对话区：过程压灰，结论正常

参照 Claude Desktop 那套分注意力：

- **工具调用、思考过程、脚本执行**统一压成浅灰，关键结论保持正常黑色。
- **长代码块与文件修改可折叠**，改动按行标出增 / 删颜色。
- 一次失败不再在屏幕上画两遍。
- **一轮失败之后，一次后台同步不再把它抹掉** —— 此前那条失败连同你
  刚打的字会在几秒后凭空消失。

### 终端从右栏页签里搬出来

多标签、铺满、自己的开关。顺带修掉三个：敲不进字（可打印字符走的是
另一条通道）、点对话区之后键盘还留在终端里、Unix 上 `$SHELL` 的回落。

### 其他

- **左栏拖宽跟不上鼠标**修了（同一帧里的位移被丢掉）。
- 白纸状态也居中、也能先定工作区；两栏可拖宽；终端配色跟主题走。
- 能力开关从三个 chip 收成两个 ——「跟随自动」是我们的词，不是你的。
- 配额超额那条提示：星号不再画在屏幕上，指的路也真的存在了。
- 子 agent 的工具描述不再承诺目录里没有的能力。
- `just app` 起来时说清楚它连的是哪个部署。

### 这一版仍然不能

- **远程接入不是「随处可用」**：那台机器得开着、Cortex 得跑着、开关得
  打开。关机、休眠、断网都会让它离线（几秒内就会显示出来）。
- **Windows 上没有进程级沙箱**。Linux 用 landlock + seccomp、macOS 用
  Seatbelt，Windows 上唯一的闸是「你逐条确认」—— 而远程接进来的一方
  能自己按那个确认。开之前想清楚。
- **绑定是机器自陈的，不是密码学证明**：你名下一台被攻陷的机器可以
  申领你的任意会话并赢得路由。这条限制写在
  `docs/controller-worker.md` 的威胁模型里，加固方向也在那儿。
- **直拨与隧道两条路并存**，为的是灰度期新旧版本混跑。等线上不再有
  拨不起隧道的机器，直拨那条会整个撤掉。
- 检索与抓取都**经上游服务商**，所以内网地址本来就够不着；
  但也意味着你的查询词会到那家服务商那里。

## [0.1.24] - 2026-08-26

这一批集中修桌面端与 Web 端的日常体验，来自一次集中反馈（十二条）。

### 「续期成功但新凭据仍不被接受」→ 被踢回登录页，根除

同一个症状背后是三个病，这次一起治：

- 桌面端本机 agent 反代请求时，把**它自己那把机器凭据**原样转给了
  服务端 —— 那把钥匙服务端不认识，答 401，客户端把它误读成「我的登录
  失效了」。现在反代（HTTP 与 WebSocket 两条路）一律按**当前用户凭据**
  替换后再转发，机器凭据永远不出这台机器。
- 服务端 refresh 轮换改成原子认领 + 30 秒并发宽限：两个窗口同时续期、
  或一次网络重试撞上自己，慢的那个不再把整条凭据链烧掉。
- 客户端只把明确的 401/403 当「凭据死了」；429 单独计账走退避，网络
  抖动走重试。每次回登录页都带原因码落盘 —— 再出现同类问题，
  日志说得出是谁踢的。

### 会话可以分叉了

对一条消息点「从这里分叉」（或会话菜单里整段分叉），历史复制成一条
新会话独立续聊，旧会话一个字节不动。执行环境、工作区绑定、项目归属
都跟着走；附件字节零复制（内容寻址，复制的只是引用）。离线时入口
停用 —— 历史的权威在服务器上。

### 右栏是真终端了

「终端」页签在本机 agent 活着时是能敲命令的真终端（Windows 是
PowerShell，Linux/macOS 是系统 shell），起在你自己的机器上。每会话
同时最多一个；Web 端与容器会话没有这个能力，也不摆出来。远程接入的
钥匙够不到这条路 —— 在别人机器上开 shell 是机器主人的权限。

### 模型选择从底部大面板换成紧凑弹层

底部面板一拉就是大半屏，把正在读的对话整个盖掉。现在是锚在 chip 上的
紧凑弹层：搜索、能力筛选（视觉/工具/思考/生图）、按来源分组、底部
直达「配置模型服务」。

### 设置页的一批

- **「获取模型列表」与连通性检查不再把整页按钮挂死**：服务端与客户端
  各自设界，供应商端点黑洞（被墙）不再让按钮灰几分钟。超时文案分清
  「供应商不可达」与「服务端没回音」两种病；写操作超时后列表自动重拉，
  界面对齐库里的现状。
- **已存的 API 密钥显示掩码点**，不再看起来像没填过。
- **API 代理地址空着时显示这家供应商的默认端点**，看得出请求会打到哪。
- 左侧导航选中项的高亮此前被底色整个盖住，看不出自己在哪一页，修了。

### 其他

- **登录页开放注册**：部署开着注册时，登录页出现注册入口，注册即登录；
  没开时不摆。
- **画廊打不开修了**：一处 SQL 占位符与参数错位，带筛选查询必崩。
- **主题切换**从顶栏挪进设置 → 外观 —— 它不是一天按几次的东西。
- 右栏的「✕ 关闭」改成**折叠**语义，与左栏对称；顶栏开关与栏头收起
  用同一个图标。
- 工作区面板头部可以**在资源管理器中打开**（macOS 是 Finder，
  Linux 是系统文件管理器）。

## [0.1.23] - 2026-08-25

### 备份的状态一览不再唠叨

`backup-status` 里「告警未配」那一行原先写着「**备份失败不会有人知道**」。
这套部署是明知代价地决定不配告警的，而对一个已经做过决定的人重复同一句
提醒，只会让整份状态输出开始被跳着看 —— 于是真正要紧的那几行（半截段、
从未演练）也一起被跳过去。改成中性陈述。

备份容器里那个可选的「死人开关」一并去掉了。为什么不要它、代价是什么、
哪天该加回来，都写在 [docs/backup.md](docs/backup.md)。

## [0.1.22] - 2026-08-25

### 备份的恢复演练能在生产上跑了

备份每天在跑、每周校验一次仓库完整性 —— 但那两样证明的是「字节没坏」，
不是「恢复得出一个能起来的数据库」。演练脚本现在随部署送上节点：

    cd /data/cortex && bash scripts/restore-drill.sh

它真起一个独立实例、真回放归档 WAL、真查一条探针。对生产库唯一的写入是
那条探针（独立 schema），临时实例不发布端口、跑完自删。

顺带修了一个让它跑不起来的：备份容器写的全量**没有 meta.env**，而演练
靠它拿起点 WAL 与数据库身份 —— 于是演练在第一步就死，而备份本身全绿。

## [0.1.21] - 2026-08-25

### 备份能自己送到异地了

以前备份只落在这台机器上 —— 那防得住误删，防不住整机丢失，而整机丢失
正是备份存在的理由。

现在多了一个**备份容器**，每天把两样东西送到阿里云 OSS：

- **数据库**走 `rustic`（加密、去重、按天/周/月保留）
- **附件与生成图**走 `rclone`（S3 到 S3，**只增不减**）

默认不开。要开的话见 [docs/backup.md](docs/backup.md)：建一个桶、建一把
只授这个桶的 key、把凭据写进节点的 `.env.secrets`，然后打开开关。

两个细节值得说：

**数据库那半仍然能恢复到任意时刻。** 参考的那套做法是每天导一份 SQL，
那样最多能回到昨晚 —— 而这一侧的 WAL 归档一直在跑，能回到一分钟之前。
所以送去异地的是「基础备份 + WAL 归档」整棵树，那个能力一并搬过去了；
另外还有一份 SQL 转储，它买的是别的东西（换机器、跨版本、只捞一张表）。

**附件那半用的是「只增不减」的镜像。** 换句话说，主存储上被删掉的对象，
备份里还留着。这是有意的：一次误删、一次桶被清空，如果备份跟着删，
两份就一起没了。

### 修复：备份失败时会主动叫人

配了 `HEALTHCHECK_URL` 之后，备份跑成功 ping 一次、失败 ping 另一个地址。
它覆盖的是「压根没跑」—— 容器死了、循环卡住了，这一类不产生任何退出码，
也就不会有任何告警，而你看到的每一个信号都还是绿的。


### 「派出去跑」终于有名字了

「让 agent 自己在云上跑一段时间，关掉电脑也照跑」这个能力一直都在 ——
但它以前叫「云端」，藏在一个问「这次对话的文件放哪」的菜单里。想派活儿
出去的人不会去翻那个菜单，于是产品里最贵的那件事只能被碰巧发现。

现在它叫**「派出去跑」**，副标题写清它买到的东西：关掉这台电脑它也照跑，
回来就看得见结果。机制一个字没改。

### 一轮跑完时，会把话送到窗口外面

从前跑完只有窗口里的两处提示（侧栏那个勾、底部提示条）—— 而你最小化了
桌面端、或者浏览器标签在后台时，那两处一处都看不到。

现在：**Windows 上任务栏按钮会闪**，一直闪到你点回来；**网页版会弹一条
系统通知**（第一次真的有东西要通知时才问权限，不是一打开就问）。
你正看着这扇窗的时候两者都不做 —— 你已经看见了。

macOS / Linux 桌面上暂时没有这个（那两个平台我们不发安装包）。

### 可以把全部对话导出带走了

设置 → 数据 → 导出。一份 NDJSON（一行一条记录），任何文本编辑器都打得开，
`jq` 也能读。对话、标题、时间、用了哪个模型都在里面；附件给的是引用，
要原件去对话里下载。

只想要**某一段**对话的话，左栏右键那条会话 → 导出，还能选 Markdown ——
那条一直都在。

### 挂连接器（MCP server）之前，先看一眼有没有 node

桌面端安装包**不带 Node.js 也不带 Python**，这是有意的：带一个是 +40 MB，
而绝大多数人一个连接器都不挂。装了 node 的人也不需要第二份。

代价是没装的人点「接上」会失败。以前那句话是「拉不起子进程 npx：程序
未找到」—— 长得像我们的 bug，于是有人去重装桌面端。现在它说的是：
「找不到命令 npx。这台机器上没有 Node.js。装一个（nodejs.org）之后重连
即可。」

也可以提前问一句：`cortex doctor` 里多了「连接器运行时」一行，直说
npx 与 uvx 在不在。云端会话不受影响 —— 那边的镜像自带。


### agent 会在工作区里搜东西了

两个新工具：按内容找（`grep`，支持正则）和按文件名找（`glob`，
`*.rs`、`src/**/*.ts` 那种写法）。

从前它只能一层层地列目录，或者自己拼一条 `rg` 命令 —— 而你的机器上
不一定装了 `rg`，它得先花一轮试出来这台机器有什么；就算装了，执行命令
默认还要你点一次确认。现在这两件是只读的，不打断你，结果条数与每行
长度都有上限（截断时它会说出来，而不是让模型把半截结果当成全部）。

搜索**尊重 `.gitignore`** —— 不然一次搜索能命中一堆 `target/` 与
`node_modules/` 里的东西，而那不是你的代码。

### 连上的 MCP server 提供的材料，agent 能取了

有些 MCP server 除了工具还带「材料」（resource）—— 文档站、知识库
那一类。现在 agent 可以列出有哪些、按需读取其中一份。

只在**真的有材料**时才出现这个能力：绝大多数 server 只提供工具，
对它们摆出这个工具的话，agent 每次调都只会拿到一句「一个都没有」。

### 智能体可以单独关掉「电脑操作」了

智能体编辑页的工具开关补上了这一格。它一格管五个动作（截屏、点击、
打字、按键、滚动）—— 拆成五格既不是设计稿的样子，也没人会只关掉其中
一个。同时补上了「搜索文件」那格。

这台机器做不到电脑操作时，这一格整个不画 —— 摆出来的话，你拨开它，
然后 agent 答应去点按钮、每一次都失败。

### 修复：给智能体单独开着「派子 agent」时，那个工具其实没进目录

表现是模型说它派不出子 agent。只在这个会话没同时开着别的能力时发生，
所以一直没被发现。


### agent 能派子 agent 并行查东西了

「这三个模块各自怎么处理超时的」这种活儿，从前要一个个查、一轮一个；
现在主 agent 会派几个子 agent **同时**去看，一轮全回来。

子 agent **只能读**（读文件、看目录、检索资料库、联网搜），不能改也不能
执行 —— 并行写会撞同一个文件、谁后写谁赢，而没有任何地方会报错。写代码
和跑命令仍然是主 agent 的事，它看得到全部子 agent 的回答，能一次改对。

一次最多派 4 个，且子 agent 不能再派子 agent（否则一次「派 4 个」会
炸成 4 的 n 次方）。它们的过程不往对话里刷屏，你看到的是结论。

### hooks：在工具调用前后跑你自己的命令

`~/.cortex/hooks.json` 里配一条，比如「每次写完 dart 文件跑一遍
dart format」：

```json
[{ "event": "postToolUse", "tools": ["write_file", "edit_file"],
   "command": "dart format $CORTEX_TOOL_PATH" }]
```

提示词是「请你记得」，hook 是「由不得你」—— 写进人设的规矩模型十次做对
八次，挂成 hook 就是十次十次。

`preToolUse` 的 hook **非零退出会拦下这次调用**（用来挡「不许碰
migrations/」这类）；hook 自己跑不起来时也拦 —— 一条本该挡住的调用被
静默放过去没人会发现，而一个突然开始拒绝的 agent 你三秒内就去查配置了。

⚠️ hook 是一条**不过权限确认**的 shell 命令（那正是它的价值），所以它
**只从你的用户目录读，不读工作区** —— 否则 clone 一个陌生仓库就等于
执行它自带的脚本。

### 长对话不再「忘掉开头」

一段聊久了的对话，最早那些轮次会因为上下文预算不够被丢掉 —— 从前它们是
**彻底消失**的：模型答不出「我们一开始说好的方案」，而你看不出任何征兆。

现在掉出去的那部分会被压成一段摘要放在最前面（用便宜的那个模型，
按「丢了多少条」缓存，所以不是每轮都重摘）。摘要里会注明它是转述不是
原话 —— 否则模型会把它当成你真讲过的句子去引用。

摘不出来时就当没有摘要，绝不会因此让这一轮对话失败。

### agent 能起后台命令了

`background_run` 起一条不等它跑完的命令（dev server、watch 模式的测试、
大构建），`background_output` 随时看输出，`background_kill` 停掉。

从前 `shell` 是同步单发、最多 10 分钟就杀 —— 「起个 dev server 然后接着
干别的」在这个产品里做不到，模型只能把一整轮挂在那儿等超时。

一个会话最多 8 个同时在跑；输出留开头 64 KB（启动日志与第一条报错在
开头，尾部通常是同一行重复一万次），截断过会明说。

### 技能能与 Claude Code / Codex / Grok 互通了

设置 → 技能里多了「导入 SKILL.md」，每条技能后面也多了导出。
`SKILL.md` 是那三家已经收敛的同一份格式 —— 你为它们写过的技能可以
直接拿过来用，反过来也行。

没有 frontmatter 的手写 markdown 也认（拿一级标题当名字）。

### 断了的 MCP server 会自己接回来

一台中途断掉的连接器（子进程被杀、HTTP 服务重启），下次用到它时会
自动重连，不用再去设置页手动点一次 —— 从前你根本不知道它断了，
只看到模型说「那个工具用不了」。

**最多自动重连三次**，之后要人去点一次「重新连接」。没有这个上限的话，
一台永远连不上的 server 会让此后每一轮对话都多等一个连接超时。

### agent 能上网搜了（要先配 key）

新工具 `web_search`：模型遇到自己不确定或可能过时的事实（新版本号、
今天的新闻、某个 API 的现状）会自己去搜。回的是搜索结果的标题、链接与
摘要 —— 不抓网页正文。

**要在服务端配 `CORTEX_SEARCH_API_KEY`**（Tavily，或任何兼容它的服务）。
没配的话这个工具整个不出现 —— 摆一个必然失败的工具比没有它更糟；
真调用到没配的部署时，错误里写的是「去 .env 里设这个变量」而不是
一句「搜索失败」。

⚠️ 它把你的话发到第三方服务上，所以权限档按「写」级对待（会问你），
而且**可以按智能体关掉**（见下一条）。

### 智能体能按工具开关了

编辑一个智能体时，能挑它用得到哪些工具（读文件 / 写文件 / 改文件 /
执行命令 / 看目录结构 / 画图）。**关掉的工具不会进这个智能体的工具目录**
—— 模型根本看不见它，也就不会答应去做那件事。

给「只用来聊天的人设」关掉全部文件工具，或者给「只读的代码审阅者」
关掉写和执行，现在都做得到了。已有的智能体不受影响（默认全开）。

### 左栏能换排序方式了

顶上多了个排序菜单，三档：

- **最近更新**（默认，与从前一样）
- **优先级** —— 按「下一步该看哪个」排：等你确认 → 跑完没看 → 在跑 →
  置顶 → 其余。会话一多，这一档能让卡着的那条自己浮上来。
- **手动排序** —— 长按一条拖到你想要的位置。顺序存在这台设备上
  （同一个人在公司和在家想要的排法未必一样）。

排序与「按项目分组」互不影响，可以同时用。

### 设置里补了「通知」与「权限与沙箱」

**通知**：跑完、以及有一轮在等你确认时响一声。跑完那档只在你人在别处时
响 —— 盯着看它跑完的话不打扰你。⚠️ **系统级通知（最小化后的桌面横幅）
还没有做**，页面上写明了这一点：窗口最小化之后应用内的提示看不到。

**权限与沙箱**：新会话的默认权限档终于能设了（此前只能在对话里逐轮改，
每开一条新会话都弹回默认）。这一页也是「这台机器上到底有没有沙箱」的
唯一去处 —— Windows 上没有内核沙箱、改由逐条确认把关，这件事你该知道。

### 左栏多了「资料库」

设计稿上有、之前一直没做的那一屏。三个页签（全部 / 图片 / 文件）、
文件夹归档、未归档单独一段；⌘K 里也能直接跳过去。

材料**不是从这一页传的** —— 把文件拖进对话里发出去它就进来了，
画出来的图也能从图片页收进来。这一页是回头找、归档、和看「哪些材料
模型现在检索得到」的地方。

### agent 能查你的资料库了

传进资料库的文档，模型现在能**按需检索**——问它「上次那份客户需求里
交付时间是什么时候」，它会自己去查、把命中的那几段读回来，而不是要你
把文件重新贴一遍。

两点说在前面，因为它们决定了你该怎么问：

- **这是关键词检索，不是语义检索。** 用文档里可能出现的原词效果最好；
  问「怎么防止逃逸」找不到只写了「sandbox 沙箱隔离」的那段。
- **资料库的内容不会自动进每一轮对话**，模型想查时才去查 —— 哪一份被
  用过，会写在那一轮的工具调用里。

pdf / docx / xlsx 现在**只能存不能查**（提不出正文的提取器还没做），
卡片上会照实说「这类文件还提不出正文」，不会假装在切分。

### 相册变成了文件夹，图片终于能一张一张归档

**相册（一张图能同时在多个里）改成了文件夹（只在一个里）。** 你原来的
相册会原样变成同名文件夹，里面的图一张不少 —— 一张图曾经在多个相册里的，
留在最早加进去的那个。

为什么改：归档要回答的是「这东西放哪了」，而一个能同时在三处的东西答不了
这句。资料库那边本来就是文件夹（一份东西只在一个文件夹里），两套并存等于
让你记两套规则 —— 在图片页拖是「加进去」，在资料库里拖是「移过去」。
现在**图片和文件共用同一份文件夹清单**，一个文件夹里可以既有图又有文档。

**顺带修了你实测报的那条**：单张图的右键菜单里现在有「移动到文件夹…」。
此前归档只有批量那条路 —— 为了归一张图得先进多选模式勾中它，绕一圈才够得着
一个本该就在手边的动作。

### agent 的工具箱补了三件、还会读项目指引了

对照 Claude Code / Codex / goose 逐项核出的差距清单（roadmap 新增 I 节），
第一批落地四件：

- **`edit_file` 精确替换**：改文件不再只能整个重写。`before` 必须唯一
  匹配；没匹配上时它会告诉模型「你是不是想找这段」——失配最常见的原因
  是缩进差一格，这个提示让模型能自己救回来。确认框里照样有 diff 预览。
- **`tree` 目录树**：认识一个项目从五次 `list_dir` 变成一眼 —— 带每个
  文件的行数标注，尊重 .gitignore（构建产物不出现）。
- **`todo_write` 任务清单**：模型给自己记的 markdown checklist，每轮都
  出现在它的上下文里 —— 多步任务跑到第 6 轮忘了自己要干什么，是所有
  长任务翻车的头号原因。
- **读 `AGENTS.md` / `CLAUDE.md`**：绑定工作区后，根目录的项目指引自动
  进系统提示词（AGENTS.md 优先，两者只取其一；超长截断）。你在仓库里
  写给 AI 的规矩，Cortex 的 agent 现在也守。

### 会话再长，撞上下文窗口不再整轮报错

从前 agent 一轮里工具跑得多了、上下文超限，整轮直接「模型返回出错」——
半截回答已经吐给你了。现在它会把这轮里**旧的工具输出**折叠成一行占位
（最近两条保留），腾出地方接着跑；模型需要的话可以重新调工具取回。

本机 agent 的 `/health` 现在会报 LLM 走的是哪条路（`direct:...` 或
`proxy:...`）—— 「我配的直连到底生效没有」从此一眼可查。

### 带鉴权的 MCP server 能连上了

连接器配置里的自定义请求头（Authorization 等）此前没有真的发出去 ——
带鉴权的 HTTP server 永远 401。现在接上了；头名或值写错会在连接时明说
是哪一个，而不是静默丢掉让你对着 401 排查。

### 「过一会儿突然被退出」修了

被踢回登录页的两条误伤路径都堵上了：

- **服务端抖一下不再删你的登录。** 从前发版重启的那几秒里（502）、限流
  （429）、乃至打到一个没有账号功能的地址（501），都会把 30 天的登录凭据
  当场删掉 —— 你被踢回登录页，且下次打开也自动续不上。现在只有服务端
  **明确拒绝这枚凭据**（401/403）才删；服务端暂时答不上来的话凭据原样
  留着，它缓过来就自动续上，你最多损失一次请求。
- **刚续期成功后的零星 401 不再当场登出。** 新旧凭据交接的头几秒里掉出
  一两条 401 是常态（在飞请求换代、服务端多实例一新一旧），从前它直接
  触发登出 —— 每 15 分钟就有一次被误杀的机会。现在这类毛刺被忽略；
  真坏的凭据仍会在下一轮续期时被服务端拒绝而正常登出。

两道新的保险丝防它走向另一个极端：续期连续失败、或连续「续上了但没用」
（服务端配置问题）超过三次，会带着**说清原因**的提示回登录页 ——
而不是无限循环或静默坏死。

### 登录页清爽了

- 「登录状态会记住 30 天」那行删了 —— 记住登录本就是默认预期，不值得占
  一行；修好上面的误踢比承诺更有说服力。
- 「先离线用着」压缩成一行说明。不同步、看不到历史这些后果，改在**进入
  离线模式之后**由常驻横幅说（那才是它们有用的时刻）；横幅同时补上了
  「以前的会话都在服务器上，接上就回来」—— 防的是对着空列表以为
  「我昨天那些对话没了」。

## [0.1.20] - 2026-08-24

### 整套界面按设计稿重做了

桌面端与 Web 是同一套代码，所以下面这些两边都有。

**颜色**：外壳一律冷调中性灰（统一偏品牌靛蓝一点点），彩色只用来说三件
事 —— 这是个动作、这是选中的、这是状态。从前灰是纯中性的，与靛蓝主色摆
一起显得脏；各偏各的又会像没校准过，所以现在是**统一偏一个方向**。

**层次**：底 → 卡片 → 填充 → 浮层四档表面，深色下越浮起来越亮（从前有几
处反着来，弹层比背后的内容还暗，看着像陷进去了）。前景三级全是实色，深浅
不再靠透明度调 —— 半透明的字压在不同底色上灰度会飘。

**圆角**：窗口 14 / 卡片 13 / 气泡 11 / 行 9 / 徽标 7，五档，每处圆角都答
得出自己是哪一档。

**一处渐变**：可以发送时的发送键。全产品只有这一个，所以它一定是那个主要
动作。

### 界面上多了两样以前没有的东西

**⌘K / Ctrl+K 命令面板**：一个框里搜会话、跳页面、@ 文件、/ 技能，四个前缀
各管一路。

**右栏三页签**：文件 / 本轮改动 / 终端。长 diff 与 shell 输出从前只能挤在
气泡里看，或者点开一个把对话挡住的弹层 —— 它们本来就是**对照着对话读**的
东西。窄屏上从底部拉起来（右边缘被系统手势占着）。

### 「在等你确认」现在跑不掉了

一轮跑到一半停下来问你，从前只在会话行上有个点；切走或者列表滚远了就看不
见，而这是唯一「不管就永远卡住」的状态。现在顶栏有一颗「等你确认 · N」，
点一下直达那条会话。

### 一批看得见的修

- 会话列表的「最近」是**纯时间线**了 —— 从前它还按项目再分一层组（带一个
  「未分组」的组头），两个维度叠在一段里哪个都拿不稳
- 会话行前面那个绿色文件夹小图标删了。它说的是「这条会话绑了工作区」——
  一个用户看不懂、也不需要在这里知道的实现细节
- 项目卡片的 ⋮ 菜单从卡片正中间回到了右上角
- 确认框里的命令**现在真的是等宽字**了。从前在 Windows 中文环境下落进
  雅黑用比例字体渲染，而 `rm -rf /tmp/x` 与 `rm -rf /tmp /x` 差一个空格，
  比例字体下看起来一样 —— 你批准的正是你看不准的那条
- 确认框的三档风险不再被一个红色标签拍平：读取档整张卡是琥珀的
- 好几处**警示是绿色的**（离线横幅、「仅本地」、「云端跑不起来」）。这套
  主题从没定义过 `tertiary` 这一档色，Flutter 静默回落成了状态绿 ——
  于是「当心」穿着「一切正常」的衣服。全部改成琥珀
- diff 的增删两色、LIVE 徽章、同步点、附件就绪、MCP 状态点曾经是**三种不
  同的绿**，其中两种在浅色底上读不到 4.5:1。现在是同一个
- 深色主题下工具输出的 ANSI 颜色换了一档亮的 —— 编译报错那几行红字从前
  在深底上是最读不清的
- 项目页与图片墙拉取失败时不再谎报成「还没有项目 / 还没有画过图」，而是说
  「拉不到」并给一个重试
- ⌘K 里 ↑↓ 不再把选中项移出视口（Enter 会执行一条你看不见的结果）
- 图片墙的格子终于有点击反馈了 —— 水波纹从前被不透明的图整个盖住

### 界面上不再宣传长期记忆

空会话的欢迎屏与顶栏副标题从前写着「记忆原生的 AI Agent」「抽取出的事实
可追溯、可审计、可回放」。长期记忆 2026-08-17 整条搬去了 Cormex，这一侧
连界面都没有了 —— 留着那句话，你问一句「上次说的方案」得到的是全然不
记得，而你会认定产品坏了，不是能力下线了。

### 「新对话」不再留下空壳

点「新对话」**不再当场建一条对话** —— 会话在你发出第一条消息时才创建。
从前点一下就往左栏塞一行「新会话」，于是每次「我看看能干什么」都留下一条
自己从没说过话的对话。

从项目里点、或者从智能体卡片点进来的，那个归属会跟着走：真开口时建出来的
对话仍然落在那个项目下、用那个人设。

⚠️ 顺带修掉一个更难发现的：**从图片页或项目页点「新对话」会被弹回上一条
对话**。「回聊天页就恢复上次那条」与「开一张白纸」在同一次点击里打架，
症状是「点了没反应」。

### 输入框能粘图了

截个图 Ctrl/⌘+V 直接粘进输入框，模型就能看。从前只能点回形针选文件。

⚠️ 剪贴板里**同时有文字和图片时优先粘文字** —— 从网页复制一段带插图的
内容就是这种情况，而十次里九次你要的是文字。

拖文件、选文件、粘贴这三个入口现在在「新对话」的白纸上也能用（从前它们在
没有会话时是禁用的）。

### 左栏：三段常驻

「项目 / Pinned / 聊天」三段现在**空着也画出来**，各自能收起展开。

从前空的段整个不画，理由是「常年空着的标题只是噪音」。代价是：置顶对话与
置顶项目在界面上只有段头这一个入口 —— 段头不画，你就不知道能置顶，
于是永远没有置顶，于是段头永远不画。空段下面会有一行字说明这里能放什么。

### 电脑操作有了自己的设置项

原先压在「连接器」页最下面，要往下滚过一整列 MCP server 才看得见。
别的连接器交出去的是一台别人写的 server，只有这一条交出去的是你这台机器的
屏幕、鼠标和键盘 —— 它该有自己的一页。做不到的部署上，这一项整个不出现。

### 「登录已过期」现在说得出原因（日志里）

红字对你没变，但日志里会写清是三条路径里的哪一条（刚续期又 401 的熔断 /
手上没有 refresh token / 服务端真的拒了）。这三条的病因毫不相干，
而此前它们在日志里长得一模一样。**这是诊断，不是修复** —— 真正的修法要等
复现出来那一行日志。

## [0.1.19] - 2026-08-23

左栏从「一列会话」变成**三个入口 + 三段树**，而那三个入口后面是这一版的
主要内容：智能体、技能、连接器。外加桌面端的电脑操作（默认关）。

### 智能体：一份存下来的人设

左栏第三个入口，一面卡片墙。别家各叫各的（专家 / 智能体小助手 / 助理 /
搭档），是同一个东西：一段身份描述 + 一个默认模型，存下来，随时挑一个用。

点卡片 = **用它开一条新对话**，不是切换当前会话的人设。撰写框那个 chip
在这条会话说过话之后会画一把锁 —— 不是界面卡了，tooltip 会直说为什么：
历史里模型已经以旧身份说过话，中途换掉，整段对话前后就不一致了。
点那把锁另开一条。

### 技能：目录常驻，正文按需取

给模型的「做法说明书」。名字与一句话说明每一轮都在，正文等它判断用得上
才取回来 —— 十份做法全塞进提示词也能跑，只是每一轮都在为那九份用不上的
付钱。

⚠️ **「说明」那一栏留空，这份技能就永远不会被取用**，而且没有任何征兆
（模型看不到它，也就不会去调）。编辑器里那行 helper 就是说这件事。

### 连接器：接什么，而不是怎么接

设置页那一列原来叫「MCP 与工具」—— MCP 是协议名，说的是「怎么接」；
你在那一列里找的是「接什么」。改叫「连接器」，协议名留在页内。

列表上面多了一排**一键精选**：浏览器（Playwright）、GitHub、
Context7 文档、PDF。点一下就接上，省掉「叫什么包、参数怎么写」那一步。
要 key 的那条提前挂一把钥匙图标，问你要的时候会说清去哪儿拿、存到哪儿。

**没有第二套机制**：接出来的就是一台普通的 MCP server，同一份
`.mcp.json`、同一个详情页、同一条删除路径。原来那些（粘贴添加、浏览
注册表）一条没少。

### 电脑操作（桌面端，默认关）

五个工具：截屏、点击、输入文字、按键、滚动。开了之后模型可以看你的屏幕
并操作鼠标键盘。

⚠️ **这是这个产品里权限最大的一组。** 别的工具都有围栏（文件工具受工作区
限制、shell 在沙箱里），这一组动的是你整台机器上正在运行的一切。所以：
默认关；每一次调用都要你确认，**截屏也不例外**（一张截图里有屏幕上的
一切，离开这台机器就收不回来）；模型看不懂图时这一节根本不画出来 ——
否则那一轮的实际经过是「拍了你的屏幕 → 发给供应商 → 对方回一句不支持图片」。

只在 Windows 与 macOS 上有。Linux 桌面用不了，界面会照实不画。

### 左栏：三个入口，三段树，项目有了自己的一页

上面是聊天 / 图片 / 智能体三个入口，下面是**置顶 / 项目 / 会话**三段。
置顶跨设备同步。收起按钮挪到了侧栏自己身上。

**项目页**是新的：一面卡片墙。在它之前，没置顶的项目没有任何入口 ——
你得先记得它叫什么，再在一列会话里找那一行。

⚠️ 一条会话只出现在一段里，顺序是 置顶 > 属于置顶项目 > 其余。

### 图片看得清了，图库也不再卡

对话里的图从 132×96 放大到 384（单张）/ 224（多张）；图库格子从 180
放宽到 330。而且**不再裁**（`contain` 而不是 `cover`）—— 从前裁一刀之后
你看到的构图跟模型画的不是一回事，在 132×96 上根本判断不了画对没有。

图库从前**一次把整页原图全下下来**：一页 24 张、每张 1.6–2.7 MB，
约 50 MB。本机回环上看不出来，走公网就是几十秒白屏。现在按需加载，
解码宽度也按屏幕真实像素算（不再写死，高分屏上不会发糊）。

### 自托管：部署改用 Ansible，节点上的 `.env` 拆成两个文件

⚠️ **只影响跟着 [docs/deploy.md](docs/deploy.md) 部署的人**，用
[docs/install.md](docs/install.md) 那条 docker compose 的不受影响。

从前改一次 `deploy/docker-compose.yml`，就得有人带外 `scp` 到节点上，
忘了的表现是**整条流水线跑二十多分钟、最后一步红**。现在 compose 与
`.env` 都由 ansible 送过去，那道分叉消失了。

节点上现在是两个文件：`/data/cortex/.env`（ansible 每次部署渲染，
**手改会被冲掉**）与 `/data/cortex/.env.secrets`（凭据，人工写一次，
ansible 从不碰）。凭据一步都不进 CI。

⚠️ 因此**节点上不能再裸敲 `docker compose`** —— 它只读 `.env`，会报
`required variable CORTEX_PG_PASSWORD is missing a value`。用
`/data/cortex/dc`（装机时生成的包装，带上了两个 env 文件）：

```bash
cd /data/cortex && ./dc ps
```

## [0.1.18] - 2026-08-22

图片这块从「一个表单」变成**一条对话**，画出来的图也变成能拿在手里的东西。

### 图片生成就是对话

图片页现在复用与聊天页**完全相同**的那一套（对话流、输入框、附件、
确认弹层）。你说「画一只戴眼镜的柴犬」，它调生图模型，图挂在回答上 ——
那条会话照常出现在左栏，跨设备也照常同步。

生成的那几十秒里，图**将来的位置**上有一块缓慢明灭的点阵在动；图回来
它就换成图本身。开了「减少动效」的话是一块静止的点阵加一行「正在创建
图片」，而不是一个跑不动的动画。

输入框底下那两个 chip 管这一次用哪个绘画模型、出几张、多大。

### 修正：画好了却看不见

模型说「画好啦」，而屏幕上一张图都没有 —— 要换个会话再回来（或者重启）
才出现。流式那条路从来不知道这一轮往回答上挂了什么图，收尾时拼出来的
那条消息是空的。现在服务端在结束那一帧就把它带过来，图**当场**出现。

### 一张图能做的事：对话里和图库里完全一样

任何一张图上右键：**复制图片**（能直接粘进 Word）、**另存为**、
**复制图片链接**、**从图库移除**。点一下放大，放大之后底下一排能缩放、
左右各转 90°、水平/垂直翻转、复原。

⚠️ 旋转与翻转**只影响你看到的样子**，复制 / 另存 / 分享出去的都是原图 ——
转过之后界面上会把这句话写出来。

### 修正：「另存为」现在真的存得下来，而且说得出存在哪

从前它静默往「下载」文件夹里写，界面只弹一句「存好了：cortex-xxx.png」——
你既没选过位置、也不知道去哪儿找。现在弹系统的「另存为」对话框，存完
报的是**真实路径**。

### 「复制图片链接」是一条真能发出去的链接

从前复制的是对象存储的临时直链，对着容器内网名签的，外网根本打不开。
现在是一条**免登录、长期有效**的公开链接，随时能「停止分享」（那条链接
当场失效）；已经分享出去的在界面上一眼看得见。

⚠️ 复制时会说清它是公开的。如果这个部署只有本机地址（比如自己在电脑上
起的 dev），还会补一句「只有这台机器上打得开」—— 免得你发出去之后对方
打不开却以为是分享坏了。

### 图库：相册与多选

「我的图片」上面多了一条相册过滤器与「新建相册」。Ctrl / ⌘ 点（或长按）
进多选，勾中之后可以批量加入相册、移出相册、从图库移除。

**「从图库移除」不是删除**：图本身还在，对话里那张照常显示。删相册也
一样，里面的图一张都不会少。

### 修正：图库找不到了

图库原本只画在空会话上，一开口说话它就让位给对话，而回去的唯一办法是
点「新对话」—— 没人会把「新建」读成「回到我的图片」。现在它在页头有
自己的入口，任何时候都按得到。

## [0.1.17] - 2026-08-22

修 0.1.16 里三处「看起来正常，其实不是」。

### 修正：型号 chip 点了没反应

图片页上那个挑绘画模型的按钮，**第一次点什么都不会发生** —— 面板在模型
目录还没拉到时会静默早退，既不弹也不说。现在它会等目录；真拉不到就说
一句，而不是让你对着一个不响应的按钮怀疑自己点歪了。

### 修正：挑绘画模型时不再警告与它无关的事

`gpt-image-2` 下面挂着一整段「官方那边这个型号不支持工具调用 —— 能不能跑
agent 得试一下」，而生图这条路根本不调工具。那段话把一个实测能画的型号
显得像有问题。

同一处：查不到上下文长度时不再画「上下文 —」。一个横杠在一行数字里读作
「0」或者「不支持」，而事实是目录里没有它。

### 修正：画廊拉不到时有条出路了

图片页的画廊只在打开时拉一次。第一下赶上网络抖动，那条红字就一直挂着，
只能重启应用。现在旁边有「重试」。

## [0.1.16] - 2026-08-22

这一版把**生图**从「藏在对话里的一个工具」变成一个能直接去的地方，
顺带修掉了一路上发现的几处「界面说了一件它做不到的事」。

### 新增：侧栏多了「图片」

左栏顶上现在有一小块导航（新建会话 / 图片），下面才是会话历史。
点「图片」进去是一个独立的页面：描述一张图 → 挑型号 → 设规格 → 生成，
画出来的都留在下面那面「我的图片」墙上（**在对话里让它画的也算**）。

点开任意一张可以放大、复制图片、下载、复制链接，或者
「以此为提示词重画」。

它不叫「编辑」是有理由的：那个按钮在别家做的是 img2img（把原图连同新指令
发回去，在**这张图上**改），而我们三条生图协议都没有带图输入的路。叫编辑，
你会写「把领结换成红色」，然后拿到一张构图完全不同的图 —— 那不是模型
没听懂，是我们从没把那张图发过去。

「复制链接」会告诉你它 **15 分钟后失效**：一条发出去打不开的链接，
比「复制不了」更坏。要长期分享请用「下载」。

### 修正：绘画模型一个都选不出来

「设置 → 默认模型 → 绘画模型」点开只有「不指派」，一个候选都没有 ——
而同一分钟里 `gpt-image-2` 明明刚画出一张图。

判定「这个型号能不能生图」的那道门和真正发请求的分发器看的不是同一组
输入：分发器在你自己填了端点的来源上直接走聊天协议、根本不看供应商是谁，
而那道门却拿供应商白名单把 OpenAI 一类整个挡掉了。现在两处同源。

自定义端点（中转站 / 网关）上还认了 `gpt-image` / `dall-e` 这两族名字。

### 修正：「生成数量」不再是个调了没反应的控件

三条生图协议里只有通义千问的请求带得了「出几张」。另外两条（Gemini、
你自己填端点的那种）此前**默默只出一张** —— 你调到 4，回来还是 1 张。

现在由服务端连发几次凑够，界面上会说清楚：那意味着 n 次调用、n 份钱、
n 倍的等待。中途坏了几次也会把成了的那几张给你，不会整批丢掉。

⚠️ 图片规格里**没有**「质量」与「背景」两项。那是 OpenAI 官方生图接口的
参数，我们三条协议一条都没走它 —— 摆出来就是一个发不出去的控件。
尺寸在中转站上也只能写进提示词，那一条界面会标出来。

### 变更：设置改成整屏一页，模型服务重做

设置不再是一个对话框，而是整屏一页、左上角一个「← 返回」。最挤的
「模型服务」因此能铺开：左边来源列表（分已启用/未启用）、右边详情，
带品牌图标、连通性检查、以及「这家有哪些型号」的完整目录。

模型列表分两层：主列表只放**你在用的**，全集在一个随叫随到的右侧抽屉里。
一把中转站的 key 能拉回 281 个型号，摊在主列表里就是 280 行灰的。

### 修正：「部署提供」那条的两个开关是真的了

总开关此前会被服务端拒掉（弹一句「非法输入」再弹回原位），每个型号旁边
那个开关**点了什么都不会发生**。两个都修好了。

### 修正：「本来就这样」不再画成红色

在「部署提供」上点「获取模型列表」会红一行，说的却是「这条是服务端配的，
型号来自它的供应商定义」—— 一个恒真的事实。红色被这样磨钝之后，真拉不动
那次也就没人看了。现在只有**本该做到却没做到**才是红的。

换一条来源时上一条的报错也会跟着清掉，不再挂在别人的详情页上。

## [0.1.15] - 2026-08-21

这一版几乎全是**「看起来正常，其实不是」**这一类问题：产品里那些说得出口
却不成立的话、查故障时给出假信号的判据、以及一个用了三个月才有人发现
自己在对着旧构建找新功能的窗口。

### 修正：中转站上的生图与工具调用不再被误报

0.1.14 里那句「它能生图，但我们还没接这家的生图接口」**在中转站上是错的**。
中转站普遍把生图包装成普通聊天（`POST /v1/chat/completions`，回一段带图
链接的 markdown），那条路我们本来就走 —— 也就是说 `gpt-image-2` 这类型号
在你自己填的端点上**早就能画**，而界面在劝你别加。

工具调用那句同理。目录描述的是**厂商官方接口**；端点是你自己填的时候，
那份描述与端点后面那个东西没有任何关系。现在自定义端点上改成「官方那边
这个型号不支持工具调用。你这条来源是自定义端点，后面接的是谁我们不知道
—— 能不能跑 agent 得试一下」。

### 新增：每条回复标出这一轮实际用的模型

「自动」档每一轮都可能挑不同的模型，而此前你完全看不出这一句是谁答的、
花了谁的钱。一轮里换过几个就按顺序列几个（连续重复的会合并）。

老消息与导入的历史**什么都不画** —— 不知道就是不知道，猜一个默认模型
填上去等于对历史撒谎。

### 新增：`cortex doctor`

出问题时第一条该跑的命令。查七项：CLI 版本、盘上真正存着的地址、凭据形态、
两条健康路、本机 agent 二进制版本、启动记录、本地数据占多大。

**它是 CLI 而不是设置页里的一个面板**：桌面端起不来的时候，设置页正好是
够不着的那一半。`--json` 给报障用 —— 贴一段比来回问五轮快得多，里面
不含任何凭据。

### 新增：设置里承认「本机 agent」的存在

桌面端**不是一个进程，是两个**：界面，与它拉起的 `cortex-local`（工具、
读写文件、跑命令全在后者）。后者的端口每次由系统随机分 —— 而此前产品里
没有任何一处承认它存在，于是设置页写着 `https://…/api`、报错却说
`127.0.0.1:9826`，除了「串台了」得不出别的结论。

连接页新增一节：在不在跑、它的版本与界面一不一致、上面那节数字是谁答的。

### 新增：本机 agent 的启动记录

每次拉起 / 就绪 / 退出往 `%LOCALAPPDATA%\cortex\logs\agent-launch.jsonl`
记一行，跨进程累积、封顶 256 KiB。崩溃循环时每一轮的死因此前都被下一轮
抹掉，而用户看到的只有界面上一句「连不上」。`cortex doctor` 会读它。

### 新增：卸载时问一句「本地数据要不要一起删」

把路径和它当前占了多大摆出来让你选，**默认保留** —— 卸载常常只是
「重装一次试试」的中间步骤。静默卸载（自动更新走的那条）一律保留。

### 改进：登录第一屏不再问「部署入口地址」

那个问题**只有自托管的人答得上来**，而它此前是第一屏第一个字段。现在收进
一行「连接到你自己的部署」，点了才出现；连不上时自动展开（那一刻地址就是
最可疑的东西）。**Web 上完全不给** —— 那份构建走同源根路径，填了反而会
把它弄坏。

同时**记住你用过的部署**：换回另一个不用再凭记忆重打一串 URL。

登录页整体重做了一遍：产品图标、居中排版、更紧凑的离线卡片。

### 改进：离线模式说清自己的边界

此前只说「不会同步到服务端」，没说**也看不到以前的会话**（历史在服务器
上，接上就全回来）。会话列表顶上现在会显示还有几条在补写 —— 那个数字
此前只在设置里，离「我的对话不见了」这个疑问四次点击远。

离线时改标题 / 移项目 / 归档会**事前**灰掉并说明，而不是等你打完字点完
确定再弹一个报错。

### 安全：本机 agent 拒绝浏览器直连

它绑 `127.0.0.1`、能跑 shell，而本机开发形态下可能根本不认证。攻击者把
自己域名指向 `127.0.0.1`，浏览器上那个页面就能同源打进来（DNS rebinding）
—— 端口随机从来不是一道认证。带 `Origin` 头的请求一律 403；桌面端与 CLI
都不带这个头，云端沙箱形态不受影响。

### 这一版仍然不能干什么

- **没有长期记忆。** 记忆整条拆去了 [Cormex](https://github.com/weironz/cormex)，
  这一侧联不联网都不做记忆注入。0.1.14 之前界面上还留着几处相反的说法，
  这一版清干净了。
- **离线时读不到历史会话。** 能对话、能用工具、这几轮会补回去，但看不见
  过去 —— 会话列表读的是服务端。
- **官方 OpenAI / xAI 端点上的生图仍然没接**（`gpt-image-*`、
  `grok-imagine-*`）。走中转站可以，见上面第一条。

## [0.1.14] - 2026-08-20

这一版几乎全是**模型**这一块：从「一个部署一个模型」变成一份你自己管的
来源列表，能挑型号、能设默认、还能画图。

### 新增：模型来源统一成一份列表，取代三个概念

此前设置里有三样东西：「跟随部署」「自己的 API key」「本机模型」。它们
结构上本来就是同一个东西（供应商 + 密钥 + 端点），只是**存储位置**不同 ——
而位置是我们的实现细节，不该由你来分类。

现在只有一份列表，第一步永远是「添加模型」。部署提供的那条也在里面，
只读、不可删，但**能关** —— 藏起来的话，一个零配置就能聊的新用户会以为
自己什么都没有；不给关，一个自带 key 的人就没办法阻止某些对话去花
服务端的钱。

供应商改成**下拉**（十家实测能连的），不再手打：填对完全靠猜中我们内部的
id —— `claude` 不行要写 `anthropic`、`kimi` 要写 `moonshot`，而猜错的反馈
要等到点保存之后。端点也**暴露出来**了，提示写的是那家的实际地址
（`留空 = https://api.deepseek.com`），公司网关 / one-api / 自建 vLLM 都填这里。

型号不再是写死的清单，而是**去问供应商**（「获取模型列表」）。一把
Qwen 的 key 拉回来 240 个型号，所以配了搜索与**能力筛选**（能跑 agent /
看得懂图 / 能生图 / 有思考），挑你要的加进来。拉不动时会回落到内置那份
并**明说**是回落 —— 悄悄回落的表现是「我点了获取列表，它给了我一份看起来
像样、其实是编译期写死的清单」。

⚠️ **顺带修掉一个此前一直在的错**：自带 key 是 A 家而部署配的是 B 家时，
三档里有两档是坏的（把 B 家的型号名发给 A 家、或者拿 A 家的白名单去拒 B
家的型号）。根因就是「模型跟部署走、key 跟用户走」这个割裂。

### 新增：能画图了

对话里直接说「画一张…」就行 —— 它是一个 `generate_image` 工具，不是
模型选择器里的一个选项。**生图与对话是两条协议**：把 `qwen-image` 选成
对话模型，每一轮都会失败。

生成的图直接进你的会话附件，与你自己上传的图**走同一条路** ——
渲染、同步、跨设备都一样。

已接两家：**通义千问**（qwen-image / wan / z-image 系列）与
**Google Gemini**（gemini-\*-image）。

⚠️ **目录里说会画、而我们还没接的那些会说清楚**（现在是 OpenAI 的
`gpt-image-*` 5 个、xAI 的 `grok-imagine-*` 2 个）：列表里那一行写着
「它能生图，但我们还没接这家的生图接口 —— 加进来也画不了」。不写的话，
它读起来像「这个模型不会画画」，既是错的，又把责任推给了模型。

### 新增：默认模型（主 / 快速 / 绘画）

设置里多了一页，配「不选的时候用哪个」。撰写框下面那个选择器管的是
**这一句**用哪个，关掉窗口就该忘；这一页管的是账号级的默认。

真正缺的是**快速模型**那一档：会话命名、内容抽取这些后台活儿
**根本不经过你**，此前只能用部署配的那个 —— 一个自带 key 的人没有任何
办法让它们走自己的账户。

**没有「翻译模型」**：我们没有翻译功能，摆一个没人调用的角色出来，
你会以为自己配的东西在起作用。

### 新增：模型切换挪到了撰写框下面

不用再进设置。按来源分组，每条写着占不占配额。

### 新增：离线也能用你自己的 key（桌面端）

加来源时顺手在这台机器的系统凭据库里留一份，断网时直接能用，没有多余
一步。⚠️ 代价说清楚：**在另一台设备上加的来源，这台机器离线用不了** ——
密钥从不明文离开服务端，只有你在这台机器上亲手填过的那次才留得下。
想让它能离线用，把密钥重填一遍即可。

### 变更：设置窗大了，「模型服务」照 Cherry Studio 重做

设置窗此前无论屏幕多大都是 940×660，而「模型服务」那一页里面自己还是
两栏，右边的型号列表挤成一条缝。现在跟着屏幕走。

### 新增：能选模型了，还有一个「自动」档

设置 → 模型里多了一个选择器。此前模型由服务端的环境变量定死，**一个部署
一个模型，用户根本选不了** —— 而界面上那一页只有「自己的 API key」和
「本机模型」两项，都不是「用哪个模型」。

内置九家：Claude / ChatGPT / Gemini / Grok / DeepSeek / GLM / MiniMax /
Qwen / Kimi，外加 goose 目录里那三十多家。每个模型旁边写着上下文窗口、
每百万 token 的价格、看不看得懂图、有没有思考 —— 这些来自一份 5184 个
模型的目录，编译期嵌在二进制里，查询不走网络。

⚠️ **不支持工具调用的模型选不了**，并且说清为什么：那样的模型跑 agent 会
流畅地回答而一个工具都不调 —— 读不了文件、跑不了命令，而界面上看不出
任何区别。而目录里查不到的模型**照样能选**，只是标一句「不知道它支不支持
工具调用」——「不知道」与「不行」是两回事，当成不行会把能用的模型挡在外面。

**自动档**：每一轮按这一轮的特征挑 —— 要不要工具、有没有图、装不装得下，
在**能干这活的模型里选最便宜的**。实测一句「hi」它挑了 Flash 而不是 Pro
（便宜三倍），账也记在 Flash 上。

界面上写的就是这句话，不会写成「智能匹配最优模型」：我们没有任何办法知道
哪个模型对某个具体问题答得更好，那需要一个评测集与一个判定器，两样都没有。

选择逐轮携带，与权限档同一个路数 —— 在设置里换完，**下一句**就按新的走。
Web 端与桌面端走同一条路（`/llm/stream` 现在带模型名），CLI 是 `--model`。

指定一个这个部署没开放的模型会被**明确拒绝**并列出可选项，不会悄悄换成
别的 —— 后者会让你拿着一个以为是 Claude 的回答做判断，而账单对不上。

### 变更：用量页的价目从两个型号扩到四千八百个

价目此前是手写的一张表，只有两个 DeepSeek 型号（而且数字还是错的）。
现在来自同一份模型目录，5184 个模型里 4805 个带价。

⚠️ 目录的价目单位是**美元**。没有配 `CORTEX_USD_RATE` 时，用量页整个用
美元显示，而不是按某个随手挑的汇率折一个数出来 —— 那种金额你没法判断
对不对，也不知道该按哪天的汇率复核。配了汇率之后界面上会写明按多少折算。


### 修复：每次启动都要重新登录（存过自定义地址的人一直如此）

登录页上写着「登录状态会记住 30 天」，而实际上**存过自定义部署地址的用户
每次打开都回到登录页** —— 也就是所有自建部署的人。

根因是启动时的一个竞态，两半都没错，凑在一起才坏：

1. 上次那个地址是异步从磁盘读回来的（`AppConfig` 先给编译期默认值，
   再排微任务去读）
2. `AuthController` 监听地址变化，一变就重置认证状态（换了服务器，
   旧凭据本来就不该带过去）
3. 而启动时那次「读凭据库 → 续会话」**比读设置慢**（要跨进程问钥匙串），
   它回来时发现状态被重置过，就把自己判成「已被取代」直接返回

结果：续期请求一次都没发出去。服务端那侧的证据很干净 —— refresh token
的 `rotated_at` 恒为空，从签发到过期一次轮换都没有过。

现在启动时先等地址落定再开始续期。顺带修掉同一条路上的第二个问题：
续期时锁内那次「重读凭据库」失败会把整次续期一起带走（异常穿出去，
表现是请求根本没发），现在它失败只记一行日志，用手上这枚继续续。

这条路径此前的测试用一个覆写了 `build()` 的替身替掉了 `AppConfig`，
于是那段异步恢复从来没被跑过，测试一直是绿的。

### 修复：`just ci` 比 CI 多红两到十六条

`just ci` 号称「本地跑一遍 CI 的全部检查」，而它比 CI 多两件事：`.env`
被灌进每条 recipe（其中的 `CORTEXD_TOKEN` 会让两条凭据恢复测试走错路），
以及本机通常起着 dev（于是两个 live 套件真的去打那个后端，红不红取决于
此刻部署的状态）。两件都复现掉了，现在起着 dev 也全绿。

要跑那两个 live 套件用 `just flutter-live`。


## [0.1.13] - 2026-08-19

### 新增：派出去的那一轮跑完了，回来看得见

此前跑完的表现是侧栏徽章从「在跑」变成**什么都没有** —— 而「什么都没有」
与「从来没跑过」长得一模一样。于是「派出去干活」这个能力在观测侧缺最后
一格：活干完了没人告诉你，只能隔一会儿点进去看一眼。

现在人不在那个会话上时跑完，侧栏会留一个勾（用勾不用第二个圆点：两个只有
颜色不同的点在色弱视角下与「在跑」分不开，而这两件事的下一步完全相反），
同时弹一条带「去看看」的提示。打开那个会话就清掉。

⚠️ **这是应用内的通知，不是操作系统级的**：窗口最小化、或者浏览器标签页在
后台时看不到。那一半要引入通知插件与权限流程，是独立的一件事 —— 在它落地
之前这里不声称「会通知你」，只声称「回来就看得见」。

### 新增：输入框里 `@` 引用工作区文件

在输入框里敲 `@` 会弹出这个会话工作区里的文件（本机绑定的目录、或者云沙箱
的卷，两支都支持），继续打字过滤，上下键选、回车或点击插入。插进去的是
相对工作区根、斜杠分隔的路径 —— Windows 上那串反斜杠原样发给模型的话，
它看到的是一个只有本机才懂、而且在提示词里还会被当转义看的字符串。

只认**词首**的 `@`：邮箱地址、`user@host`、Dart 注解都带 `@`，都弹一个文件
列表出来的话输入框就没法用了。没绑工作区的会话整个不弹 —— 一条引用了文件
的问题发给一个没有文件工具的 agent，回答会一本正经地跑偏。

`node_modules` / `.git` / `target` 这类目录按名字跳过（等发现一个目录有三万
个文件时，那三万次读已经做完了），清单封顶 800 个；到顶时列表底下会直说
只收了前 800 个，免得用户搜不到就以为文件不存在。

### 新增：把一段会话导出成 Markdown 或 JSON

侧栏每条会话的「⋯」菜单里多了两项。Markdown 给人看（贴进笔记、发给别人、
存档），JSON 给程序看（保留 id 与工具调用的结构）。

工具调用**写进去**：少了它们，一份「agent 帮我改了三个文件」的存档里
只剩下「好的，改完了」—— 而改了什么正是这份存档的价值。失败的调用会标出来。
附件带上哈希：字节没有跟着导出来，哈希是把它找回来的唯一线索。

⚠️ 导出的是**整段**历史，不是屏幕上加载了的那一页。后者会让一段几百轮的
会话安静地只导出最后几十条 —— 文件能打开、格式完全正确、就是少了一半，
而用户直到去找某句话找不到时才会发现。真的长到翻不完时，提示里会直说
这份存档不完整。

### 新增：一轮失败之后有「重试」，自己说过的话能改一改再发

出错那一块的红框右边多了「重试」—— 它重发的是**前面那句用户消息**，
而不是让你把刚才打的字重新敲一遍（失败的原因通常与你打的字无关）。

自己的每条消息下面多了两个动作：原样再发一次、以及把这句话放回输入框
改完再发。附件跟着走 —— 一条「看这张图」重发时丢了图，那一轮必然答非所问。

⚠️ **它不叫「编辑」，因为它真的不是编辑。** 这里的历史是 append-only，
改完之后是**新的一轮追加在末尾**，原来那条留在原地。别家那种「编辑后
把旧的换掉」在这里做不到，而只在客户端把它藏起来更糟：换一台设备回放，
那条「已经改掉」的消息原样还在，而用户以为它没了 —— 他可能已经在里面
写过不该留下的东西。

顺带修掉了同一处的老毛病：顶部错误横幅上那个「重试」以前会把失败那一轮
**从屏幕上抹掉**再重发 —— 而服务端两条都在，刷新一次页面它们原样回来。
现在它与气泡上那个走同一条路：追加，不改写。

### 新增：看得见自己用了多少 token、花了多少钱

设置里多了「用量」一页：最近 30 天（滚动，不是自然月）的花费、token 总量、
配额进度条、以及逐模型的明细。

服务端从开放注册那天起就在记这笔账（每次 LLM 调用一行），而在这一页之前
**没有任何地方看得见它** —— 用户唯一会知道自己用了多少的时刻，是撞上配额
被拦下那一次。

⚠️ **没有价目的模型显示「没有价目」，不是 ¥0.00。** 后者读起来是「免费」，
而事实是这个部署不知道它多少钱 —— 那两件事在界面上必须长得不一样，否则
用户会按一个编出来的数字做决定。总额里少算了哪一部分，页面上当场说。

价目在 `CORTEX_MODEL_PRICES` 里配（`模型=输入/输出`，元 / 百万 token），
不设就用内置那张只有 DeepSeek 两个型号的小表。本机模型明写 `=0/0` ——
「真的不要钱」与「不知道多少钱」是两回事。

### 新增：搜索会话与消息

侧栏顶上多了一个搜索框，搜的是**标题与全部消息正文**，不只是当前这一段
对话。结果按会话聚合（一个会话一行，带命中条数与一段上下文），标题命中的
排在最前，命中的词在结果里高亮。

搜索在服务端做 —— 消息全在库里，客户端手上只有当前会话的一页，拿它去搜
等于「只能搜到刚才看过的东西」。已归档的默认不出现在结果里，与侧栏列表
同一个默认。

⚠️ **中文能搜到**，这一点不是免费的：Postgres 自带的分词器把连续 CJK 当成
一个词（`to_tsvector('simple','今天天气很好') @@ websearch_to_tsquery('simple','天气')`
是 `f`），照着 `tsvector` 那套写出来的搜索**编译得过、跑得动、什么都搜不到**。
这里走的是 `pg_trgm` 三元组索引 + `ILIKE`，中英文一视同仁。

老服务端没有 `/sessions/search`，那里搜索框整个不出现 —— 而不是给一个
点下去只会报错的框。

### 修复：桌面端打开云端会话时，文件栏说「你还没绑定目录」

而 agent 明明刚在云端建完文件。那块面板的判据是「这是不是 Web 构建」，
而不是「**这个会话**的文件在哪」。同一个判据还有两份拷贝（沙箱文件树自己、
以及「下载工作区」按钮），都按构建把自己藏了起来 —— 于是桌面端的云端会话
既看不见文件，也没有把产物拿回本机的入口。

现在判据只有一处（`session.workspace == null`），与底部那个「云端」标签、
以及服务端决定这一轮在哪儿跑的那个判据，是同一件事。

### 修复：同时开两个云端会话，先开始的那一轮会被打死

症状：文件改完了，回答却进不了历史 —— 那一轮以一句
「这把凭据只能写它绑定的那个会话」收场。触发条件很日常：在 Web 上同时开着
两个会话（两个标签页，或者一个跑着长任务时你去另一个说句话）。

根因是授权的粒度比容器细一格：一个容器服务同项目下的全部会话、共用一个卷，
而那把凭据只认签发时那一个会话，于是每来一个新会话就得改绑，把上一个还在跑
的那一轮打死。现在判据换成「这个会话属不属于这个容器」，跨项目仍然硬拒。

### 修复：「停止生成」从前只停了客户端，服务端那一轮照跑

按下之后屏幕上写着「已停止生成」，而模型还在烧 token、工具还在按它的意思改
文件 —— 那个按钮只取消了客户端的订阅。

现在它真的把服务端那一轮掐掉：闸门随之放开（排在后面的那条自动开始），
半截回答不落库（与「模型中途出错」同一条规矩）。停不掉时**照样收界面**，
但把原因写在那条消息上，而不是给一句干净的「已停止」。

⚠️ **停不掉的是已经派出去的子进程**：一条正在跑的 shell 命令不会因此消失。

### 修复：Web 上「关掉浏览器回来接着看」从来没生效过

客户端只打 `/runs/{id}`，而 agentd 只有带 `/sandbox` 前缀的那条 —— 于是 Web 上
重挂打的是一条不存在的路由，404 被当成「没在跑」静静吞掉。加了裸
`/runs/{id}`，两端从此是同一个名字。

### 修复：agent 刚写完的文件，在文件栏里看不见

那棵树列过一层就记住（懒加载的全部状态就是这份缓存），而最常见的那件事恰恰是
「agent 说它写了 report.md，我看看」—— 打开树，里面没有它。真机上撞到过：
切会话没用，**只有把整栏关掉再打开**才重列。

两条一起做：

- **一轮跑完自动重列**已展开的那几层（折叠着的不动 —— 为看不见的目录发请求，
  等于把懒加载退回急切加载）。判据是「流从有到没有」，不是工具事件：那样得先
  认出哪些工具会写文件，而漏认一个就是一次静默的不刷新
- **底部多一个刷新按钮**。文件也可能由别的东西改（编辑器、git、另一台设备上的
  同一个会话），那些没有「一轮结束」这个信号

正在预览的那个文件也跟着重读 —— agent 刚重写的很可能就是它。

### 新增：本机工作区的文件也能预览了

云沙箱那棵树点一下出预览，而本机那棵树点文件**什么都不发生**。现在两棵树用
同一个预览面板、同一套判据（太大不展开、二进制照实说、markdown 直接渲染）。

### 新增：点一个文件是预览，不是下载

从前点一下直接下载。那对「把产物拿走」是对的，对更常见的那件事 ——
「agent 说它写了 report.md，写了什么」—— 是绕远路。

现在下半栏出一个预览：markdown 直接渲染（与聊天里同一套样式），其余按纯文本
显示、可选中。下载挪到预览的标题栏上。超过 512 KiB 的不展开（连请求都不发），
二进制的照实说「下载下来用别的程序打开」。

### 新增：Web 上真的能选云端工作区了（0.1.11 只做了半条）

0.1.11 让一个会话可以把根收窄到卷里的命名子目录，但那一版**只做到 HTTP**：
界面上一个入口都没有，`container_workspace` 在整个 Flutter 客户端里零引用。
面板里那句注释还写着「云端那一支没有『更换工作区』可点 —— 那儿的根是服务端
定的」，而它从 0.1.11 起就不成立了。

现在文件面板右上那个换向箭头在 Web 上也出现（与桌面端**同一个图标、同一个
位置**，但打开的是另一个界面 —— 两边问的不是同一个问题）。点开能：

- 看到卷里已有的子目录（`GET /sandbox/files` 列一层，只取目录）并选一个
- 新建一个名字，形状在按下按钮之前就校验（与库里那条 CHECK 同一份规则）
- 选回「整个卷」，发的是**显式的 `null`**

面板标题栏现在直接显示当前那一层（`/workspace/client-a`），不点开也看得见。

界面里明说了一句要紧的话：**同名的会话共用同一个目录**。不说的话，用户会
以为选一个名字等于「给这个会话开一个私有目录」，然后在别的会话里找不到文件。

## [0.1.12] - 2026-08-18

**只修一件事，但那件事让上一版的每个在线用户都被踢下线了。**

### 这一版仍然不能干什么

与 [0.1.11](#0111---2026-08-17) 一样，没有变化。

### 修复：发完版所有在线的人被踢回登录页，红字说「登录已过期」

而他们的登录**完全有效** —— 续期那条路一直是好的（令牌在库里，重启不受影响）。

链条：发版重启 agentd → 还在跑的沙箱容器仍持着上一代的入站令牌 → 浏览器每隔
几秒轮询的 `GET /confirmations` 被 agentd 转进容器 → 容器回 401「缺少或无效的
凭据」→ **agentd 把这个 401 原样传给了浏览器** → 客户端把任何 401 都读成
「你的登录过期了」→ 续期（成功）→ 又一条 401 落地 → 它的熔断器判「刚续过还
401，续期不是答案」→ 把人踢回登录页。

修在源头：**反代路上的 401 绝不原样外传**。这条路上的 401 不可能是「调用方的
登录过期了」—— 调用方在进来之前就被 agentd 认证过，而转进去用的是我们自己铸的
那把钥匙。所以它只有一个意思：那一端不认我们这把钥匙了。现在换成 409，正文写明
「你的登录没有问题」。

同一个形状仓库里记过很多次：**一个状态码身兼两职**。

## [0.1.11] - 2026-08-17

「绑在某台机器上的会话只能在那台机器上继续」这条限制**开了一个口子**：那台机器
可以显式同意被接入，之后 Web 上的一轮就真的跑在它上面。加上「撞上正在跑的那一轮
不再丢话」，以及一个让 `cortex chat` 卡五分钟的老 bug。

### 这一版仍然不能干什么

- **不是 P2P。** 接入靠的是云端能连到你那台机器报出来的地址。机器在 NAT 后面
  就得自己做端口映射并用 `--attach-addr` 告诉它 —— 没有打洞、没有中转
- **中继看得到明文。** agentd 转发这一轮的全部内容。对「自己部署 agentd」的人
  不构成新暴露（消息本来就存在它的库里、LLM 也是它调的），但「agentd 由别人
  运营」那种部署还不该开这条路
- **排队只在一个 agent 进程内。** 它是内存里的一把闸门，不是跨进程的锁
- **Web 上没有「接进去」这个按钮。** 能力在服务端，界面还只是把那句 409 原样
  显示给你（那句话里写了该加哪个参数）

### 新增：Web 能直接挂到你那台开着的机器上（不必再走过去）

上一条把「是哪一台」说清楚了，但用户还是得**走到那台机器前面**。现在只要那台
机器同意，Web 上的这一轮就直接跑在它上面 —— 文件是你磁盘上的文件。

在那台机器上这样起 agent：

```
cortex-local --allow-remote-attach --bind 0.0.0.0:8090
```

**默认关着**，而且必须显式开：让云端够到你笔记本上一个能跑 shell 的进程，只能
是机器主人的一次决定，不能是「装上就有」。绑在 loopback 上时启动直接拒绝 ——
报一个 `127.0.0.1` 出去，云端打过去是打到它自己身上，于是名册里会出现一个
「可接入但连不通」的谎。机器在 NAT / 端口映射后面时用 `--attach-addr` 单独告诉
它对外那个地址（**通告地址与监听地址是两回事**，实测撞过：能监听的地址不一定
是别人够得到的那个）。

云端拿到的**不是**那个 agent 的入站凭据（它能换出站凭据、改 MCP 配置、把
agent 指向本机任意目录），而是另铸的一把只在接入面上有效的钥匙：
`/health`、`POST /chat`、`GET /runs/*`、`POST /confirmations`，其余一律 401。
这把钥匙**从不下发给任何客户端** —— 名册里只有一个布尔。

那句 409 因此分成了三档，各说一件不同的事：机器在线但没开放接入（附带怎么开）、
开了但连不通（多半是 NAT / 防火墙）、以及没有任何在线 agent 持有它。

顺带修掉一个 30 秒的窗口：在桌面端绑好一个会话之后立刻去 Web 上打开它，原先
最多有 30 秒会被告知「没有任何在线的 agent 持有它」—— 名册只认上一条心跳报过
的东西。现在绑定一变就立刻补一条心跳。

### 新增：一个会话正在跑时，你补的那句话不再被丢掉

同一个会话一次只跑一轮（两轮并跑会写同一个工作区、往同一段历史追加，谁覆盖谁
取决于时序）。撞上时原先回 409 —— 代价是**用户敲的那句话就此消失**，而他往往
正是因为看到「还在跑」才补的那一句。

现在它排队，前面跑完接着答，流上先来一条：

```
{"type":"queued","ahead":1}
```

界面上写成「这个会话前面还有 1 轮在跑，你这句排在后面」。**不说一声不行** ——
排队期间那条流除了 keepalive 什么都没有，屏幕上与卡死一模一样。

排队上限 4 条（不含正在跑的那一条），超了才拒。不设上限的话，等前面终于跑完会
**一口气连跑一串**十分钟前的指令，每一条都在改同一份文件。

顺序由 tokio 信号量的 FIFO 公平性保证，不是自己写的队列 —— 那正是最容易写出
「偶尔插队」的地方，而插队的症状是「模型答了上一个问题」。

### 修复：`cortex chat` 答完之后卡住五分钟

回答和 episode id 都打出来了，然后进程不退 —— 实测挂到那条轮次记录被清掉
（`KEEP_FINISHED`，5 分钟）。

原因是 SSE 在 `done` 之后**不结束**：广播的发送端活在「正在跑的轮次」那张表
里，而那张表为了让断线的客户端回来还挂得上，跑完之后刻意留 5 分钟。图形界面
看不出问题（它收到 `done` 就收尾），所以这个 bug 活了很久 —— 而两个「等流结束」
的调用方全中：`cortex chat`，以及 Flutter 那份 live 测试（30 秒超时，两条）。

现在 `done` / `error` 之后流立刻结束。判据与轮次登记簿里那个「终态」是同一处，
不再各写一份。

### 新增：那句「请到那台机器上打开它」现在说得出是哪一台

一个绑在某台机器上的会话在 Web 上会被顶回来，而那句话原来**没说是哪一台、
也没说它现在开着没有** —— 用户要么记得，要么挨个试。

本地 agent 现在每 30 秒向服务端报到一次（机器名 + 它手上有哪些会话的绑定），
于是同一个 409 变成：

    这个会话绑在 WILLOPTPC 上的一个目录里，它的文件只在那儿。
    那台机器现在**在线** —— 在它上面打开这个会话就能接着聊。

机器不在线时换成「没有任何在线的 agent 报告持有它的绑定」。
`GET /agents[?session=]` 能看到当前在线的机器。

**这一步只报不判**：名册不参与任何授权，哪怕 agent 谎报，后果也只是那句话说
错了。沙箱容器不报（它不是一台用户能过去的机器），而且**报不上去** ——
委托令牌的白名单里没有这条路。

### 新增：云端工作区可以有多个命名子目录

同一个项目下的会话原来共用一个 `/workspace`（会话之间没有文件隔离，只有项目
之间有）。现在一个会话可以把根收窄到卷里的一个命名子目录：

```
PATCH /sessions/{id}  {"container_workspace": "client-a"}
PATCH /sessions/{id}  {"container_workspace": null}          # 回到卷根
```

名字**可以被多个会话共用** —— 这不是「一个会话一个目录」：按会话分的话，
「昨天让你生成的那份报告呢」会得到一个空目录。不设名字时行为与以前一字不差。

### 修复：一条还没发过话的会话，`GET /sessions/{id}` 回 404

`session_digest` 从 episodes 聚合，所以刚建好的会话在它眼里不存在；而 `PATCH`
那条路一直支持这种会话（「先选工作区、再发第一句」）。两条路对同一个会话看法
不一致 —— 直接后果是容器里的 agent 在**第一轮**取不到工作区名。

判据改成「有没有生命周期事件」而不是「有没有消息」。真正不存在的会话仍然 404。

### 新增：改口令 —— 在这之前完全没有这条路

没有 API、没有 CLI、`CORTEX_ADMIN_*` 明确只建号不改密。第一个账号的口令
一旦定下就是永久的，想换只能进库改 argon2 哈希 —— 而「口令可能泄露了」
恰恰是最不该要求动手改数据库的时刻。

两条互补的路：

```
cortex passwd                        # 用户自己改：走网络，必须带旧口令
cortex-agentd --set-password <用户名>  # 机器主人恢复：走 shell，不需要旧口令
```

后者不验旧口令是**故意**的：跑得了它的人已经有这台机器的 shell，也就有
`.env` 里的库口令，随时能直接 UPDATE 那张表。要求旧口令挡不住任何人，
只是把「忘了口令怎么办」从一条命令变成一次手写哈希。

改完**作废这个人所有设备**的登录凭据。改口令的意图九成是「我怀疑它泄露
了」，只作废当前这条链等于把另外两台机器上仍然有效的凭据留在原地，
而用户以为自己刚把门锁换了。

`POST /auth/password` 不吃「认不出就当第一个账号」那个回落：拿着部署预共享
token 的人本来就能以 1 号身份读写，但**改掉 1 号的口令**多一件事 —— 把真正
的主人锁在外面。认不出身份就 403，并说清该先 `cortex login`。

### ⚠️ 拿掉了长期记忆

**Cortex 不再有长期记忆。** 系统提示词不再提它，工具目录里没有
`memory_search`，`/episodes` 不再转发给记忆服务，健康里不再报
`memory_reachable`，CLI 的 `cortex search` 与 `--no-memory` 一并去掉。

为什么：0.1.9 把记忆拆成独立产品（Cormex），0.1.10 把身份搬进 Cortex ——
于是用户手上那把 bearer 是 agentd 签的，而记忆服务不认识它。结果是那条路
**三段全断**：

- 每一轮转发都被回 **401**，日志里一行 WARN，用户毫无察觉
- 召回同一条路，同样 401 —— 「本轮不注入记忆」
- `memory_search` 工具打的是 agentd 的 `/memory/search`，而那条路由**不存在**
  （404），委托令牌的白名单里也明确没有它

而系统提示词写着「你是 Cortex，一个**有长期记忆**的通用 AI 助理…也可以
检索长期记忆」。于是模型照着它承诺：对一句 hello 答「我可以帮你读写工作区
里的文件、执行命令、**检索记忆**等」。生产上核过：Cormex 库里最新的
episode 停在 2026-08-13，而当天的对话只落进了 Cortex 自己的库。

**留一个必然失败的能力，比没有这个能力更糟** —— 它不报错，只是让 agent
替你撒谎。要接回来，先解决那条认证，再把提示词、工具、注入三处一起加回去。

对话历史**不受影响**：那是会话，不是记忆，一直在 Cortex 自己的库里。

顺带清掉的死重量：`cortex-agentd` 的 `Remote` 客户端与 `CORTEX_MEMORY_URL`、
`cortex-core` 的 `injection` 模块（唯一还有用户的 token 估算搬进了
`cortex_core::tokens`）、proto 的 `FactDto` / `MemorySearch*` / `ChatEvent::Memory`
/ `EpisodeAck.memories`。本地 agent 健康里的 `memory` 字段改叫 `server` ——
它一直指的是上游服务端，叫 `memory` 会让健康页写着「记忆：已连接」。

### 修复：线上除了聊天和沙箱，整个应用都在打记忆服务

边缘只把 `/api/chat` 与 `/api/sandbox` 转给 agentd，而 0.1.10 把身份、会话、
项目、同步、附件全搬了过来。症状是登录时回一句英文
「this deployment has no database attached」—— 那是记忆服务在答
`/api/auth/login`。

规则反过来写了：默认给 agentd，把记忆那一侧指名让出去。

### 新增：第一个账号可以在 compose 里定义

`CORTEX_ADMIN_USERNAME` / `CORTEX_ADMIN_PASSWORD` 的代码 0.1.10 就有，但两份
compose 都没把它们透进容器 —— 也就是说这条路对它面向的那类人（只有 compose、
没有 shell）从来没通过。只建号，不改密码。

---

## [0.1.10] - 2026-08-17

拆分（0.1.9）之后真正把云端那一半跑通的两天。**自托管的人有两条必读的
配置变化**，都写在最前面。

### ⚠️ 数据库的环境变量改名了：`POSTGRES_*` → `CORTEX_PG_*`

这个仓库现在**有自己的数据库**。0.1.9 把记忆搬走之后，会话、消息、附件、
项目、同步流水、沙箱快照这些还留在记忆服务的库里 —— 后果是停掉 Cormex
之后 agent **连上一句话都读不到**。它们已经搬进 Cortex 自己的
`cortex-store` 与 `migrations/`，判据只有一句：**这张表离开记忆能力还有没有
意义**。有，就是 Cortex 的。

两个栈各有一个库，于是变量名不能再叫 `POSTGRES_*`：

```
CORTEX_PG_USER=cortex
CORTEX_PG_PASSWORD=…        # 必填，没有默认口令
CORTEX_PG_DB=cortex
CORTEX_PG_PORT=5432
```

**漏改不会静默**：`CORTEX_PG_PASSWORD` 在 compose 里是必填（`:?`），
只留着旧的 `POSTGRES_PASSWORD` 时 `docker compose` 直接报错退出，
不会拿一个默认口令把库起起来。

### ⚠️ 第一个账号：以前**建不出来**

`--create-user` 与 `.env` 里的 `CORTEX_ADMIN_USERNAME` / `CORTEX_ADMIN_PASSWORD`
在文档里写了、在 `.env.example` 里列了，但**没有实现** —— 于是一套全新部署
起来之后没有任何账号，也没有任何办法建一个。现在两条路都真的通了：

```
cortex-agentd --create-user alice        # 口令从 stdin 读，不进 shell 历史
```

`.env` 里配了 `CORTEX_ADMIN_*` 的话，启动时会确保那个账号存在（已存在则
什么都不做，**不会**改口令）。建号跑在 docker 预检**之前**，所以一台还没配好
沙箱的机器也能先把账号建出来。

### 修复：沙箱里 agent 跑的每条命令都没有网 —— 从第一个沙箱提交起

`NetworkPolicy::Allowed` 这条分支**在生产里从来没有被调用过**：seccomp 过滤器
一律按「拒绝」装配，于是容器里跑的 agent 连 DNS 都发不出去，`pip install`、
`git clone`、任何 HTTP 请求全部失败。

藏得住是因为验证脚本用 `docker exec` 进去 curl —— 那是**另一个进程**，
不经过 agent 的 seccomp。`sandbox-verify.sh` 现在补上了 agent 自己那条路
（`cortex-local --self-check`），走的是与真实工具调用**同一个** `sandbox::prepare`。

出网仍然只有代理这一个口子（CONNECT allowlist），改的是「agent 的子进程
能不能拿到 socket」，不是把沙箱敞开。

### 修复：备份与恢复演练从拆分起就没成功过

`just backup` 打的是拆分前那个容器名，之后连着五层都是坏的：容器名 → 凭据
变量名 → **根本没开 WAL 归档** → alpine 上 `pg_verifybackup` 不在
`/usr/bin` → 演练目录的属主 uid 对不上。最后一层最阴：脚本里还留着查
`redactions`、导 `episodes.tsv` 的记忆时代断言，那两张表已经不在这一侧，
`set -e` 下**静默把演练杀掉**，退出码却是 0。

现在 dev 上端到端跑通并量过：RPO 2.5–27 秒（受 `archive_timeout=60` 约束），
RTO 35–46 秒。compose 里补齐了 `archive_mode` / `wal_level` / `wal_compression`。

### 新增：`cortex login` / `cortex logout` / `cortex whoami`

在这之前 CLI 只能用预共享 token，而那把 token 映射的**永远是第一个账号**。
单人部署没事；多用户下，桌面端登的是 2 号、CLI 进去读的是 **1 号的数据** ——
不报错，就是另一个人的会话。

```
cortex login                 # 存 refresh token 到 ~/.cortex/credentials.json（0600）
cortex whoami                # 我是谁、记忆在哪个 schema、凭据是哪来的
cortex logout                # 作废服务端那条链，并删掉本机凭据
```

`whoami` 会额外打一行**凭据来源**：预共享 token 与本机登录可能指向不同的人，
只报用户名的话，拿着预共享 token 的人会看到「admin」然后以为没问题。

### 新增：CLI 的 `--permission-mode`

`ask`（默认）与 `accept-edits`。**`bypass` 显式拒绝**并说明为什么：那个模式
只有在沙箱里才有意义，而 CLI 默认跑在你自己的机器上。

### 变更：云端那一半搬完了

身份、列会话与翻历史、项目与会话的写操作、实时同步、模型代理（借模型 /
自带 key / token 额度）、`/episodes`、导入、附件（`/blobs`）、沙箱快照账本 ——
全部由 `cortex-agentd` 直接打 Cortex 自己的库。边缘的分流规则里只剩记忆
那两条还转给 Cormex。云端对话（委托凭据 + 确认回路反代）因此真的通了。

### 新增：认证端点限流

服务端此前允许任何客户端每秒签 34 对令牌。现在每 60 秒窗口内：同一把
refresh token 最多 5 次、同一个用户名最多 10 次**失败**登录（全局兜底 100
次）、注册全局 5 次。

### 新增：附件真的进模型

图片走多模态块，小文本进上下文，其余落工作区让工具去读。随后一轮对抗审查
又补了四个洞：总死线 15 秒、中转体积上限、跨轮覆盖、HEIC 被当成能直接读的
图片。

### 修复：Web 端登录链断裂 + 幽灵 service worker

Web 端把「种子凭据」与「记住我」折叠成了同一个值，整条登录链因此断掉。
同期修的还有：半路 401 先续期而不是把人弹回登录框、迟到的 401 不再把现任
凭据拉下马（登录后被弹回的死循环）、续期加断路器（上一版把「掉线」修成了
续期风暴）、未登录阶段不再发认证请求（登录页上那些红字是自己吓自己）。

浏览器里还跑着三版之前的 service worker，连 `index.html` 都从缓存里给，
于是灭杀脚本永远到不了 —— 换成了自杀版。

### 修复：客户端一批小的

- 工具输出里的 ANSI 转义现在渲染成颜色，不再是满屏 `[32m`
- 会话与项目的同步帧到了、游标推进了，却**没有一个面板认它** ——
  另一台设备上改个标题、建或删一个项目，这台机器要等整体重拉才看得见
- 删掉「正在检索记忆…」：协议里根本**没有**检索事件，那句话是编的
- 删掉设置里的「使用 Mock 数据源」开关
- 绑工作区那一屏说清「只属于这台机器」

### 修复：本机 agent 的存活指针只增不减

`agent-*.live` 只写不收，主人进程死了指针还在。启动时收割一次。

---

## [0.1.9] - 2026-08-15

### ⚠️ 自托管的人先看这一条：记忆服务搬走了，现在要起两个

**记忆那一半（存储、抽取、四路召回、双时间轴回放、MCP 门面）已经成为一个
独立产品**，在 [Cormex](https://github.com/weironz/cormex)，独立发版、
独立版本线。这个仓库剩下的是 agent 那一支。

升级到 0.1.9 之前，`.env` 里必须多一个变量：

```
CORMEX_VERSION=v0.1.x     # 记忆服务的版本，与 CORTEX_VERSION 各走各的
```

**先部署 Cormex，再部署 Cortex。** 顺序反了的话 agent 编排器起来时连不上
记忆服务，一条对话都开不了 —— 它的 `/health` 会把 `memory_reachable: false`
写在脸上，部署验证也断言这一条。

漏配 `CORMEX_VERSION` 时 `docker compose` 会直接报错并告诉你该怎么办，
不会静默起一个残缺的栈。

**为什么拆**：记忆引擎是这个项目唯一自己写的东西，也是唯一别人没有的东西。
它绑在一个 agent 产品里，等于要求想用它的人连 agent 一起装。拆开之后
第三方 agent（Claude Code / goose）经 `/mcp` 接进来，走的是**与我们自己人
完全相同的 API** —— 那个 API 才不会烂。

### 修复：回放里被推翻的事实与现行事实长得一样

时间回放到过去时，「当时成立、此后已被推翻」的事实与仍然成立的事实在界面上
**完全一样**。服务端一直在发这一位（`invalidated`），记忆浏览器那条路
从来没读过它。

这恰好落在最不该出问题的地方 —— 回放唯一有信息量的场合就是看见「那时对、
现在不对」，没有这一位，时间轴只是把同一份列表换了个好看的画法。

### 变更：云端 agent 编排独立成进程

`cortex-agentd` 接手了「按需拉起沙箱容器、把请求反代进去、闲了回收」，
它是**唯一碰 docker 的进程**。记忆服务那边的 docker 权限（`docker.sock`
挂载与整套沙箱配置）一并撤掉了 —— 它早就不读那些变量，权限却还留着。

对用户没有可感知的行为变化。生产上多一个容器，边缘按路径分流
（`/api/chat` 与 `/api/sandbox` 进编排器，其余进记忆服务）。

### 变更：不再发 Linux / macOS 的 docker 镜像里那个记忆服务

镜像矩阵现在是 `cortex-agentd` / `cortex-web` / `cortex-sandbox` /
`cortex-egress-proxy`。记忆服务的镜像由 Cormex 自己发（叫 `cormex`）。

---

## [0.1.8] - 2026-08-13

### 新增：Web 端也能让 agent 真的动手了

在这之前，浏览器里的 Cortex 只会聊天 —— 它没有可以读写的地方。桌面端的
agent 动的是你自己的硬盘，而浏览器读不到硬盘，于是「帮我把这个 CSV 整理成
报表」在 Web 上只能得到一段说明文字。

现在每个用户在云端有一个自己的**容器工作区**（`/workspace`），跨会话保留：

- **发消息时打开「云沙箱」** —— agent 在那个容器里读文件、写文件、跑命令，
  改动照常有 diff 看
- **侧栏能看见那棵文件树**，点目录展开、点文件下载
- **能往里传文件**：手上那份 CSV 直接进工作区，agent 用 `read_file` 打得开
  （贴成附件是给模型看的，不是工作区里的文件）
- **能把整个工作区打包拿走**（`workspace.tar`）
- **项目级环境**：工作区里放一个 `.cortex/setup.sh`，第一次会跑一遍并把结果
  缓存成镜像；之后每次起容器都直接带着装好的依赖

容器空闲 30 分钟会停掉，**卷保留** —— 下次发消息秒级回来，文件一个字节不少。

几条要知道的边界：

- 沙箱**默认全拒出网**，只放行装依赖与拉代码要用的那些源（pypi / npm /
  github / crates.io / 阿里云与清华镜像）。撞到没放行的地址时，agent 会在
  同一轮里收到「为什么被拦、该换什么」，自己改用镜像源
- 每个沙箱 512 MB 内存、1.5 核。**不要指望它能跑 `cargo build`**
- `/workspace` 每 15 分钟自动快照一次；卷里还有一个 git 仓库，每轮结束
  自动 commit 一次，误改能回滚

### 变化：embedding 不再跑在 cortexd 进程里

官方 docker 镜像不再编进那份进程内推理引擎。**升级前请确认 `.env` 里的
`CORTEX_EMBED_BACKEND` 不是 `fast`** —— 是的话 cortexd 会拒绝启动并说清怎么办
（它不会静默降级成假数据）。

改用云 API 或者起一个独立的 embedding 容器，两条路都在
[docs/operations.md](docs/operations.md) 里。

这么改是因为它常驻 1 GiB：一台 2 核 3.5 G 的机器上，「API 服务器顺手把推理
也做了」这一个选择单独决定了还能跑几个沙箱容器。顺带镜像从 237 MB 降到
207 MB，出网代理那个从 125 MB 降到 **2.4 MB**。

### 新增：改文件之前，先看得见要改什么

以前 agent 动文件，界面上只有一行 `write_file  某某.txt`。现在能看见 diff，
绿加红减，三个地方：

- **确认框里** —— 在你按「允许」之前。之前这一步等于盲签：
  问的是「要不要让它写工作区外面的文件」，却答不出「写什么进去」
- **工具行**，改完之后点开还能看
- **右上角新的「本会话改动」** —— 这一整轮对话动过哪些文件，各改了几行。
  同一个文件改了两次就是两段，不合并

diff 只算 `write_file`。shell 跑完之后文件变成什么样，agent 自己也不知道。

太长的会截断（约 400 行），末尾写明还剩多少 —— 不会悄悄切掉。
它也**不进模型的上下文**：模型刚把新内容发过来，没必要再收一份。

### 修复：多设备同步会整个卡死（上一条带出来的）

`write_file` 之后，别的设备拉同步会拿到 500，而且**游标从此不动** ——
那台设备之后的所有变更都收不到。原因是 diff 那一列忘了加进下发同步的
那句查询。只影响未发布的开发版本。

### 修复：切后端 / 退登录点了没反应

两个动作都要先断开实时同步，而断开那一步会永久挂住（等一帧永远不会来的
WebSocket 关闭确认）。表现是点一下什么都不发生，也没有任何报错。

### 界面：左下角有账号栏了，两侧都能收起来

对齐 Codex / Claude 桌面端：

- **左下角账号栏**：头像 + 用户名，点开是「设置」与「退出登录」。
  设置从顶栏收进了这里 —— 它与「我是谁」是一组
- **两侧各一个伸缩按钮**：左边收起会话与工作区，右边收起记忆栏。
  收起状态跨重启记住
- 顶栏右侧只剩状态与显示开关（工作区、连接、主题、记忆栏）

没有用户名时那一行会说清是**哪一种**没有：Mock 数据源 / 离线 · 无记忆 /
未启用认证 / 已连接（老服务端答不出账号）。四种处境完全不同，
其中三种一切正常。

### 新增：桌面端会自己更新了

顶栏多了一个小图标。平时点开是「关于」（版本号、更新日志），
**有新版本时它右上角会多一个点，点一下就把整件事走完**：下载、校验、
安装、重启。不弹窗打断你。

安装包没有代码签名，所以下载完会拿发布页上那份 `SHA256SUMS` 对一遍；
对不上就删掉、不装，并说清楚。

只有 Windows —— 现在也只发 Windows 的桌面产物。

### 修复

- **改一个设置会把另一个抹掉**：设置是整表覆盖存的，而每处只写自己那一项。
  于是改一下权限档，重启之后 cortexd 地址就回到了默认值 ——
  正是 0.1.7 刚修完的「重启要重填地址」，从另一扇门回来了

## [0.1.7] - 2026-08-12

**这一版把「agent 在谁的机器上干活」这件事摆到了明面上**，并给会话加了
分组。

### agent 不再被工作区关死

此前工作区之外的路径是**硬拒**的：你说「把桌面上的 test.txt 删掉」，
它回一句「当前工作区里没有 test.txt」—— 连问一句的机会都没有。

现在越界路径会**弹确认**，把完整的绝对路径摆给你看。批准之后，
那个目录在本会话内不再问。符号链接那道关一个字没松：
`工作区/link -> C:\Windows` 会如实归类成越界，并显示它真正指向哪。

### 新增：输入框底部的三档权限开关

对齐 Claude Code：

| 档位 | 什么时候问 |
|---|---|
| 逐条确认（默认） | 写文件与执行命令都问 |
| 自动改文件 | 写文件不问，执行命令才问 |
| 完全放行 | 一律不问 |

选「完全放行」要单独确认一次，之后开关会变成警示色。这台机器上如果没有
内核围栏（Windows），那意味着 agent 的每条命令都是裸跑的 —— 界面会说明。

### 服务端不再假装能动你的文件

Web 端此前也能「绑定工作区」，绑的却是**服务器上**的目录 ——
既没有容器的隔离，也没有「这是你自己的机器」这句话。这一格已经删掉：
`PATCH /sessions` 带 workspace 现在会明确拒绝并说明该用桌面端或本机
运行的 `cortex-local`，而不是静默忽略。

### CLI 现在也在你自己的机器上跑工具

`cortex chat` 会自己把本地 agent 拉起来，工具动的是**你的**目录，
不再是服务器的。只装 CLI、不装桌面端也成立。

同一台机器上**只会有一个 agent**：桌面端开着时 CLI 直接复用它。
两边因此共用同一批会话与上下文 —— 在桌面端聊到一半，
`cortex chat --session <id>` 接着聊，它记得前面说过什么。

### 新增：项目（会话分组）

会话攒到一百多条，侧边栏就找不着东西了。现在可以建项目、把会话拖进去、
直接在某个项目里新建会话。

**删项目不会删会话**：里面的会话落回「未分组」，消息、附件、
已经记住的事实一条不少。确认框里写明了这一点，以及会有多少条会话
变成未分组。

### 修复

- **重启之后要重新登录、重填服务器地址**：凭据与地址两半都没接上持久化
- **每次冷启动都报一次「拉不到这个会话的消息 / 连不上 cortexd」**，
  而 cortexd 好端端活着 —— 唯一的出路是手点重试。记忆面板的
  「检索失败」是同一个原因
- **agent 不知道工作区根是哪个目录**：于是它把「桌面」当成了工作区，
  在里面找不到文件就说不存在
- **越界确认把「读取文件」标成了「写入」**：两边各写了一份风险文案，
  漂开了
- **CLI 拉起的 agent 可能连错服务器**：记忆会静默写到别处

### 运维侧的一处变化

第一个账号改由 `.env` 两行指定，取代此前「谁先注册谁是主人」。
已有部署不受影响。

## [0.1.6] - 2026-08-12

**这一版把账号体系从「零件都在」接成了一条线**，并新增一个不连服务器
也能干活的模式。

### 账号体系现在真的能用了

0.1.5 里它是**不通的** —— 注册成功、登录成功、拿到 token，然后每一个
请求 401：入站那道门只认预共享 token，从没认过登录换来的那把。
而三类测试都在各自那一侧，没有一条从登录走到用它。

同一版里还有两处只做了一半：请求路径仍是单租户（两个账号读写同一片库），
登录界面仍然只有一个 64 位十六进制输入框。

这一版全部接上了：

- 登录界面收**用户名 + 密码**，带 autofill；token 收进「旧方式」后面
- 请求路径按账号分流。读写数据的方法在类型上**不绑定租户就调不到**
- 第一个账号接管 `public`：一个先用了几个月才建号的人，
  建完号一登录不会发现记忆全空了

### 新增：离线模式（不连服务器也能干活）

服务器连不上时此前只能停在登录界面。现在可以「离线使用」：对话照常、
本地工具照常、写入排进本地队列，接上服务器后自动补回去 ——
**而界面上一直挂着一条不可关闭的提示：这些对话没有在记忆里**。

需要在设置里配一次本机模型（设置 → 模型 → 本机模型）。

### 新增：自带 API key

设置 → 模型 → 自己的 API key。用自己的 key 不占配额，
可以同时指定自建端点（公司网关 / one-api / LiteLLM / 自建 vLLM）。

### 新增：`CORTEX_LLM_BASE_URL`

把请求打到任何 OpenAI 兼容的端点。此前供应商定义连同地址是编译进
二进制的，想指向别处只能改代码重编。

### 修复

- **换账号会混本地队列**：outbox 与工作区绑定原先每设备一份，
  一台机器上换账号会把两个人的离线队列混在一起
- **新用户的记忆永远不会被回填**：向量回填与转录回扫只认第一个用户的库。
  没有任何症状 —— 只是「搜东西好像不太准」
- **删除请求可能被安静丢掉**：purge 轮转只查第一个用户的库，
  其他人的删除请求会被报成「没有需要抹除的东西」
- **关掉认证的部署会静默丢掉本地 agent**：表现是「装了桌面端，
  但它读不到我本机的文件」，而界面上没有任何说明
- **本地模型这条路根本走不通**：免鉴权的供应商（ollama）连启动都失败
- 界面上有四处 Markdown 星号直接显示给用户看

### 其它

- 每条消息都能一键复制（此前只有回答那半有）

## [0.1.5] - 2026-08-10

**这一版的重点是三个「不报错」的故障，以及导入终于有了界面。**

### 修复：会话历史从来没进过模型的上下文

同一个会话里问「我刚才说了什么」，它答不上来 —— 因为两条对话路径
（Web 与桌面）都只把**当前这一条**消息交给模型，历史一次都没加载过。

最坏的形态不是「不知道」：实测中用户这一轮说 8629，模型从记忆库捞出
上一次的 4173 **自信答错**。

现在两端都会带上最近若干轮，按 token 预算截断（保留最新的），
丢掉多少会写进日志。

### 修复：绑定工作区之后界面立刻显示「未绑定」

绑定其实成功了，但服务端回的会话对象里 `workspace` 是空的，客户端拿它
整个替换掉内存里的会话。**离线时更糟** —— 已经写进本地了，却报「绑定失败」。

现在绑定走一条完全不碰网络的本地端点，读的时候由本地 agent 填回真实值。

### 性能：向量召回快了两个数量级

主召回路上的 HNSW 索引**从项目第一天起就没被用上过** —— 结果一直正确，
只是每次召回都在做全表扫描加几万次 1024 维距离计算。5 万条事实上实测：
**289.7 ms → 0.5 ms**。库越大差距越明显。

### 新增：导入 ChatGPT / Claude 历史，现在有界面了

**设置 → 数据 → 导入 ChatGPT / Claude 历史。** 此前只有命令行，而命令行
甚至没进桌面安装包。

- 桌面端选本机文件，**97 MB 的字节一次都不过网络**（本地 agent 直接读）
- 网页端上传，服务端流式落盘（实测传 97 MB 时内存只涨 1 MB）
- **先把账摊开再给「开始」按钮**：一份真实的 Claude 导出是 1163 段对话、
  6047 次抽取调用、408 万 token。导入之后没有「撤销」
- 中断了重跑即可，已经导过的会被跳过，不重复计费

### 新增：账号体系（**服务端已就绪，客户端还没接上**）

`POST /auth/{login,refresh,logout,register}`、`cortexd --create-user`、
每用户一个数据库 schema、配额。登录一次可用 30 天。

**但这一版里它还用不了**：桌面端的登录界面仍然是粘 token 那个，
而请求路径还是单租户（两个用户会共用同一片库）。零件都在、都验过，
接线在下一版。现有的 token 登录方式完全不受影响。

### 其它

- 新增 `.gitattributes` 钉死 `.sql` 的行尾。行尾一变，migration 的校验和
  就变，而 cortexd 启动时跑 migration —— 症状是**服务起不来**

## [0.1.4] - 2026-08-09

**装了桌面端的人请更新。** 0.1.3 的实时同步是全程断开的 —— 界面上那个
「已重试 N 次」不是网络问题，是有一条路径根本没实现（见下面第一条）。

### 修复：装了桌面端之后，实时同步从来没连上过

界面上一直显示「实时同步已断开（已重试 N 次）」，N 只增不减。
**其余功能全是好的** —— 对话、历史、工具、记忆抽屉都走普通 HTTP，
所以它看起来像「同步偶尔抽风」，实际是**这条路径根本没通过**。

只影响装了桌面端的用户（本地 agent 在中间转发）。浏览器直连 cortexd 的
那条路一直是好的。

两处独立缺陷叠在一起：

- 本地 agent 的入站认证只认 `Authorization` 请求头。而 WebSocket 握手
  **只能**把凭据放进查询串（`?ticket=`）—— 跨平台的
  `WebSocketChannel.connect` 没有 headers 参数，浏览器更是明令不允许。
  于是每一次握手都是 401。
- 就算过了这一关也没用：转发走的是普通 HTTP 通道，而它按 RFC 9110
  把 `Connection` 与 `Upgrade` 当逐跳首部剥掉了 —— 对普通请求完全正确，
  可**升级握手恰恰就靠这两个头**。到达 cortexd 的是一个平凡 GET。

现在 `/ws` 走一条真正的 WebSocket 反代。票据原样交给远端验（本地没有
那本簿子），而且本地 agent **不给没验过的请求补自己的凭据**。

顺带：远端拒绝时客户端拿到的是**带原因的 502**，而不是一条连上就断的
连接。后者会被重连逻辑当成网络抖动，于是无限重试而错误一个字都传不出来 ——
截图里那 40 次就是这么来的。

### 把 ChatGPT / Claude 的历史导进来

`cortex import <conversations.json>`。这个产品此前需要攒好几周才有用 ——
记忆是它的全部卖点，而空记忆库和普通聊天框没有区别。

导出方式：ChatGPT 在「设置 → 数据控制 → 导出数据」，Claude 在
「设置 → 隐私 → 导出数据」（桌面端就能做）。两家都是邮件发下载链接。

- **默认只算账不动手。** 打印对话数、消息数、会触发多少次抽取、多少 token、
  时间跨度，然后停下。真跑要显式 `--confirm`。
- **中断了直接重跑。** episode id 由「原始时刻 + 稳定种子」派生，服务端按
  id 判重 —— 已经写进去的不会重复，也不会重复计费。没有进度文件，
  也就没有「进度文件和实际状态对不上」这条故障。
- **每段对话变成一个会话**，标题写「ChatGPT · 原标题」。记忆卡片上的
  「出处」点进去就是那段原对话。
- **原始时间戳保留。** 三年前的对话就是三年前 —— 否则「最近聊过什么」
  会永远返回那三年的历史。
- ChatGPT 的导出是一棵**树**（重新生成会开分支）。只导用户最后看到的那条
  路径：别的分支是他主动丢掉的东西，全导还会让抽取次数成倍增长。

先拿 `--max-conversations 5` 试水。

### 凭据不再进抽取管线

此前只有 extract prompt 里的一句「凭据与密钥的字面值不抽」—— 那是软约束，
模型看得见原文，而原文已经完整过了一遍网络。

现在私钥块、厂商前缀 key、`Authorization: Bearer`、JWT、`.env` 形式赋值
在**调用模型之前**就被挡掉，整轮不抽。原文照常保留在 episodes
（那是唯一真相来源，不能有洞）。

只做零歧义的那几类。邮箱、URL、泛型高熵串一概不管 —— 硬阻断它们会让
正常的技术讨论大面积抽不出记忆，而那种损失是静默的。

### 抽取有并发上限了

以前是没有上限的：一次对话一个后台任务，谁都不看别人。正常聊天下没问题
（打字速度就是天然的限流），但批量写入会瞬间起几千个任务、每个调一次
LLM，足以把一台小机器连同上面别的服务一起拖死。

默认 3 路，`CORTEX_EXTRACT_CONCURRENCY` 可调。

### ⚠️ 破坏性：cortexd 连不上数据库时**不再回落到假数据**

以前它会照常起来、返回 HTTP 200、`status: "ok"`、对话正常、检索有结果 ——
只有 `/health` 里 `database: "not_wired"` 一个字段能看出这一库记忆全是编的。
而生产 compose 里 cortexd **根本没配 healthcheck**。

生产上它是这样发作的：cortexd 因崩溃或 OOM **自动重启**时，compose 的
`depends_on: condition: service_healthy` 是不生效的（那个条件只在
`docker compose up` 那一刻管用）。于是 Postgres 恢复期间的一次自动重启，
就足以让它带着假记忆对外服务。

这与 0.1.2 那次「embeddings 找不到权重、静默变成假向量」是同一个失败形状。

现在连不上就退出（退出码 1），docker 会反复重启直到数据库真的回来 ——
**看到重启循环是预期行为，不是故障**。

- 想不带数据库跑要明说 `CORTEX_BACKEND=mock`，那时 `/health` 报 `status: "mock"` 而不是 `ok`。
- 打错的取值（`CORTEX_BACKEND=moc`）直接报错，不回落。
- 启动失败的报错会写清连的是哪个地址，**密码已抹掉**。

### 本地 agent 崩了会自己起来

以前它一挂，桌面端就成了空壳：所有对话都失败，只能重启整个应用。
现在会退避重启（1、2、4、8、16 秒），连续五次起不来就不再试并回落到直连远端 ——
有些失败重试一万次也不会好，把真正的错误埋在一墙相同的日志里更糟。
活过 30 秒就算「起来过」，重新计数：要抓的是**崩溃循环**，
而崩溃循环的特征是死得快，不是死得多。

顺带修了一个更要命的：子进程的输出以前直接丢弃，于是「本地 agent 没起来」
后面永远没有下文。现在保留最后几十行，**失败原因会一路带到错误信息里**。

### 两侧协议不兼容时，拒绝启动而不是将就着跑

桌面端里的 agent 与你自托管的 cortexd 从这一版起各自独立升级。
两边对不上时降级运行的表现是「某个功能悄悄不对」，比起不来难查得多。

协议版本与发布版本**分开**：0.1.3 到 0.1.4 之间绝大多数改动与线协议无关，
拿版本号比等于每发一个补丁就宣布一次不兼容，那样这个检查很快会被当成噪声关掉。

- 两个方向都查：对端太老，或者对端已经不再支持这么老的本端。后者只有对端知道，所以它也要报出自己的下限。
- **连不上不算不兼容** —— 离线是本地 agent 明确支持的形态，因为网络不好就拒绝启动，等于把一次断网变成「装了个用不了的东西」。
- 0.1.3 及更早的 cortexd 不报这两个字段，一律按兼容处理。升级桌面端不需要同时升级服务端。

### 记忆抽屉：这条是你说的，还是模型猜的

以前看不出来 —— 一条**用户亲口说的**事实和一条**模型从对话里推断的**
在界面上长得一模一样。而「为什么记得这个」正是这个产品的卖点。

来源通道从 0.1.0 起就写进库了，只是从来没下发给客户端。现在卡片上会标
「你说的 / 推断 / 派生 / 工具 / 外部」，颜色按可信度分。

同时修掉一个假数字：卡片上那个 **100%** 是服务端写死的常量，与这条记忆
无关。现在显示的是抽取器的真实打分；拿不到就不显示，而不是编一个。

### 标识

Cortex 有自己的图标了：两道嵌套的 C 形弧，皮层的沟回，也是名字的首字母。
外弧用主色，内弧用**记忆专用的那支强调色** —— 记忆在这个产品里是核心，
所以它在标识的核心位置。

标签页、任务栏、安装程序、PWA 图标全部换掉。16px 那一档用的是简化版
（外环 + 实心点）：母版的两道弧缩到 16px 会糊成一个灰点，而标签页
**只有**这个尺寸。源文件在 `assets/brand/`，`scripts/build-icons.sh`
可以从它重新生成全部尺寸。

## [0.1.3] - 2026-08-09

**只修部署，不改功能。** 0.1.2 的 `deploy/docker-compose.yml` 有两个会让
自托管节点**当场坏掉**的问题，在给生产节点升级前一刻发现。

如果你已经在跑 0.1.2 且**没有重新部署过**，那你没事 —— 问题只在
`docker compose up` 那一刻才发生。**别用 0.1.2 的 compose 部署，直接上 0.1.3。**

### ① 默认后端 `fast` 找不到模型 → 静默变成假记忆

`FASTEMBED_CACHE_DIR: /var/lib/cortex/models` 这一行在 0.1.2 的 compose 里
被删掉了。没有它，fastembed 回落到 `$HOME/.cache/…` —— 那个路径**不在**
挂进容器的 `models` 卷里，于是它会去 Hugging Face 现下 560 MB。

在够不到 HF 的机器上（国内节点常态：DNS 被污染、hf-mirror 也不通）后果是
最坏的一种：**cortexd 照常启动、`/health` 返回 `ok`**，只在日志里留一行
「回落到 mock 数据源」—— 记忆检索是假的，而没有任何红灯。

这个坑 0.1.0 首次上线时踩过一次。

### ② `CORTEX_EMBED_BATCH` 空串让 cortexd 拒绝启动

compose 里写的是 `CORTEX_EMBED_BATCH: ${CORTEX_EMBED_BATCH:-}`。`.env` 里
没这一项时，它把变量**设成空串**而不是不设，而 `env::var` 对空串返回
`Ok("")`。`FastEmbedderConfig::from_env` 于是拿 `""` 去 parse，失败，
**cortexd 拒绝启动** —— 而运维那侧根本没配过这个变量。

这是同一个坑第二次出现（第一次是 0.1.1 修的 `CORTEX_LLM_MODEL`）。
更难看的是同一个 crate 里 `embed_api::from_env` **有**这个处理、
`embed_local` 没有 —— 两份实现对同一个坑给出了两种答案，只有一份是对的。

现在 `embed_local` 与它对齐了，并且有一条 `embed_env_empty` 测试守着：
三个 `CORTEX_EMBED_*` 变量设成空串时必须与「不设」逐字段一致，
而真给了值仍要生效、给了非法值仍要响亮报错。
（做过反向验证：把修复回退，这条测试当场变红。）

## [0.1.2] - 2026-08-09

**这一版的主题只有一个：编码那一半开始工作。**

在此之前，桌面端装完只是一个填服务器地址的空壳 —— agent 读写的是
**服务器**上的目录，你本地的代码它看不见。现在循环与工具跑在你的机器上。

> ### ⚠️ 升级必读：服务端与桌面端要一起升
>
> 0.1.2 的桌面端依赖 cortexd 的 `POST /episodes` 与 `POST /llm/stream`，
> 而这两个端点是这一版才加的。**接到 0.1.1 或更早的 cortexd 上，
> 每一轮对话都会失败**（`/episodes` 返回 404，而 4xx 被判为不可重试，
> 不进离线队列 —— 重试多少次都一样）。
>
> 先升 cortexd，再升桌面端。

### 这一版的下载页上少了东西

| | 0.1.1 | 0.1.2 |
|---|---|---|
| Windows 桌面安装程序 | ✅ | ✅ **现在带本地 agent** |
| Windows 二进制包（cortexd + CLI） | ✅ | ✅ **现在带 cortex-local** |
| Linux / macOS 二进制包 | ✅ ×3 | ❌ 暂停 |
| Flutter Web 静态包 | ✅ | ❌ 暂停 |
| docker 镜像 | ✅ | ✅ |

停掉的两样都是**构建时间**的取舍，不是能力缺失：

- **Linux / macOS 二进制** → 用 docker 镜像，里面 cortexd 与 `cortex` CLI
  都在。只要 CLI 的话 `cargo install --git https://github.com/weironz/cortex cortex-cli`
- **Flutter Web 静态包** → `cortex-web` **镜像照常构建**，自托管部署里的
  Web 界面一点没少。停的只是「我自己搭静态服务器」那份 tar.gz，
  要它的人 `cd app && flutter build web --release`

恢复方式写在 `.github/workflows/release.yml` 里被停掉的那两个 job 的注释中。

### 新增 `cortex-local` —— agent 循环搬到本地，**编码那一半开始工作**

在此之前，工具跑在 cortexd 进程内，agent 读写的是**服务器**上的目录 ——
你本地的代码它看不见。桌面安装程序装完只剩一个填服务器地址的空壳。

现在多了一个二进制 `cortex-local`：循环、工具、沙箱在**你的机器上**跑，
记忆仍然只有远端 cortexd 一个权威副本。

**客户端一行不用改。** 它讲的是与 cortexd 完全相同的协议 ——
`/chat`、`/confirmations`、`/health` 自己答，其余路径原样反代给远端。
把 CLI 或桌面端的服务器地址从远端改成 `http://127.0.0.1:8090` 就完事。

```bash
CORTEX_REMOTE=https://你的-cortexd  CORTEX_TOKEN=…  cortex-local
```

**连不上 cortexd 时不是「打不开」，是「变成一个没有记忆的编码 agent」**：
循环、工具照常，界面上明说「记忆未连接」，这一轮排进本地 append-only 队列
（`%LOCALAPPDATA%\cortex\outbox.jsonl`），恢复连接后自动灌回。
**不存第二份记忆库** —— 队列只是待发件箱。

**工作区绑定改成设备本地的。** 你点的目录在这台机器上，而服务端的校验会
拿它去 canonicalize 服务器的文件系统。更根本的是：一个本地路径在别的设备上
要么是死路径，要么**指向别处而不报错**。所以绑定存在
`%LOCALAPPDATA%\cortex\workspaces.json`，不进跨设备同步的会话记录。

**Windows 上执行命令改成逐条确认。** 这台机器上没有 landlock / Seatbelt
的对等物，此前 `shell` 是硬拒的（「能改代码、不能跑测试」）。现在换了一种
保证：每跑一条命令弹一次确认，参数原文完整展示，你批准了才执行。

这只对**本地** agent 成立 —— 人就坐在屏幕前。cortexd 那侧继续硬拒：
远端批准的人可能在另一个城市，而命令跑在服务器的文件系统里，
他看得到命令、看不到它会碰到什么。

**它随桌面安装程序一起装、随界面自动起停**，你不用管它。界面退出时收掉；
界面崩溃或被任务管理器结束时，它自己盯着那个 pid，两秒内一并收工 ——
留下的孤儿会握着你的 token 并且仍能执行命令，而你以为自己已经退出了。

**还没做的**：agent 崩了不会自动拉起（得重启界面），也没有自动更新。

配套地 cortexd 加了两个端点：`POST /episodes`（写回 + 检索 + 归因，
**幂等**，供离线队列重放）与 `POST /llm/stream`（LLM 代理，
默认路径 —— API key 只在服务端一处，多设备不用每台配一遍）。

### 换 embedding 模型不再静默失忆

上一条（默认后端换成远端 API）把 `embedding_model` 从
`fastembed:gpahal/bge-m3-onnx-int8` 变成了 `api:bge-m3`。而四条向量查询
全部按这个字段过滤 —— 于是**升级上来的库里，全部存量事实会当场退出
向量召回**。

它糟糕的地方不是「丢了」，是**不响**：BM25 / 图遍历 / 近因三路照常工作，
界面上只表现为「语义召回好像变差了点」。没有任何报错，没有任何日志。

三件一起做：

**① 说出来。** 启动时普查一遍库里有哪些模型、各覆盖多少条，与当前模型
比对，不一致就 **WARN** 并写清「它们进不了向量召回」。`/health` 多一节：

```json
"embedding": { "model": "api:bge-m3", "covered": 1180, "stale": 4321 }
```

`stale` 就是进度条 —— 它长期不降说明回填被关了或一直在失败。

**② 补回来。** 新增 `fact_embeddings` / `entity_embeddings` 旁表，
后台回填器把旧事实在新模型下的向量补出来，**默认开着**。

- **纯追加**，不动 `facts.embedding`。CLAUDE.md 的 append-only 原样成立
  （`cortex-store` 里仍然零处 `UPDATE`），而且同一条事实能同时持有
  两个空间的向量 —— 这是第 ③ 件的前提
- **不进 `sync_log`**。`sync_payload.rs` 早就定过案：
  「换 embedding 模型时会全量变化，**白白撑爆增量同步**」。
  十万条事实的一次回填会推出十万条客户端一个字段都用不上的载荷
- **默认开、但先把数字报出来再动手**。关掉的代价没有任何症状，
  开着的代价（一次全库 embedding 调用，走云要花钱）是可见的。
  不想让它跑：`CORTEX_REEMBED=off`
- 每轮 64 条、间隔 5 分钟，**新的先补**（近期记忆被检索到的概率高得多）

**③ 回填期间也别丢。** 配上 `CORTEX_EMBED_LEGACY_ENDPOINT` / `_MODEL`
之后，查询会被同时编到新旧两个空间各捞一遍，按名次**交替**合并
（不是当前空间整体排前面 —— 那等于把全部历史压到 41 名之后，
而 RRF 的核是 `1/(k+rank)`）。

> ⚠️ **③ 救不了 0.1.1 → 0.1.2 这一次迁移**：那次的旧模型是进程内的
> fastembed，而它默认已经不编进二进制了。这一次只能靠 ② 把窗口熬过去。
> ③ 覆盖的是**将来的 api→api 换模型**（换云厂商 / 换模型名）。

### 顺带查出来一个一直存在的问题：主召回路上的向量索引是摆设

写执行计划守卫时 `EXPLAIN` 出来的，**与本次改动无关**：

```
Sort (Sort Key: f.embedding <=> …)
  ->  Nested Loop Left Join
        ->  Seq Scan on facts f
        ->  Sort  ->  Seq Scan on fact_events
```

`active_facts` 是带 LEFT JOIN 的视图，planner 必须先把 join 做完才能按
距离排序 —— `idx_facts_vec` **从来没被用上过**。每次向量检索是 `facts`
全表扫 + `fact_events` 全表扫再排序。小库上完全看不出来。

这一版**没修**（两条候选修法都会改变召回语义，是独立的一件事），
但记下来了。

### 向量化多了两条路：调远端 API，或自建一个独立容器

原来只有一条路 —— `fastembed`（内部 ONNX Runtime）在 `cortex-memory` 里是
**硬依赖、没有 feature gate**。代价是三条：

1. **Intel Mac 根本编不出来** —— `ort-sys: no prebuilt binaries available`。
   这是**构建期**失败，连 `CORTEX_EMBED_BACKEND=hash` 都救不了。
   0.1.0 发版时为此把 Intel Mac 从产物矩阵里删掉了
2. **每台机器都要能拿到那 560 MB 权重**。第一次上生产时那台节点
   Hugging Face 完全不可达（DNS 被污染、`hf-mirror.com` 也不通），
   cortexd 于是**照常启动、`/health` 返回 `ok`**，只在日志里留一行
   「回落到 mock 数据源」—— 记忆检索是假的，而没有任何红灯
3. 冷启动 12 秒、常驻 1.0 GiB

现在 `fastembed` 是可选 feature `local-embed`，而 `CORTEX_EMBED_BACKEND`
有三个取值：`fast`（进程内 ONNX）、`api`（任何 OpenAI 兼容的
`/v1/embeddings`）、`hash`（离线用，不是语义空间）。
compose 里也多了一个自建的 `embeddings` 服务（HuggingFace TEI + bge-m3）——
自建与用云是**同一个协议**，互换只改 `CORTEX_EMBED_ENDPOINT`。

**但默认仍然是 `fast`，自建那个服务默认不启动**（挂在 profile
`selfhost-embed` 上）。中途一度把默认改成 `api` + 自建，又改了回来：
那个服务 bge-m3 fp32 权重 2.2 GB、TEI 载入后常驻 2.5–3 GB，
**2 核 4 GB 的机器跑不动**，而那正是本项目的目标机型 ——
让它默认起来等于让参考部署在 `docker compose up` 那一刻就 OOM。
官方 docker 镜像因此带上了 `local-embed`：把它做成可选 feature 的初衷是
让 Intel Mac 能编出**裸二进制**，而裸二进制这一版已经不发了（见下），
那个理由在 docker 这条路上并不成立。

**要注意的三件事：**

- **模型必须是 1024 维**（schema 写死 `VECTOR(1024)`）。
  bge-m3、阿里百炼 `text-embedding-v3`、`jina-embeddings-v3` 都可以；
  OpenAI 的 `text-embedding-3-small` 是 1536、`large` 是 3072，
  直接接会被 cortexd 在**第一次响应**就拒掉 —— 而不是留给 Postgres 的
  约束去报一条看不懂的写失败
- **从源码编要显式加 feature**：`cargo build -p cortexd
  --features cortex-memory/local-embed`。不加而又设了 `=fast`，
  启动时会报「需要 feature `local-embed`」，不会静默降级
- **换后端等于换向量空间**，哪怕「还是那个 bge-m3、只是从进程内换到远端」——
  量化与池化实现并不逐位一致。所以库里写的 `embedding_model` 变成了
  `api:<模型名>`，与旧的 `fastembed:...` 可分辨；
  评测基线也按模型标定，回归门会核对它

开发机上那个服务放在 `embed` profile 里，**`just up` 默认不起**：
`docker compose --profile embed up -d`。

### 裸二进制收敛到只发 Windows

0.1.1 的下载页上有四个 `cortex-v0.1.1-<平台>.tar.gz` / `.zip`
（Linux x64 / Linux arm64 / macOS arm64 / Windows x64）。0.1.2 只剩
Windows 那一个 —— 但它**多了一个二进制**：

```
cortexd.exe       守护进程（记忆权威）
cortex.exe        命令行客户端
cortex-local.exe  本地 agent  ← 新增
```

理由是构建时间：四个平台各自本机编译一遍，占掉整条发版流水线的绝大部分。
前期迭代快，另外三个的成本不值。

**Linux / macOS 的能力没有减少** —— docker 镜像里 cortexd 与 CLI 都在。
减少的是「不想碰 docker，下个二进制就能跑」这条路。替代路径见
[docs/install.md](docs/install.md) 的 B 节；只要 CLI 的话
`cargo install --git … cortex-cli` 最短。

> 中途有一版把四个全砍了。加回 Windows 是因为**这一版的重点就在 Windows**：
> 本地 agent 让 CLI 用户第一次真的能改自己机器上的文件，
> 而那正需要一个能下下来就跑的 `cortex-local.exe`。
>
> 恢复另外三个平台：`release.yml` 的 `build` job 里那个 matrix 加回条目即可，
> 注释写了会撞上什么（Intel Mac 的 `ort-sys` 障碍在
> fastembed 改成可选 feature 之后**理论上没了，但没验证过**）。

---

## [0.1.1] - 2026-08-08

这一版的由来：**0.1.0 发出去的那个
`cortex-v0.1.0-x86_64-pc-windows-msvc.zip` 里没有桌面 GUI**，
只有 cortexd 与 CLI —— 而它的名字看上去就像「Windows 版 Cortex」。
0.1.1 把桌面安装程序补上，同时带上首次真实上线时挖出来的三个修复。

> **桌面端是瘦客户端，它需要一台 cortexd。**
> 安装程序里没有服务端 —— 装完第一屏就是让你填 cortexd 的地址与凭据。
> 记忆的权威只有远端 cortexd 一处，桌面端与 Web、CLI 走完全相同的
> HTTP/SSE 协议，不走任何私有捷径（见
> [docs/architecture.md](docs/architecture.md)）。
> 还没有服务端的话，先看 [docs/deploy.md](docs/deploy.md)。

### 修复：CI 从 08-07 07:18 起一直是红的，没人看

十几次 push 全红，期间照样发了 v0.1.0、照样合了六七个提交。
**一个长期红着的 CI 等于没有 CI** —— 这条比下面两个具体原因严重。

两个原因叠在一起，前一个把后一个挡住了：

- `scripts/lib.sh` 的 `load_env()` 收一个可选参数而没有任何调用点传过它
  （shellcheck SC2120）。改成在调用点显式写出路径，而不是加一行
  `# shellcheck disable` —— 一个只走过默认路径的可选参数，
  等于一段从没被执行过、但长得像能用的代码
- `docker compose config` 那道门只喂了两个必填变量，而 compose 里
  用 `${VAR:?}` 声明必填的一共六个。**这道门在本地是过的**：
  docker compose 会自动读仓库根的 `.env`，于是本地跑它等于没跑。
  顺带把 `deploy/docker-compose.yml`（线上真正跑的那份）也纳入检查

### 修复：环境变量设成空串时不再顶掉默认值

自托管部署时会撞到的一个静默故障。`docker compose` 里写
`CORTEX_LLM_MODEL: ${CORTEX_LLM_MODEL:-}`，在 `.env` 没有这一项时
它把变量**设成空串**（而不是不设）；而 `env::var` 对空串返回的是
`Ok("")` 而不是 `Err`，于是代码里的默认模型名被一个空串顶掉。

它的糟糕之处在于**看起来一切正常**：cortexd 正常启动、`/health`
返回 `ok`、日志里没有任何异常，直到发出第一条真实对话才收到供应商的
`The supported API model names are ... but you passed .`（注意句号前那个空位）。
本地开发一路撞不到，因为 `.env` 的写法是「不写这一行」。

现在 `CORTEXD_BIND` / `S3_BUCKET` / `S3_REGION` / `CORTEX_LLM_MODEL` /
`CORTEX_LLM_CHEAP_MODEL` / `CORTEX_DEVICE_ID` 等配置项一律把空串（含纯空白）
当作「没设」；`DATABASE_URL` 为空串时也直接报「缺少环境变量」，
而不是把空串交给 sqlx 再炸出一句看不懂的话。

### 修复：发布说明取错 CHANGELOG 段落

`release.yml` 里取「这一版的段落」用的是动态正则
`"^## +\\[?" ver "\\]?"` —— awk 把 `\[` 当成普通 `[`，整段退化成一个
**字符组** `[?0.1.0]?`，于是**任何 `## ` 标题都算命中**，而且每个标题
都命中第一条规则并 `next`，终止那条永远轮不到。

0.1.0 发版时看不出来（那时 `## [0.1.0]` 恰好是第一个二级标题）。
加了「未发布」一节之后，它的表现是**把整个 CHANGELOG 当成这一版的
发布说明发出去**，而 `[ -s notes.md ]` 那道检查照样绿。
改成 `index()` 逐字比对。

### Windows 桌面端有安装程序了

`cortex-desktop-vX.Y.Z-x86_64-pc-windows-msvc-setup.exe` —— 双击装、
开始菜单与桌面有快捷方式、控制面板里能卸干净。装的是**客户端 GUI**，
不是服务端。

- **装在 `%LOCALAPPDATA%\Programs\Cortex`，全程不要管理员权限。**
  它不注册服务、不写 HKLM、不装驱动，没有任何需要提权的动作 ——
  而一个既没签名又要求提权的安装程序，会让用户连过两道吓人的弹窗
- **MSVC 运行库随包带**。Flutter 的 windows 产物不带 CRT，而 Windows 11
  不预装 VC++ 2015-2022 可再发行组件。缺了它的症状是：安装成功、
  快捷方式也在、**双击之后什么都不发生**（进程立刻退出，没有任何提示）。
  三个 DLL 放在 exe 旁边，用户不需要先去装 redist
- 打包脚本在打包前**真的启动一次那个 GUI**，若干秒内自己退出就当场失败 ——
  上面那个缺 DLL 的场景恰好是立刻退出

**这个安装程序没有代码签名，Windows SmartScreen 会拦一次。**
下一节把这件事写清楚了。

### 修订：桌面安装包的发布决定

0.1.0 说的是「不发桌面安装包」（见下面那一节，理由留着没删）。
现在改成 **Windows 发、macOS 与 Linux 不发**。

改的不是标准，是因为原来那一条把三个平台混为一谈了 ——
**它们的拦截强度根本不是一回事**：

| 平台 | 系统怎么拦 | 结论 |
|---|---|---|
| **Windows** | SmartScreen 是**警告**。点「更多信息 → 仍要运行」就装得上 | **发**，但安装说明里必须写清楚会看到什么 |
| **macOS** | Gatekeeper 是**硬拒绝**。没公证的 .dmg 打不开，没有「仍要运行」 | 不发（要 Apple Developer 账号 99 USD/年 + 公证） |
| **Linux** | `.deb` / `.rpm` 本身不难，但要么自建 apt 源要么让人手动 `dpkg -i` | 不发（Linux 用户本来就更接受 `flutter build`） |

原来的理由「发一个装不上的安装包比不发更糟」现在只对 macOS 成立：
不公证的 macOS 包是**真的打不开**，而不签名的 Windows 包是**能装、
但会被警告一次**。这两件事不该由同一条结论覆盖。

所以代价是：**必须把用户会看到的那一屏写在前面**，而不是让人自己撞上去。
安装说明（[docs/install.md](docs/install.md)）与安装包里的 `README.txt`
都写了三件事：会看到什么、为什么会看到、怎么继续。以及**怎么自己核实** ——
发布页的 `SHA256SUMS` 里有这个文件的哈希，`certutil -hashfile <文件> SHA256`
对一遍。这一步比「点仍要运行」重要：SmartScreen 拦的是「没见过的发布者」，
它本来就不该被无脑跳过，而校验和是用户手上唯一能真正确认来源的东西。

---

## [0.1.0] —— 2026-08-07

**第一个可安装的版本。** 此前从未发布过任何东西。

一句话：一个自托管的、单用户的、记忆是内核的 AI agent。跟它聊过的东西
会被抽成事实存下来，下一次对话自动召回；换设备接上还是完整的你。

### 这一版能干什么

**记忆（这是这个项目存在的理由）**

- **对话永久归档**。全链路 append-only，没有 `UPDATE`、没有 `DELETE`。
  唯一的例外是显式触发、二次确认、留墓碑的 `redact` / `purge`
- **自动抽事实**。每轮对话之后异步抽取实体与事实，确定性矛盾消解：
  改主意时旧事实被标记为 superseded 而不是抹掉
- **双时间轴**。区分「事情何时变」与「我何时知道」，所以
  「三个月前我以为用什么」和「现在用什么」是两个能分别答对的问题
- **四路召回 + RRF 融合**：中文 BM25（jieba 分词 + tsvector）、
  bge-m3 语义向量、实体图遍历、时间近因，融合后按 token 预算注入
- **注入不打穿前缀缓存**。稳定的核心画像块进可缓存前缀，回合检索块贴
  最新消息一侧，历史轮次的记忆块保留不剥离。朴素实现会让成本涨 5–10 倍，
  而且只有几周后从账单上才看得出来
- **可审计**。每条记忆可追溯到具体那一轮对话，能点开出处

**多模态**

- 发图片进对话，图片按 SHA-256 内容寻址存进对象存储，异步转录成可检索
  的文字。过几天问「那张架构图里有哪些组件」能召回
- 转录质量受 vision 模型限制。8B 级模型有明显 OCR 抖动，认真用要指到更大的模型

**三端**

- `cortex` 终端瘦客户端、Flutter 桌面（Windows / macOS / Linux）、Flutter Web
- 三端走**完全相同**的 HTTP / SSE / WS 协议，没有任何一端有私有捷径
- 实时流式回复；WebSocket 推送跨设备同步

**Agent 与工具**

- 会话可绑定一个工作区目录，agent 能在里面读写文件
- 路径围栏 + OS 级沙箱：Linux 用 landlock + seccomp，macOS 用 Seatbelt。
  逃逸测试在容器里实跑过，**先做对照组**证明不加沙箱时那些命令确实能成功
- 高风险工具（写文件、执行命令）走服务端问、客户端答的确认回路，
  超时按**拒绝**处理

**认证**

- Bearer token。服务端只存 SHA-256 摘要，明文只在生成那一次出现
- **没配凭据 cortexd 拒绝启动**。这是刻意的：一个不认证的 cortexd 会把
  整个记忆库交给任何能连上这个端口的人，而这件事没有任何症状

**运维**

- `pg_basebackup` 全量 + WAL 归档（PITR），`pg_verifybackup` 逐文件校验
- blob 增量镜像到第二存储（`rclone copy`，**绝不带 `--delete`**）
- 出本机那份自动加密（`rclone crypt`），本机那份保持明文可验证
- **恢复演练脚本**。59 MiB 库实测 RPO 46.9 s / RTO 43.6 s；走加密第二存储
  取回再恢复 RPO 50.9 s / RTO 67.7 s
- 告警三层：失败告警 / 本机看门狗 / 外部心跳，盲区互补
- `purge` 之后轮转备份，把归档 WAL 与旧全量里的残留一并抹掉

**质量门**

- 116 题检索评测集，逐题型回归门（只比总分会放过「某一类塌了」）
- 当前基线（bge-m3 int8 真实向量）：
  **R@1 0.640 · R@5 0.877 · R@10 0.931 · MRR 0.771**

### 这一版**不能**干什么

这一节比上一节重要。不写清楚，第一批用户会拿一个错误的预期去撞墙。

| 做不到 | 具体是什么 |
|---|---|
| **离线** | 客户端是瘦客户端，agent 循环与工具执行都在服务端。**断网就用不了**，没有本地缓存、没有离线写队列 |
| **移动端** | 没有 iOS / Android 构建。Flutter 代码在，但没做过、没测过、不发布 |
| **多用户 / 多租户** | 认证是**一个** token 对**一个**部署。没有用户概念、没有权限分级、没有按用户隔离记忆 |
| **Windows 上执行命令** | 没有对等的 OS 沙箱，`shell` 工具在 Windows 上**默认拒绝执行**。Windows 上只能读写文件。这不是 bug，是刻意的安全侧选择 |
| **语音 / 视频** | 没有 ASR。音频视频能存下来，但转不成文字，因此**不可检索** |
| **桌面安装包** | 不发 `.msi` / `.dmg` / `.deb`。见下面「为什么不发安装包」。**这条在 0.1.0 之后被修订过**：Windows 已经开始发安装程序，见「未发布」那一节 |
| **高可用** | 单机自托管。恢复期间服务是停的，RTO 就是停机时间 |
| **arm64 docker 镜像** | 生产镜像只有 `linux/amd64`。arm64 二进制有，镜像没有 |
| **Intel Mac（x86_64 macOS）** | **完全不支持，而且不是「没出二进制」——是编不出来。** ONNX Runtime（`ort-sys`）不再为 Intel macOS 提供预编译产物，而 `fastembed` 在 `cortex-memory` 里是硬依赖、没有 feature gate，所以 `ort-sys` 在**构建期**就失败，`CORTEX_EMBED_BACKEND=hash` 也救不了。Apple 已停止支持 x86_64，GitHub 的最后一个 Intel 镜像也将于 2027-08 撤掉。要支持它得先把 fastembed 做成可选 feature —— 那是独立的一块工作，且 hash 后端不是语义空间、检索质量明显下降。**Apple Silicon 的 Mac 不受影响，`aarch64-apple-darwin` 正常发布。** |
| **schema 兼容承诺** | migration 只前滚，没有 downgrade。0.x 期间 schema 会不兼容地变 |

**检索上诚实的几个数**（同一份基线里最弱的几项，不藏着）：

- `跨域检索` R@5 只有 **0.667** —— 六个题型里最差的一类
- `应召不到` 误召率 **0.50**：一半的「本来就不该召回」的问题仍会召回干扰项。
  判定为检索单独解决不了，需要生成侧参与
- `专名精确` 的误召率同样是 **0.50**
- R@1 与 MRR 的小数第三位是噪声（末级定序用的 ULID 每次 ingest 现生成）。
  拿它们判定小于 0.01 的改动等于在读刻度线的宽度

### 为什么是 0.1.0 而不是 1.0.0

因为上面那张表。一个断网就用不了、没有移动端、只服务一个人、
schema 还会不兼容地变的东西，叫 1.0 是在说谎。

也不是 0.0.1（此前 `Cargo.toml` 里的占位值）：闭环是真的通了，
备份能恢复且演练过，检索有回归门守着，装得上、跑得起来、能用。
0.1.0 是「第一个能给别人用的版本」的诚实刻度。

### 为什么不发桌面安装包

> **这一节记录的是 0.1.0 当时的判断，留着不删。**
> 其中 Windows 那一条已在「未发布」一节里被推翻 —— 因为 SmartScreen 是
> 警告而 Gatekeeper 是硬拒绝，把两者写成同一条结论是当时的错。
> macOS 与 Linux 的理由不变。

签名与公证是真实成本，不是懒：

- **macOS**：`.dmg` 要 Apple Developer 账号（99 USD/年）+ notarization。
  不公证的话用户拿到的是 Gatekeeper 直接拒绝打开的东西
- **Windows**：不签名的 `.msi` 会被 SmartScreen 拦，OV 证书按年付费
- **Linux**：`.deb` / `.rpm` 本身不难，但要么自建 apt 源要么让人手动 `dpkg -i`

发一个装不上、或者一打开就被系统拦住的安装包，比不发更糟 —— 它会让
第一次接触这个项目的人以为是软件坏了。**0.1.0 只发二进制、Web 静态产物
与 docker 镜像**，这三样都不需要代码签名就能用。要桌面 GUI 就自己
`flutter build windows|macos|linux`，仓库里那套是能构建的。

### 已知问题

- **`cortex-web` 镜像里的 API 地址是编译期钉死的**，换域名要重新构建镜像
  而不是改 `.env`。根因有两层：dart2js 会把 `String.fromEnvironment` 常量
  折叠进 `main.dart.js`；而且客户端的 `_normalise` 只接受
  `http://` / `https://` 开头的**绝对**地址，相对路径 `/api` 会被拼成
  `http:///api` 这种没有 host 的废 URI。同源部署也必须写完整域名
- **`status:"ok"` 与 docker 的 HEALTHCHECK 都不代表 cortexd 接上了后端。**
  已实测：少一个 `DEEPSEEK_API_KEY`，整个真实后端初始化就会失败并**回落到
  mock 数据源继续启动** —— `/health` 报 `status:"ok"`、容器转 `healthy`，
  而 `database` 是 `"not_wired"`，它在服务假数据。
  对象存储凭据不对时同理，`blob_backend` 变成 `"local_fs"`：上传的媒体
  只存在于容器里，不进对象存储、不随备份走、签不出 presigned URL。
  **上线后必须逐字段检查 `database` 与 `blob_backend`**，不能只看 `status`
- 生产 cortexd 默认 `CorsLayer::permissive()`。bearer token 不会被浏览器
  自动附带，所以危害有限，但公网部署应当收紧到具体来源
- 第一次启动要下 ~590 MB 的 embedding 模型（BGE-M3 int8）。容器里落在
  `cortex_models` 卷，只下一次。设 `CORTEX_EMBED_BACKEND=hash` 可以完全
  不用模型跑起来，但那不是语义空间，检索质量会明显下降

### 取件

供应商适配层取自 [goose](https://github.com/block/goose)，沙箱选型取自
[codex](https://github.com/openai/codex)，两者均为 Apache-2.0。
逐处出处见 [NOTICE](NOTICE) 与对应源文件的头部注释。

---

[0.1.0]: https://github.com/weironz/cortex/releases/tag/v0.1.0
