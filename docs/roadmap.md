# 开发计划

**接下来做什么。** 已完成的见 [roadmap-done.md](roadmap-done.md)；
为什么这么设计见 [architecture.md](architecture.md)。

> **实现纪律：能搬就不写。**
> goose 与 codex 均为 Apache-2.0，供应商适配、apply-patch、沙箱这类繁琐但已被
> 工业验证的部分一律取件，保留版权头并在 `NOTICE` 注明。
> 自己写的只有别人没有的东西。**那件事以前是「记忆引擎」，现在不是了** ——
> 它 2026-08-15 独立成了 [Cormex](https://github.com/weironz/cormex)。
> 在这个仓库里值得花时间的是 **agent 侧**别人没有的：沙箱与权限、
> 桌面与云同一份 agent 循环、反向隧道。与 CLAUDE.md 开篇一致。

---


## 还剩什么 —— 一眼看完

> **这一节 2026-08-28 提到了最前面。** 它叫「一眼看完」，而它此前排在
> 180 行分析之后 —— 一份「接下来做什么」的文档，最该被一眼看到的东西
> 不该要先翻过三节才见得着。

I / J 两节 15 条做完之后，这个仓库**没有成建制的缺口了**。2026-08-28
对着 codex 与 DeepSeek Harness 又核了一遍（详见「对着两家开源 harness
核了一遍」那一节），剩下这些：

| | 事项 | 为什么还没做 |
|---|---|---|
| 1 | 删掉直拨路径 | 判据要攒一周。量具 2026-08-30 从**容器日志**搬进了库（`cortex_auth.attach_route_stats`）—— 此前挂在日志上，而 agentd 没有任何卷，重建一次容器观察窗口就清零（实测撞到：为改邮件配置重建两次，攒了一天的窗口变回 20 分钟）。现在判据是一句 SQL：`tunnel.count > 0`（证明真有人接入）且 `direct.last_seen` 在一周前。**时钟随下一次发版开始走** |
| 2 | E 阶段 1：`Workspace` 成一等对象 | 阶段 2、3、4 都在不动它的前提下做出来了，所以**它挡不住任何东西**了。剩的价值是内部整洁，而动它要改 schema + 迁移、买到的是零个能力 —— 排在任何有用户可见收益的事情后面 |
| 3 | **compose 项目重名** | 这一侧的前置早就满足，没钉 `name:` 的那七个卷**全是 Cormex 的** —— 改名要在那个仓库做 |
| 4 | 子 agent 的事件面 | controller+worker 设计稿里最后一笔（工具行级，delta 不进重放缓冲）。没人在等 |

**Windows 沙箱 2026-08-28 收线**，从这张表出去了：两档后端、`cargo`（含
拉新依赖）/ `git` / `curl` 全通、逃逸测试进 CI；遗留清单（卷根 ACE、WFP
真封网、AppContainer 桌面隔离）**全部拍板不做**，理由与推翻条件在
[windows-sandbox.md](windows-sandbox.md) 第六节。CHANGELOG 的「这一版
仍然不能」已跟着改过。

剩下的四件没有一件挡用户：第 1 件在等生产日志攒够一周，第 2、3 件
一件是内部整洁、一件不在这个仓库，第 4 件没人在等。

2026-08-25 那两轮收掉的，全部搬去了
[roadmap-done.md](roadmap-done.md)：桌面端不带 node / python（文档 + doctor +
连接失败那句话）、「派出去」的入口、整账号会话导出、操作系统级通知、
**异地备份从零到生产上跑起来**；以及归档进「不做」的
E ②（中继端到端加密）、H2（工具注册表）、H3（全插件化）。

---

## 最近做完的几大块

- **Windows 沙箱收线**（2026-08-28）：两档后端、`cargo`（含拉新依赖）/
  `git` / `curl` 全通、逃逸测试进 CI、遗留清单拍板不做。实现、实测数据
  与一路的坑全在 [windows-sandbox.md](windows-sandbox.md)。
- **「登录很快过期」诊断闭环**（2026-08-28）：中途被踢的埋点此前几轮已齐，
  这轮读了真机日志（124 条「被踢」全是测试污染残留，真实为零）并补上
  最后的盲区 —— 启动续会话的结果落盘、成功也记。下一版装出去后，
  日志一眼定案是哪条路径踢的人。
- **web_fetch + 四家搜索服务商**（2026-08-27）：Tavily / 博查 / Exa / Brave，
  抓正文只有前两家有，界面与服务端两处都挡着。详情在 roadmap-done。
- **controller + worker 架构定案 + 反向隧道**（2026-08-27）：结论是
  「账本中心化、执行与生命周期不中心化」，落地的只有阶段 1（隧道 + 开关）。
  详情在 roadmap-done。

**逐条经过、踩过的坑、以及那份「不做清单」的触发判据，搬去了
[roadmap-done.md](roadmap-done.md)。** 这份文档只回答「接下来做什么」——
两块加起来 180 行的完成记录留在这里，读的人每次都要先翻过它们才看得到
待办，而那正是这份文档最没用的读法。

## 现在在哪

M0–M8、N1–N3、R1–R11 全部完成，**并且在 2026-08 拆成了两个产品**。

```
你的机器                          云端节点
─────────                        ─────────────────────────────
桌面端 / CLI ──► cortex-local     浏览器 ──► traefik ─┬─ /api/chat·/api/sandbox
                     │                                │        └──► cortex-agentd
                     │                                │                  │ docker
                     │                                │       沙箱容器：cortex-local
                     │                                │                  │
                     └──────── HTTP ──────────────────┴─── 其余 /api ────┤
                                                                          ▼
                                             ┌──────────────────────────────────┐
第三方 agent ─────────────── /mcp ──────────►│  Cormex 记忆服务（另一个仓库）    │
（Claude Code / goose）                       │  抽取 · 四路召回 · 双时间轴回放  │
                                             └──────────────────────────────────┘
```

**这个仓库里已经没有 `cortexd`。** 记忆那一半（存储、抽取、召回、MCP 门面）
整个搬去了 [weironz/cormex](https://github.com/weironz/cormex)，独立发版。
本仓库剩下的是 agent 那一支：`cortex-local`（循环与工具）、
`cortex-agentd`（容器编排）、`cortex-egress-proxy`、CLI 与 Flutter 客户端。
完整图见 [architecture.md](architecture.md)。

> 下面那些检索与恢复的数字属于记忆服务，现在由 Cormex 那边维护。
> 留在这里是因为它们仍然是这套系统的实际表现。

检索基线（116 题真实向量 bge-m3）：
**R@1 0.640 · R@5 0.877 · R@10 0.931 · MRR 0.771**
（逐题型最弱：`跨域检索` R@5 0.667；`应召不到` 误召 0.500）

恢复演练实测（59 MiB 库）：**RPO 46.9 s（稳态上界 = `archive_timeout` 60 s）· RTO 43.6–83 s**；
走加密第二存储取回再恢复：**RPO 50.9 s · RTO 67.7 s**（异地取回 30 s 不计入）。

工具执行有 OS 沙箱（Linux landlock+seccomp / macOS Seatbelt），
逃逸测试在容器里实跑；**没有沙箱能力的平台默认拒绝执行**。

**我们现在也是 MCP 客户端了**（2026-08-15）。此前只当供给方（`/mcp` 把记忆
交出去）不当消费方，这是不对称的。`cortex-mcp` 接 stdio 与 HTTP 两种传输，
配置格式与 Claude Code 通用（桌面端读用户目录的 `mcp.json`，容器读工作区根
的 `.mcp.json`），设置页里能加/删/开关/重连、能搜官方注册表一键安装。
**外来工具默认 `Risk::Execute`**，逐次过闸门 —— 见 H 那一节末尾。

---

### 2026-08-23 新增：智能体 / 技能 / 连接器 / 电脑操作

四样一起做的，因为它们回答的是同一个问题的四个面：**模型手上有什么、
它以什么身份说话**。

| | 是什么 | 在哪儿 | 关键约束 |
|---|---|---|---|
| **智能体** | 一份存下来的人设 | 左栏第三个入口，卡片墙 | 换人设 = 开新对话（中途换会打穿 prompt caching，且历史前后不一致） |
| **技能** | 一份写好的做法 | 设置 → 技能 | 目录每轮进提示词，正文由 `load_skill` 按需取回 |
| **连接器** | MCP 的说人话入口 | 设置 → 连接器 | 不新造机制；四条一键精选 + 原来那套全保留 |
| **电脑操作** | 截屏 + 键鼠 | 设置 → 连接器最下面 | 默认关；这是产品里权限最大的一组 |

**四样共用一条纪律**（CLAUDE.md 约束 2 的四次具体应用）：能力与它在提示词 /
工具目录里的广告**同生共死，判据只算一处**。

* 技能：`Caps.has_skills` 同时决定「印不印目录」与「摆不摆 `load_skill`」；
* 电脑操作：`Caps.can_use_computer` 同时决定「印不印坐标系说明」与「摆不摆
  那五个工具」；
* 智能体：空人设在**控制器**里就滤掉，不留给某一条传输去挡；
* 连接器：精选里每个包名都对 npm registry 查过，已接过的不给再点（`PUT` 是
  覆盖，会盖掉用户改过的 trust 与填过的 key）。

各自的完整理由在 [design.md](design.md) 第十二～十五节与
[architecture.md](architecture.md)。三处踩过的真坑：

* `/agents` 已经是本机 agent 的心跳注册，智能体只能叫 `assistants`
  （撞上时 axum 直接 panic）；
* `GET /skills/body` 得进委托令牌白名单 —— 漏了的话云端会话里 `load_skill`
  连吃 403，而桌面端一切正常；
* 电脑操作拿一个不支持 vision 的模型开着，代价是**屏幕白拍一次并已经发给
  供应商**（截图在工具执行时才拍）。

---

### 这两天新增的（2026-08-19 ~ 20）：模型这一整块

| | |
|---|---|
| 模型来源统一成一份列表 | 取代「跟随部署 / 自带 key / 本机模型」三个概念。顺带修掉一个线上 bug：自带 key 是 alibaba 而模型跟部署（deepseek）走，三档里两档是坏的 |
| 拉供应商的型号列表 + 能力筛选 | 240 个型号里挑，按「能跑 agent / 看得懂图 / 能生图 / 有思考」筛 |
| 生图 | `generate_image` + `POST /llm/image`，走已有 blob 通道 |
| **默认模型（主 / 快速 / 绘画）** | 补上中间那一层。`ModelTier::Cheap` 根本不经过用户，此前自带 key 的人**没有任何办法**让会话命名、内容抽取走自己的账户 |
| 撰写框下的模型 chip | 逐轮切换挪到最顺手的位置 |

### 更早（2026-08-16 ~ 17）

| | |
|---|---|
| 备份与恢复演练 | 从拆分起就没成功过，五层问题修完，本机真机跑通 |
| `cortex login` / `logout` | CLI 不再只能是 1 号用户 |
| `cortex-agentd --create-user` + `.env` 建号 | 此前默认配置下**建不出第一个账号** |
| `cortex-local --self-check` | 验「agent 真跑命令时能不能出网」，走 agent 那条路 |
| `--permission-mode` | CLI 收 ask / accept-edits（bypass 显式拒绝） |
| 终端 ANSI 渲染 | 富文本四件套最后一件 |
| 会话/项目同步 | 服务端一直在发，客户端那一行没认 |

---

## 对着两家开源 harness 核了一遍（2026-08-28）

两份参考都在本机：`D:/codes/_ref/codex`（OpenAI Codex，Apache-2.0）与
`D:/codes/_ref/dsh`（DeepSeek Harness，MIT，2026-08-13 开源）。
按仓库第一纪律「能搬就不写」，这一节记的是**它们有而我们没有**的东西，
以及每条值不值得搬。

⚠️ **逐条对着 Cortex 的代码核过，不是照着它们的 README 抄清单。**

**想当然列进来、一查发现早就有的六条**（写下来，免得下一个人再查一遍）：

| 以为缺的 | 实际 |
|---|---|
| 工具输出截断 | `MAX_TOOL_OUTPUT_CHARS = 8000`，且注释写清了「进 history 就永远在那儿」 |
| 后台任务 | `background_run` / `background_output` / `background_kill`，输出上限 64K |
| 逐次 shell 超时 | `run_shell` 收模型给的 `timeout_ms` |
| 循环上限 | `DEFAULT_MAX_ROUNDS = 8` |
| **读项目指引**（`AGENTS.md` / `CLAUDE.md`） | `workspace::project_guide` 已经读，两个候选名按优先级、24K 截断、且写清了「为什么自动读而 `.mcp.json` 坚持先问一次」——爆炸半径不同 |
| 长会话的上下文压缩 | `recap.rs`（掉出窗口的轮次用廉价模型摘要）。至于「工具结果堆积」那条：`fit_history` 只吃文本轮次，工具结果**不跨轮累积**，所以不是缺口 |

「照着 roadmap 排期而不核代码」这仓库已经犯过三次，这次没让它变成第四次。

### 已经补上的（今晚做完）

| 借鉴自 | 补的真空 | 提交 |
|---|---|---|
| `dsh` guard/timeout-policy | **MCP 工具调用没有超时** —— 一台卡住的第三方 server 能把整轮永久挂死，而它在设置页里显示「连接正常」 | `ee9de4e` |
| `dsh` guard/repeat-tool-reminder | **认不出「原地打转」** —— 同一个 grep 重跑八轮然后说「我没能完成」 | `54a959b` |

**顺着同一条线索又揪出四件**，都不是借鉴来的，是核代码时撞到的：

| | |
|---|---|
| `913ad15` | **MCP 连接挡着 agent 启动** —— 每台 60 秒且串行，而桌面端只等 20 秒，超时就把「本地 agent 启动失败」给用户看，**而 agent 好好的** |
| `7d039cd` | `CORTEX_MCP_CONFIG` —— 上一条的修复本来没有确定性测试（红不红取决于机器上装了哪些 MCP server）。配置路径可注入之后，`022ceae` 用一台「接了连接就沉默」的假 server 把它钉死了 |
| `34c86bb` | **技能那段说明是以「代码块」的形式讲给模型的** —— 9 个前导缩进进了字符串，而 markdown 里那是缩进代码块。那段唯一要说的话（「先 `load_skill` 取正文」）因此是以示例代码的形式讲的 |
| `4103ad5` | 给工具描述加了「没被 fmt 压扁」的常驻闸 —— `b413dc6` 那次手工扫的 131 处，从此守得住 |

⚠️ 这一晚两次撞上**「闸只盖了一半目标」**（见形状表新增的那一条）：
工具链闸漏了 Flutter 三个引用点里的一个（把发布流水线弄红了），
工具描述闸第一版只查了十个构造器里的一个。

### 值得做，按性价比排

| | 是什么 | 为什么值 | 代价 |
|---|---|---|---|
| **1. Windows 沙箱** | codex `windows-sandbox-rs`：独立本地用户 + 受限令牌 + ACL + WFP 网络过滤 + 独立桌面，一次性管理员配置 | **阶段 1 已于 2026-08-28 落地**：文件与网络两条边界都成立了 | 比预计小得多：**AppContainer 不要管理员**，后端约 500 行。剩下的是桌面隔离，以及那个要人拍板的 `git`（见上面「Windows 沙箱」一节）|
| ~~**2. MCP 配置路径可注入**~~ | `CORTEX_MCP_CONFIG` | **做完了** —— 环境变量在 `cortex_mcp::config_path`，确定性的故障注入在 `local_agent_test.dart` 的「一台装死的 MCP server 不挡启动」（一个接了连接就一个字节都不回的监听器扮演那台 server）| 已归档 |
| **3. plan 模式** | `dsh` plan：先探索与设计，再动手 | Cortex 有 `todo_write`（列清单）但没有「这一轮只许读不许写」的档位。它与已有的权限档是同一个位置，接得上 | 中 |
| **4. LSP 代码导航** | `dsh` lsp：跳定义、找引用 | 今天靠 `grep`/`glob` 猜符号。大仓库上这是准确率差别，不是省事差别 | 中大（要管 language server 的生命周期，形状与 MCP 那套一样） |

⚠️ **这张表里原先还有第 5 条「git worktree 隔离」，写着「子 agent 并行改
同一个仓库时会互相踩」—— 一查就没了：子 agent 是 `readonly_specs()`，
只筛 `Risk::Safe`，改不了文件也跑不了命令。没有并行写，也就没有要隔离的
东西。**

这一条是我在写这一节时自己加进来的，一小时后自己查掉的 —— 与上面那六条
「以为缺其实早就有」是同一个形状。**列清单的时候手比脑快，这件事对
自己也成立。**


### Windows 沙箱：选型已经**实测定案**（2026-08-28）

> 📄 **实现、最终方案与一路踩的坑，完整版在
> [windows-sandbox.md](windows-sandbox.md)。** 下面这一节是当时的决策过程与
> 遗留清单，两份都留着 —— 一份记「怎么走到这儿」，一份记「现在是什么样」。

这是唯一一条**能力上**的硬差距。这一轮把「用什么机制」这个风险最大的
未知量变成了**测出来的数**，证据留在
`crates/cortex-agent/tests/win_sandbox_probe.rs`（默认 `#[ignore]`，
`cargo test -p cortex-agent --test win_sandbox_probe -- --ignored` 可复跑）。

#### 三条路，两条被实测排除

| 机制 | 要管理员吗 | PowerShell / .NET | |
|---|---|---|---|
| 受限令牌 `CreateRestrictedToken` | 不要 | **起不来**（5.1 `0xFFFF0000`、7 `0xE0434352`，都是 CLR 启动失败） | ✗ |
| 独立本地用户（**codex 的做法**） | **要**（一次） | 正常 | 可行但贵 |
| **AppContainer** | **不要** | **正常** | ✓ **定这条** |

受限令牌那条**边界是成立的**（用户目录挡得住、`cmd` 与 `git` 都能跑），
死在 **.NET** 上：CLR 在受限令牌下起不来。

> ⚠️ 上一版这里写着「Windows 的 `shell` 工具走 PowerShell」，**那是错的** ——
> `shell_argv` 在 Windows 上回的是 `cmd.exe /C`。结论不变但理由不同：挡住
> .NET 挡掉的是**用户命令里**的 `dotnet`、`.ps1` 与一大票 .NET 写的 CLI。

⚠️ **这同时解释了 codex 为什么宁可要那一次管理员配置** —— 独立用户拿的是
正常令牌，.NET 没问题。所以上一版这里写的「照 codex 的做法分三阶段」
是**照抄了一个我们不必付的代价**：他们的约束不是我们的约束。

AppContainer 实测：`cmd` / PowerShell 5.1 / pwsh 7 / git **全部正常**，
用户目录里的文件**挡得住**，工作区授权之后读得到 —— 而且不需要管理员。

#### 阶段 1 **已经做完**（2026-08-28）

后端在 `crates/cortex-agent/src/sandbox/windows.rs`，逃逸测试在
`crates/cortex-agent/tests/sandbox_escape_windows.rs`。a–e 五件都落地了：
SID 派生、工作区授权、`SECURITY_CAPABILITIES` + helper 进程
（`cortex-local --win-sandbox-exec`）、`capability()` 如实报 AppContainer、
四条逃逸测试（工作区内读写照常、读不到用户目录、写不进用户目录、
能力必须是 AppContainer）。

**接线之后才暴露的三件事**，每一件都比接线本身值钱：

| | 症状 | 根因 | 现在怎么做 |
|---|---|---|---|
| 1 | 每条命令 **+23.4 秒** | `SandboxPolicy` 把 `~/.cargo/registry`、`~/.cache`、`~/.gradle/caches` 无条件放进可写根（Landlock 下不要钱），而 Windows 上每一个都是一次**持久的递归 ACL 重写** | 只授**工作区子树**；授过就不再授；授权挪到父进程 —— helper 跑在命令超时**里面**，放那儿的话大仓库第一条命令会假报「命令超时」 |
| 2 | 逃逸测试**全绿但什么都没验** | helper 是 `target/debug/cortex-local.exe`，而 `cargo test -p cortex-agent` **不重编它** —— 改完 `windows.rs` 验的是旧二进制 | 测试目标改成 `harness = false`，**测试二进制自己就是 helper**，从机制上消掉这一类 |
| 3 | 逃逸测试**仍然**全绿但什么都没验 | `command_line` 把引号按 `CommandLineToArgvW` 的规则转义，而 **cmd.exe 不认那套** —— 命令根本没跑起来 | `cmd /C` 后面那段**原样**传，一个引号都不加 |

⚠️ **第 3 条是一个先存在的产品 bug，与沙箱无关**：`std::process::Command`
对 `cmd.exe /C` 用同一套转义，所以 **Windows 上任何带引号路径的命令一直是
坏的**（`type "C:/Program Files/x/a.txt"` 那种写法 → 「文件名、目录名或卷标
语法不正确」），沙箱内外都一样，hook 也一样。它是靠「把沙箱整个关掉，逃逸
测试照样绿」才浮出来的 —— 这条线索的价值超过沙箱本身。

#### 实测的代价

| | 耗时 |
|---|---|
| 能力探测（进程内只做一次） | 61 ms |
| 空工作区，每条命令 | **60 ms** |
| 793 059 个文件的仓库，**首次**授权 | **153.8 s** |
| 同一个仓库，之后每条命令 | **201 ms** |

首次那一笔是 AppContainer 机制自带的（访问检查要求对象 DACL 上有容器 SID
的 ACE，而带继承标志写 DACL 会铺满整棵已有子树），**跨重启只付一次**。
`target/` 占了那 793k 里的 762k —— 沙箱建出来的文件是**创建时继承**的，
不要钱；贵的只有「绑定一个已经建过的大仓库」这一种情形。

#### 网络：测出来是一个 capability 的事

| | `curl -m 8 https://example.com` | DNS |
|---|---|---|
| 不给 capability | 退出码 6 | 失败 |
| 给 `internetClient`（`S-1-15-3-1`） | **200** | 通 |

所以 `NetworkPolicy` 在这一层是个干净的二元开关，已经接上了（三条测试守着：
默认不出网、放开网络不放开文件、放开网络也连不上本机）。

⚠️ **但两边的 `Allowed` 不是同一个东西**：Linux 侧走 `cortex-egress-proxy`
的 CONNECT 白名单，这边一放就是整个互联网。

#### git：能用，前提是工作区不在主目录底下

2026-08-28 挖到底了。git 每条子命令开头都要 `getcwd`，Windows 上那是
`GetLongPathNameW` —— 它要**在每一级上级目录里把这一级的名字查出来**。

所以沙箱自动给工作区的每一级上级授「可列举」，**零提权**（前提是那些目录归
当前用户）。卷根不需要 —— 实测 `D:\` 一条 ACE 都没有时 git 全通。

| 工作区在哪 | 沙箱里的 git |
|---|---|
| 第一级目录名**不像 8.3 短名**（`D:\cortex-workspace\...`） | ✅ 零提权可用 |
| 第一级目录名短（`D:\codes\...`、`C:\work\...`） | ⚠️ 要卷根一条 ACE（一次管理员） |
| `C:\Users\<你>\...`（含 `%TEMP%`、桌面、文档） | ❌ 还要 `C:\Users` 上一条，同样要管理员 |
| 上级目录归别人（属主是解析不出的 SID） | 看那一级授不授得上 —— 老接口只要对象本身的 WRITE_DAC，比 `icacls` 宽 |

> **为什么与名字长度有关**：`GetLongPathNameW` 只在一个组件**可能是 8.3
> 短名**时才去父目录里查它。`cortex-probe`（12 字符）不可能是，于是那一级
> 不需要列举权；`codes`（5 字符）可能是，于是必须查。这条解释了之前所有
> 看起来矛盾的观察。而真实项目目录的名字大多是短的，所以实践中卷根那条
> ACE 多半还是要的（它需要管理员：实测老接口对 `C:\`、`D:\` 都回
> `ERROR_ACCESS_DENIED`，那是 WRITE_DAC，不是传播的锅）。

**链断了就不再往下授。** 中间少一级，`GetLongPathNameW` 整条失败，下面授得
再多也没用 —— 而每一级列举权都让容器多看见一层文件名。这条最初写错了
（「授不上就跳过、继续往下」），后果是在一台 `C:\Users` 授不上的机器上把
**主目录**的文件名露给了容器，而 git 依然一步都走不了。**白付代价。**
现在有测试守着（`链断了就不再往下授`）。

`~/.gitconfig` 那一半：它在主目录里、沙箱读不到，而 git 把「读不了全局配置」
当致命错。沙箱**合成**一份只带 `user.name` / `user.email` 的配置给它，
credential helper、`url.*.insteadOf` 里的令牌、签名密钥一个都不进沙箱。

#### 授权为什么不再慢：换了个不做继承传播的接口

`SetNamedSecurityInfoW` 会做「自动继承传播」——把可继承的 ACE 重新算一遍
铺到每一个子项上。工作区那次**必须**走它（子项本来就要拿到权限），但祖先
目录那些 ACE 是**不继承**的，子项什么都不会变，那一趟遍历纯属白跑。

实测（797 381 个文件的目录，加一条不继承的 ACE）：

| | 耗时 |
|---|---|
| `SetNamedSecurityInfoW` | **198.9 s** |
| `SetFileSecurityW`（被它取代的老接口） | **0.38 ms** |

差五个数量级。所以非继承那条路改走老接口。

⚠️ **反过来不成立**：老接口**不能**用来撤销可继承的 ACE —— 根上撤了、
子项还带着继承来的那条，留下一个「根没权限而子树有权限」的不一致状态，
而且没有任何症状。实测踩过。

#### 唯一需要管理员的东西：`dir` / `vol`

它们要卷信息，那要卷根上一条 ACE，而卷根归 SYSTEM：

```
icacls C:\ /grant "*<容器 SID>:(RX)"
```

不带 `/T`、不带 `(OI)(CI)`。**不加也能用** —— git 不需要它，`list_dir` 工具、
cmd 的 `for` 通配、.NET 的 `Directory.GetFiles` 都不需要。要不要做，是产品
决定，不是技术阻塞。

#### 第二后端：受限令牌（2026-08-28，env 门控、默认关）

AppContainer 那一档**当时**被判「跑不了完整构建」—— 2026-08-29 用三件套修好了（见 [windows-sandbox.md](windows-sandbox.md) 5.8）。`cargo check` / `clippy` / rustc 一直都通，
但 `cargo build` 的**链接步**挂：MSVC `link.exe` 回 `0xC0000142`，换 `rust-lld`
则从主目录树 mmap 自身镜像被拒（FULL 授权也没用，是 LowBox 对对象命名空间的
隔离，ACL 够不着）。

于是搬了 codex 的受限令牌机制做第二后端
（`crates/cortex-agent/src/sandbox/windows_restricted.rs`，约 550 行）。
**两个后端是两种取舍，不是强弱关系**：

| | AppContainer（默认） | 受限令牌（`CORTEX_WIN_BACKEND=restricted`） |
|---|---|---|
| 读 | 默认拒绝，强 | **不设界** —— 读身份就是用户本人 |
| 写 | 只有工作区 | 只有工作区（`WRITE_RESTRICTED` + 只授工作区的 logon SID） |
| `cargo build` 出 .exe | ✗ | **✓**（实测，零管理员） |
| `git` / `dir` / `vol` | 受 8.3 与卷根限制 | 全部正常 |
| 网络 | capability 控制，默认断 | 未隔离 |
| 桌面 | 未隔离 | 私有桌面（挡屏幕抓取 / 输入注入） |

**读边界为什么修不了**：`WRITE_RESTRICTED` 只让 restricting SID 参与**写**
检查，读走普通令牌 —— 而沙箱进程的读身份就是用户自己，**没有能区分
「沙箱 vs 用户」的 SID**。试过给秘密目录下 `Everyone` 的 DENY，结果把用户
自己也挡了（实测把 `~/.ssh` 弄成用户自己读不了，已回滚）。所以这一档
**不挡读**，且这一点写进了模块文档、`shell` 工具描述与能力上报，有测试守着。

**用途因此是明确的**：「防误写 / 防篡改地跑我信得过的工具链」，不是
「关住不受信代码防它偷秘密」。要防偷读用 AppContainer —— 但它跑不了构建。
两者不可兼得，这是 Windows 原生沙箱的结构现实（微软自己遇到同类问题的
答案也是换原语，见上一节）。

⚠️ **网络这一层实测做不成，且现状是两头不讨好**（2026-08-28）：

| | 实测 |
|---|---|
| 明文 `http://` | **200，出得去** —— 所以不构成边界 |
| `https://`（curl / git / PowerShell） | 当时**全挂**在 `SEC_E_NO_CREDENTIALS` —— 所以也坏了可用性。git 与 cargo 已修，见下 |
| 零管理员封网 | **做不到**：`FwpmEngineOpen0` 只读，写事务回 `ERROR_ACCESS_DENIED`；Job object 网络限速设不了（err 87） |

也就是说：**这一档既拦不住外发，又拉不了依赖。**（后半句已经不成立了 ——
2026-08-28 当天把 git 与 cargo 两条路都修通了，见下。前半句仍然成立。）
codex 的 WFP 封锁同样只在它的提权档，非提权档只是设 `HTTP_PROXY` 环境变量劝退。

两件事要分开做，都还没做：

1. ~~**修 HTTPS**~~ —— **开发路径已经全通**（2026-08-28）。
   根因定位到「受限令牌下证书存储只能只读打开」（注册表键上明写
   `NT AUTHORITY\RESTRICTED: ReadKey`，是系统既定语义，不是漏授）。
   解法是绕开 schannel 而不是要写权限：

   | | 现状 |
   |---|---|
   | `git`（clone/fetch/push） | ✅ 通 —— 注入 `http.sslBackend=openssl` |
   | `cargo` 更新索引 | ✅ 通 —— 走 git 协议 |
   | `cargo` 下载 `.crate` | ✅ 通（2026-08-28）—— 绕开 TLS：本机明文回环镜像 + 沙箱专用 `CARGO_HOME`，见 [windows-sandbox.md](windows-sandbox.md) 的 4.4 |
   | `curl https://` | ✅ 通（2026-08-28）—— 沙箱备一份 LibreSSL 版 curl（下载 + SHA-256 钉死），见 [windows-sandbox.md](windows-sandbox.md) 的 4.5 |
   | PowerShell `Invoke-WebRequest` / .NET | ❌ 挂 —— SSPI，没有可换的后端；模型被告知用 curl / git 代替 |

   规律：**能换掉 TLS 后端、或能被换成明文的都通了**。cargo 属于后者：
   它的 libcurl 静态链且只编了 Schannel，没有开关 —— 但它的**下载源**可以
   用 `source.crates-io.replace-with` 换成本机的明文回环镜像，TLS 由宿主
   这一侧做。`curl` 没有这一层，所以留着。

   ⚠️ 同一轮还发现一堵**比 TLS 更靠前的墙**：用户真实的 `~/.cargo` 沙箱写不进，
   默认 `CARGO_HOME` 下 cargo 第一步就「拒绝访问 (os error 5)」，哪怕依赖全
   在缓存里。此前之所以没被发现，是因为那条测试自己设了私有 `CARGO_HOME`
   —— 测试替被测对象把环境铺好了。现在这一档把 `CARGO_HOME` 指到
   Cortex 自己的目录，代价是第一次构建重新下载依赖（如实写进了工具描述）。
2. **真封网**要么一次管理员（WFP 按 SID，codex 的形状），要么把需要出网控制的
   场景交给云沙箱（那边有 `cortex-egress-proxy` 白名单）。

#### 阶段 1 之后还欠的 —— **2026-08-28 全部拍板：不做**

用户过了一遍这张表，决定收在这里：主线已闭环，剩下的每条都是「一次管理员」
或「收益配不上再一轮踩坑」。判据与逐条理由在
[windows-sandbox.md](windows-sandbox.md) 的第六节 —— 要重启哪条，先去推翻
那一格。下表保留当时的分析原文：

| | 欠什么 | 现在怎么样 |
|---|---|---|
| a | ~~`git` 用不了~~ **已解决**（见上一节）。剩下的是 `dir` / `vol`，以及「工作区在主目录底下时 git 仍然不行」 | 前者要卷根一条 ACE（一次管理员，只买这两个命令）；后者要 `C:\Users` 上一条 ACE，同样要管理员。两条都不阻塞主线 |
| b | ~~**CI 上一条 Windows 测试都不跑**~~ | **已解决**：`ci.yml` 的 windows-latest 腿现在跑 `--lib sandbox` 与两份逃逸测试。仍然只是那几条 —— 改 `windows*.rs` 之后**本机跑一遍**这条纪律不变 |
| c | 首次绑定一个**已经建过**的大仓库要等一两分钟 | 现在**至少有一行日志说得清**（只在真的要写时打，靠 `grant_to_container` 的回值分辨）。真正的进度条要 `TreeSetNamedSecurityInfoW` 的回调，而进度往哪儿送在当前这条路上还没有出口 —— `prepare` 回的是一个待起的 `Command`，不是一条能追加文字的结果 |
| d | ~~`%TEMP%` 没给~~ | **不用给**：AppContainer 自动把 `%TEMP%` 重定向到容器私有目录（`…\Packages\<容器>\AC\Temp`），实测可写。上一版把它列成待办是想当然 |
| e | `NetworkPolicy::Allowed` 在这一层是**全开**，没有白名单 | Linux 侧走 `cortex-egress-proxy` 的 CONNECT 白名单。要对齐，得让沙箱里的进程走代理 —— 而 AppContainer 没有「只许连这个代理」这一档，只能靠约定 |
| f | ~~配置失败时不能静默从「有沙箱」掉回「逐条确认」~~ | **已有测试守着**（2026-08-28）：`attended_execution` 里「装配失败必须报错_不许降级裸跑」—— 探测说有沙箱、装配失败时必须报错，人在场也不放行；故障注入验过红。CI 的 windows 腿显式跑它 |

#### 这个边界买到什么、买不到什么

买到：子进程**读不到用户目录里的东西**（`~/.ssh`、`~/.aws`、浏览器
profile、文档）—— 正是沙箱模块头里那句「`bash -c 'cat ~/.ssh/id_rsa'`」
说的威胁。

买不到（一条都不许含糊）：不挡网络、不挡 `ALL APPLICATION PACKAGES`
可读的系统路径、不挡把工作区里的东西发出去。

### 顺手核出来的两处「只增不删」（不是借鉴，是核代码时撞到的）

**① `request_tenant` 的测试往真库插用户，从不删** —— 每次 `just ci`
攒 2 行，数出来时 dev 库里 610 个用户有 604 个是 `probe-<ULID>`。
已修（`31a17ce`）：测试自己收拾，只删自己这一轮插的 id。

**② `BackgroundBooks` 只增不删** —— 见下。

**`BackgroundBooks` 只增不删。** 它是 `HashMap<session_id, Tasks>`，
`for_session` 用 `entry().or_default()`，而**整个仓库没有任何地方删过条目**。

每本最多攥着 8 个 `kill_on_drop(true)` 的子进程 —— 那个标志的意思是
「簿子被丢掉时才收」，而簿子永远不会被丢掉。于是：

- 在 20 条会话里各起过后台命令 → 最多 160 个子进程活到 agent 退出
- 跑完的任务也各留一份 64K 输出缓冲

`MAX_TASKS = 8` 那条注释说它能让「模型跑飞了当场看出来」—— 那句话在
**单条会话内**成立，跨会话不成立。

⚠️ **没有当场修**：修法要回答「一条会话什么时候算结束」，而 agent 侧
今天没有这个信号；随手加一条超时回收，杀掉的可能是用户正跑着的
dev server —— 那比泄漏更糟。要做的话得先有「会话关闭」这个事件，
或者按「最后一次被访问」加一个足够长的窗口 + 明确告诉用户收掉了什么。

严重性不高（要很多会话 × 用过 `background_run` 才够得着），但它是真的。

### 还有一处同族的，**没有当场修**（2026-08-28）

今晚修的两条（MCP 调用超时、MCP 不挡启动）是同一个形状：
**第三方代码坐在关键路径上，而它的耗时与调用方的耐心没有任何关系。**
顺着这个形状把剩下的路径都核了一遍：

| 路径 | 有没有界 |
|---|---|
| MCP 连接 | ✅ 60 秒（`CORTEX_MCP_TIMEOUT_SECS`） |
| MCP 工具调用 | ✅ 120 秒（今晚补的） |
| hooks（用户自己写的命令） | ✅ 10 秒 |
| `shell` | ✅ 模型逐次给 `timeout_ms` |
| **`llm_stream` 的 `.send()`** | ❌ **一点都没有** |

`Remote::llm_stream` 刻意不设超时，注释写着「模型『想』几分钟是常态」——
那句话是对的，但它把两件事合成了一件：

1. **拿到响应头**（对端开始应答）—— 这一段该有界
2. **读那条流**（模型逐字吐）—— 这一段不该有界

今天两件都没有界。于是一个「接了 TCP 连接然后一个字节都不发」的对端
（供应商过载、中间盒黑洞、agentd 自己卡住）能让那一轮**无限期**挂着。

**为什么没有当场修**：正确的修法是只给第一段设界
（`tokio::time::timeout` 包住 `.send()`，拿到头之后一个字节都不碰那条流）。
但有些供应商/代理**会等到第一个 token 才发响应头** —— 对它们来说
「拿到头」本身就是慢活，砍早了会把正常的慢当成故障。这个数要对着真实
供应商量过再定，而不是在凌晨拍一个。

严重性被一件事压住了：**用户有出口** —— `DELETE /runs/{id}`（界面上的
停止键）能掐掉那一轮。所以它是「卡住直到你按停止」，不是「永久卡死」。

### 看过但**不做**的

| | 为什么不 |
|---|---|
| `dsh` 的「万物皆插件」微内核 | 那是**给别人做生态**的架构，而 Cortex 是一个产品不是一个框架。抄它等于为一个不存在的插件作者付抽象税 —— 与「不做最小公倍数式的供应商抽象」同一条理由 |
| codex `apply-patch` | 我们的 `edit_file` 是精确匹配替换，与 Claude Code 同款。换 diff 格式买不到正确性，只换一种失败形状 |
| codex `memories` | 记忆整个在 Cormex，**这个仓库不做记忆**（CLAUDE.md 开篇） |
| `dsh` goal / schedule | 「一个会话一个持久目标 + 自动续跑」。有意思，但没有用户在等，而它会与已有的轮次语义打架 |
| codex `code-mode` / `v8-poc` | 让模型写代码来调工具，而不是逐个工具调用。是个真方向，但它要一个进程内 JS 运行时 —— 那是新的一整块攻击面，而进程级沙箱刚补上文件那一半、网络还没有 |

**第 1 条原本最重要也最贵**，2026-08-28 做掉了阶段 1（文件边界）。
剩下的差距缩到「网络没有被这一层挡住」—— 也就是说沙箱里的进程读不到
用户目录，但把工作区里的东西发出去这条路还开着。其余几条是省事与准确率。

---

## 反复出现的形状 —— 写代码前过一遍

这些复盘随完成项搬去了 [roadmap-done.md](roadmap-done.md)，但它们是**清单**，
不是历史。逐条的现场都在那边：

| 形状 | 次数 | 一句话 |
|---|---|---|
| 造好了没人调用 | 13+ | 写下「为什么需要 X」并把 X 打进产物，与**让 X 真的被调用**是两件事。最近一次：备份容器的配置渲染到 `/home/postgres`，而那个镜像里 postgres 的家是 `/var/lib/postgresql` —— 文件真的生成了，只是不在会被读的地方（2026-08-25）|
| 空串顶掉默认值 | 7 | `VAR=` 被读成「配过了」。最近一次在 2026-08-17 新写的 CLI 身份解析里 |
| 换后端只清了手上的，没作废在飞的 | 3 | 清空之后落地的异常/成功结果照样写进 state |
| 一个状态码身兼两职 | 3 | 404 既是「没这个功能」也是「它已经不在了」 |
| 把一个环境里量到的数当普遍事实 | 4 | 512 MiB 是 `--memory` 上限不是占用；30 分钟抄自成本结构完全不同的产品 |
| 配置有两份，改了一处 | 3+ | 编译器不看 YAML。最近一次：全量备份有两处实现（主机脚本 + 备份容器），容器那份**没写 `meta.env`** —— 备份全绿，而证明它读得回来的演练在第 0 步就死（2026-08-25，已加闸）|
| **验证走的不是用户那条路** | 2 | 断言过了、也真的走过一条路，只是不是用户那条（`sandbox-verify.sh`）。最近一次：备份容器本机验过，而我测试时 `chown` 过挂载目录 —— 生产上那个目录是 `root:root`，归档从配上那天起一次都没成功过（2026-08-25）|
| **照着 roadmap 排期而不核代码** | 3 | 最近一次（2026-08-28 清理时）：整节「演示场景」三条全是**记忆**的演示，还写着「前两条已经能跑」—— 而记忆 2026-08-17 就整个拆去了 Cormex。**验收标准量的是另一个产品。** 同一轮还发现「哪些会话在跑」那条待办的封条（「这个仓库做不了」）在两周前就掉了而没人撕。更早：这一批十条里有四条的前提是错的 |
| **三态里的 `null` 漏掉一个已知事实** | 3 | 最近一次：远程接入那颗标记的判据是 `.value != true`，左边从 `bool?` 变成 `LocalAttach?` 之后**对任何状态都成立** —— 答不出来的后端也常亮。是 analyzer 抓的（`unrelated_type_equality_checks`），不是眼睛（2026-08-27）。更早：「不知道就放行」放走了 20 个 qwen-image —— 它们 `tool_call` 是 null，而**旁边的 `image_output` 明说了**它们是画画的。写 `a != false` 之前先看 `b == true` 算不算数（2026-08-20） |
| **假数据让测试失去区分力** | 新 | 上一条的测试一开始用 `toolCall: false` 造生图模型，而真实形态是 `null` —— 测试绿着，线上是坏的。造夹具时照**线上真实形状**造，不照「方便断言的形状」 |
| **验证工具自己造出「通过」** | 3 | 判据落在了一个总能满足的东西上。三次：演练靠 `[ -f 段名 ]` 判「已归档」，而一段上个数据库留下的同名文件就满足它，报出一个编造的 RPO；新加的闸靠 `grep MSYS_NO_PATHCONV`，而**注释里**正好有这个词，删掉代码它照样绿；同一道闸判「调 docker」时把**自己的错误提示**也算了进去。判据一律要落在**代码行**上，且**每道闸都要故障注入一次**（2026-08-25）|
| **闸只盖了一半目标** | 新 | 判据是对的、故障注入也会红 —— 但它**枚举的目标少了一半**，于是另一半从来没被看过，而它一直绿着。2026-08-28 一晚上两次：工具链闸把 Rust 的六个引用点逐个列了，Flutter 却只查了三个里的两个（漏的那个把发布流水线弄红了）；工具描述闸只查 `builtin_specs()`，而 `spawn_agents` / 生图 / 联网 / 资料库 / 电脑操作 / 技能全在别的构造器里。**与「验证工具自己造出通过」不是一回事**：那个是判据总能满足，这个是判据对、但没跑到该跑的对象上。自查问句：**这道闸要盖的东西，我是不是逐个列出来了？** |
| **只在故障 / 离场路径上跑的代码** | 3 | 最近一次：关停时那句「我要重启了」**一次都没送出去**，而单测是绿的 —— 信号只被 `borrow()` 读了一眼（没登记 waker），帧没冲完就伪造 EOF 把 socket 关了。两处都从真机 `docker restart` 的日志里才看出来（2026-08-27）。更早： 没人验就等于没写。桌面端那三个（`app-status` 读到假地址、启动诊断误报崩溃循环、卸载器抛异常导致整个卸载中止）；以及备份那条 —— `archive_command` 失败时 PG 当成「归档到头了」，**正常结束恢复、升主、一个字都不说** |

| **一个信号有两个订阅者，只接了一个** | 新 | 远程接入开关拨动时叫醒了隧道、忘了心跳 —— 症状是按下开关后**另一台设备最长 30 秒才更新**，而这台机器上一切正常。有隧道时这条根本到不了（隧道一建起来 controller 自己就来拉），所以它只在**没有隧道**那条路上现形（2026-08-27）|
| **测试全绿 ≠ 看过一眼** | 新 | 链路真机验过、测试全绿，**把那一页点开**仍掉出三处：标题画了两次、同一台机器在页上出现两次且看不出是同一台、顶栏标记判据恒真。前两处任何测试都抓不到 —— 它们不是「错」，是「看着不对」（2026-08-27）|

### ~~桌面端「两个进程」~~ —— 六条 2026-08-20 全部做完

调研（六家同架构产品）在
[agent-lifecycle-survey.md](agent-lifecycle-survey.md)，落地记录搬去了
[roadmap-done.md](roadmap-done.md)。留在这里的只有那三个 bug 共同的形状：

> **只在故障路径 / 离场路径上跑的代码，没人验就等于没写。**
> 三处现场：`just app-status` 读到一个「看着很像真的」旧地址、
> 启动诊断把每次冷启动必然发生的一次重建记成失败（开三次机就报崩溃循环）、
> 卸载器里一句格式化抛异常导致**整个卸载中止，程序文件一个都没删**。

## 手头挂着的 —— 没有了

上一版这里挂着三条（模型能力判定、备份、沙箱镜像），**全部做完了**，
2026-08-28 整块搬去 [roadmap-done.md](roadmap-done.md)。

留这一节的空壳是有意的：下一次真有「正在做、还没完」的事时，它该回到
这里，而不是散在别处。**一个说「进行中」的标题比一条过期的待办更误导**
—— 那三条在这个标题下躺了两三天，读的人会以为有人正在做。

## 下一批 —— 挡在「敢给别人用」前面的

> 「日常自己用」的五条（R1~R5）与运维三条（R6~R8）、沙箱（R10）
> 均已完成，见 [roadmap-done.md](roadmap-done.md)。

### E · 任何在线的 agent 都能被挂上去（原「Web 挂到在线的本地 agent」）

> **CLI 不走这条。** 它和你的文件在同一台机器上，直接连 `127.0.0.1` 的本地
> agent 即可（没起就拉起来）。需要中继的只有 Web 与「另一台设备上的 GUI」。

本地 agent 跑完整会话，agentd 只当**中继**，Web/CLI 挂上去看和操作。跨网络的
是 **UI 事件流**，不是每一次工具调用 —— 这是它与被否决的「工具外派」的全部
差别，代价差一个量级。做完之后 Web 的限制就只在**一台在线桌面都没有**时成立。

**2026-08-17 重新框过一次，并做了一路调研**（[references.md](references.md)
「远程挂载与多端」）。原来写的是一个特例（Web 挂到本地 agent），现在的框法是
**「哪个 agent 在线，谁都可以连过去」** —— 那等于把 `SessionRuntime` 从
「这个会话被钉在哪」改成「现在是哪个 agent 在托管它」，于是那句 409
（「请到那台机器上打开它」）这个概念就没了。

设计与基数见 [architecture.md](architecture.md)「六者什么关系」。四个阶段：

| 阶段 | 做什么 | 买到的不变量 |
|---|---|---|
| 1 | `Workspace` 成一等对象，`session.workspace_id` 取代裸路径；`runtime` 降级为派生字段 | 服务端第一次**知道**工作区是什么，而不是二选一地猜。对外零变化 —— 这正是它安全的原因 |
| 2 | ~~容器工作区可命名~~ **✅ 2026-08-17 服务端 / 2026-08-18 界面** | 「容器里指定工作区」。不动路由、不动身份。⚠️ 服务端那半做完之后**界面隔了一版才接上** —— 期间这个能力在 HTTP 上活着而用户点不到，就是「造好了没人调用」那个形状 |
| 3 | ~~agent 上报在线名册~~ **✅ 2026-08-17 做完** | 「哪台机器」第一次成为可见信息，而授权规则一个字没改 |
| 4 | ~~中继 + 消息 FIFO 排队~~ **✅ 2026-08-17 做完** | Web 上的一轮真的跑在你那台机器上；撞上「正在跑」的那句话不再被丢掉 |

阶段 1 是「大改小用」，但它是后三步共同的前提，而且它自己就修掉一件事：
**服务端现在没有能力按工作区路由**（`session_state.workspace` 是个裸路径字符串，
不带机器；项目的本机落地目录只存在客户端的 `workspaces.json` 里）。

#### 阶段 2–4 做完之后，阶段 1 还剩什么

2、3、4 都是在**不动**阶段 1 的前提下做出来的，所以有必要说清它现在的处境：
它不再是「后三步的前提」，而是一次**内部整理**。

能不做阶段 1 而做出接入，是因为路由的判据从来没有落在服务端的数据上 ——
它落在「哪个 agent 拿得出这份绑定」，而那是 agent 自己报的。这正是 model.rs 里
那条论证的兑现（见下面「设备身份」一节）。

阶段 1 现在剩下的价值有两条，都不是能力：
- `session_state.workspace` 是个裸路径字符串，服务端**存着一个它无法解释的值**
- `runtime: Cloud|Local` 一个字段承担两件事（文件在哪、谁跑这一轮）

也就是说它值得做，但**它挡不住任何东西**了 —— 于是排在别的能力后面。

#### 调研改掉的两处

**① 单写者租约 → 消息 FIFO 排队。** OpenHands 踩过我们推演出的那个 bug
（WebSocket 收到消息就直接 `run=True`，多个执行循环同时跑），修法不是拒绝第二个
客户端而是**排队**：agent 忙时进 FIFO，当前这轮跑完再处理，另有一把 state lock。
比租约好在第二个客户端发的话不会被顶回来，而且对我们几乎免费（`/sync` + `/ws`
已经在，episodes 本来 append-only）。

**② 中继看到明文这件事有解。** VS Code dev tunnels 的分层是「WebSocket 中继层
由 connect JWT 认证 + SSH over WS 做端到端加密」，于是中继**读不到内容** ——
对自托管是净赚：agentd 当中继时不必被信任。服务端用的 `russh` 是 Rust 的。

> **② 还没做。** 阶段 4 的中继是明文的（agentd 读得到这一轮的全部内容）。
> 对当前部署不构成新的暴露 —— agentd 本来就存着全部消息、也是它在调 LLM。
> 端到端加密要买到的是「**自托管者不必信任中继方**」，那是给「agentd 由别人
> 运营」那种部署准备的，现在还没有那种部署。做的时候记住：真正的判据是
> **agentd 不再需要看内容**，而不是「加了一层 TLS」。

#### 阶段 4 落地时改掉的一处

调研里没预料到、真机上撞出来的：**通告地址与监听地址是两回事**。

第一版只有 `--allow-remote-attach`，通告的就是实际监听到的那个地址。它在
「机器直接暴露端口」这一种情形下对，而那不是常态 —— NAT、端口映射、
Docker Desktop 上那个属于 WSL 而不属于 Windows 宿主的网关地址，
三种情况下能监听的地址都**不是**别人够得到的那个。于是有了 `--attach-addr`。

拒绝 loopback / 通配符这条校验也因此必须验**通告的**那个地址，不是绑定的那个：
`--bind 0.0.0.0:8090 --attach-addr 203.0.113.7:8090` 是完全正当的组合。

#### 设备身份：**降级成路由提示，不做授权依据**

`model.rs` 里那条论证不能推翻（「本机绑定存在 `workspaces.json`，天然只在那一台
机器上，那本身就是设备检查 —— 比一个 device id 更硬，id 在重装/克隆之后会骗
人」）。所以在线名册里的 `machine_hint` 只用于**找人与界面文案**，真正的判据仍然
是「那个 agent 拿不拿得出这份绑定」—— 一个撒谎的 id 在它声称能跑那一轮时当场
被抓。

#### 三件明确不做

- **挂宿主机路径进云容器** —— 那是 `ExecEnvironment` 刻意留空的第三格（无隔离 +
  默认免确认 + 服务器全盘）。除非部署明确单用户自托管**且**那种会话强制回到逐条
  确认
- **抄 OpenHands 的 `session_api_key`** —— 维护者自己说它是「bearer + 沙箱绑定，
  不是有意义的第二因子」，而他们 issue 里在求的降权凭据**我们已经有了**
- **在阶段 4 之前给 agentd 加长连接** —— 它现在的失败模型是「起不来就不启动」，
  加长连接会多一类「半死」状态，而那要有在线名册才诊断得清

依赖 D2（得先有本地 agent 可挂）。

> 顺带纠正一个容易传开的误解：**CLI 与 Web 现在都不是「只能聊天」**。
> CLI 会渲染工具事件、能答确认；Web 有附件上传下载、图片理解、
> markdown + 代码高亮。**唯一由架构决定的限制是「不能操作你机器上的文件」**。
> 与 ChatGPT / Claude 的真实差距在**生成侧**（生成图片、生成 Word/PPT/Excel —— 都没有），
> 那是独立的产品缺口，不在 D/E 的范围里。逐项对照见
> [architecture.md](architecture.md)「CLI 与 Web 现在到底能做什么」。

依赖 D2（得先有本地 agent 可挂）。

### F · 生成侧 —— 生成图片 / 生成文档

与 D/E **无关**的独立缺口。接收侧我们不差（附件上传下载、图片理解、
markdown + 代码高亮都有，逐项见 [architecture.md](architecture.md)），
差的全在生成侧：

> **接完 MCP 客户端之后重新量过（2026-08-15），这一节的答案变了。**
> 原先四行都是「自己写一个工具 + 一个库」。实际上**只有图片那一行仍然成立**。

### 文档生成：走 MCP，别自己写

docx / xlsx / pptx 的 MCP server **已经有好几个成熟的**（OfficeMCP、
OnlyOffice MCP、mcp-ms-office-documents、GenFilesMCP…），覆盖模板、
Markdown 转换、图表、页眉页脚这些我们自己写要花很久的部分。

前置条件也已经具备了：沙箱镜像里有 node + python3 + pip，
出网放行清单里**已经有 `registry.npmjs.org` 与 `pypi.org`** ——
`npx` / `uvx` 起的 server 在容器里装得下来。

所以要做的不是「写文档生成工具」，是**让 MCP 能被配上**：

| | 现状 | 要做什么 |
|---|---|---|
| 桌面端 | ✅ 能用 —— 往 `<用户目录>/mcp.json` 放配置即可 | 但安装包**不带 node / python**，得在文档里说清；或者考虑随包带一个（几十 MB） |
| 云端会话 | ✅ 能用 —— 工作区根上放 `.mcp.json`（2026-08-15） | 镜像自带 nodejs / npm / python3，`npx` 起得来 |
| 配置入口 | ✅ 设置页（2026-08-15） | 列表/详情/粘贴添加/注册表浏览。设置窗顺带从单列 AlertDialog 改成左导航 |

云端那条补法见 `cortex_mcp::config_path`。两点值得记住：

- **文件名与 Claude Code 的项目作用域同名**（`.mcp.json`），所以仓库里
  已经有那份的人，把项目搬进云沙箱**不用再配一遍**
- **桌面端刻意不合并读它**。Claude Code 两个作用域都读，但用项目那份前
  会问一次；我们还没有那个询问界面，合并就等于 clone 一个陌生仓库、
  绑上工作区，它自带的 `.mcp.json` 在真机上拉起了子进程。设置页做完
  再合并

出网默认放开（`CORTEX_EGRESS_ALLOW=*`）。锁死出网的部署要自己把
`registry.npmjs.org` 加进白名单，否则 `npx -y ...` 拉不下包。

### ~~图片生成~~ / ~~diff 视图与终端 ANSI~~ —— 都做完了

图片生成 2026-08-19/20（`generate_image` + `POST /llm/image`，DashScope，
**不是**选择器里的一个模型 —— 生图与对话是两条协议）；富文本四件套的
最后两件 2026-08-17。都搬去了 [roadmap-done.md](roadmap-done.md)。

> **F 剩下的只有一格**：桌面端安装包**不带 node / python**，而挂
> Office 类 MCP server 要 `npx` / `uvx`。要么在文档里说清，要么随包带一个
> （几十 MB）。云端会话不受影响 —— 镜像自带。

### G · 派 agent 去云上干活 —— **机制已经有了，缺的是产品化**

「让 agent 自己在别处跑一段时间，回来交活」这个能力今天就有，
**但不是这一节原先写的那个机制**。

> ⚠️ **这一节的前提换过一次，值得记一笔。**
> 原先写着「cortexd 里已经有 `Live::workspace_turn` 与会话绑定工作区
> （`bind_workspace`），服务端那条循环本来就是一个云上工作区 agent，
> 所以别删」。**那条循环后来还是删了** —— 理由不是它没用，而是它是
> **第二份 agent 实现**：同一个 `Turn::run` 有两处装配，每加一个工具、
> 每改一次注入纪律都要记得改两遍，而漏掉的那一遍没有任何测试会红。
>
> 能力没有消失，换了个宿主：**cortex-agentd 拉起一个容器，里面跑的是
> 同一个 `cortex-local`**（只多一个 `--exec-env=container`）。
> 于是「派出去」与「在本机跑」共用一份循环，而不是两份。

| 场景 | 走哪条 |
|---|---|
| 改我本机的代码 / 文档 | 本机 `cortex-local` |
| 派出去干活，关掉笔记本也照跑 | 容器里的 `cortex-local`（agentd 按需拉起） |
| 没有在线桌面时随便聊两句 | 同上 |

要补的部分：给它一个明确的入口（「派出去」，而不是碰巧开了云沙箱）、
~~任务状态可见~~（**已落地**，见下）、~~跑完通知~~（**已落地**，见下）。

> ✅ **跑完通知**（2026-08-19）。此前跑完的表现是侧栏徽章从「在跑」变成
> **什么都没有** —— 而「什么都没有」与「从来没跑过」长得一模一样，于是
> 用户只能隔一会儿点进去看一眼，而那正是这个能力想省掉的事。
>
> 现在 `ChatState.finished` 记「人不在的时候跑完的」那些会话：侧栏画一个勾
> （不是第二个圆点 —— 两个只有颜色不同的点在色弱视角下与「在跑」分不开，
> 而这两件事的下一步完全相反），同时弹一条带「去看看」的提示。打开就清。
>
> ⚠️ **这是应用内的通知，不是操作系统级的。** 桌面端最小化、或者浏览器
> 标签页在后台时，用户看不到它。补那一半要引入一个通知插件（Windows /
> macOS / Linux 三套原生通道）与权限流程，是独立的一件事 ——
> 在它落地之前，这里不声称「会通知你」，只声称「回来就看得见」。

> ⚠️ **「不是新架构」这句只对了一半**（2026-08-15 核过代码）。
> 执行侧成立，观测侧当时一格都没有。**观测侧那一半在同一天补上了。**
>
> **执行侧一直是对的。** 轮次跑在一个独立的 `tokio::spawn` 里，与 SSE
> 响应解耦；客户端断开不中止它，episode 照常落进**这个仓库自己的库**
> （`cortex-store`，不是记忆服务 —— 2026-08-15 搬的）；容器的空闲回收看
> 「令牌多久没被用过」，跑着的轮次一直在续命。**关掉浏览器，活还在干。**
>
> **观测侧原先是空的**：事件发往一个已经被丢弃的 channel，人回来只能等
> episode 落库。现在有 `crates/cortex-local/src/runs.rs` —— 一份轮次登记簿，
> 带保序的重放缓冲与广播：
>
> | | |
> |---|---|
> | `GET /runs` | 这个进程上还在跑的那些 |
> | `GET /runs/{session_id}` | 挂上去（**重放 + 后续拼成一条 SSE**） |
> | `GET /sandbox/runs/{id}`（agentd）| 同一件事，多穿一层容器 |
>
> 客户端在 `selectSession` 时自动试挂，挂不上（404 / 旧后端 / 断网）一律
> 静悄悄 —— 绝大多数会话此刻都没在跑，任何噪音的代价是每打开一个旧会话
> 都来一次。
>
> ⚠️ **「哪些会话在跑」这个列表没做。原先写着「这个仓库做不了」——
> 那个理由 2026-08-15 就不成立了。**
>
> 当时的论证是「会话表在记忆服务，`SessionDto` 加不了字段」。而会话表
> **那天就搬进了 `cortex-store`**，是这个仓库自己的库。字段加得了，
> owner 也枚举得出。
>
> 这是「照着 roadmap 排期而不核代码」的又一个现场：一条被封死的待办，
> 封条在两周前就掉了，而没人回来撕。**它现在是可做的**，只是仍然没人
> 在等 —— 归到「等判据」，不是「做不了」。
>
> 替代方案是**客户端自己记**「我发出去还没见到收尾的」（`ChatState.unfinished`）：
> 不需要任何后端、桌面端与云端同一份代码，且恰好覆盖真实场景 ——
> 「我发完就走了，回来看看」。局限是别的设备起的轮次看不到。
>
> 顺带纠正了三处在「客户端会一直连着」这个前提下写的行为，见那次提交：
> `bridge_events` 里「send 失败就 break」（没人看时连工具调用都不记）、
> 确认因「此刻没人连着」提前放弃（而人会回来答）、以及同一会话并发两轮
> （现在 409）。
>
> **E 要的是同一套机制**，所以它现在也只差一个入口了。

> ⚠️ **这里原先写着「派出去的 agent 与你共用同一个记忆权威…值得在演示里
> 专门做一遍」。那句话今天是假的** —— 记忆 2026-08-17 整个拆去了 Cormex，
> 这个仓库连一条召回都不做（CLAUDE.md 开篇）。照它去准备演示，会当场
> 发现 agent 什么都不记得。
>
> 真正独有的那一条还在，只是换了内容：**派出去的和在本机跑的是同一份
> agent 循环**（同一个 `cortex-local`，只多一个 `--exec-env=container`）。
> Devin 那类云 agent 是另一套实现，行为与本地不一致。

### H · 工具是写死的 —— 按需加载与插件化

**现状（2026-08-25 重核了一遍）**：工具从定义到分发仍然全是硬编码，
散在三处 —— **不写行号**，那两个函数一直在长，写死的行号半个月就骗人：

| 在哪 | 是什么 |
|---|---|
| `tools.rs` `builtin_specs()` | 一个 `vec![]` 字面量，现在是 9 个 `ToolSpec`（2026-08-15 记的是 6 个） |
| `tools.rs` `execute()` | 一个 `match call.name.as_str()`，每个工具一条臂 |
| `turn.rs` 三处 | 直接调 `builtin_specs()`，各自再过滤 |

加一个工具 = 改同一个文件的两处 + 顾到三个调用点。而**由宿主执行的那些**
（`web_search`、`library_*`、`mcp_resource`、生图、电脑操作…）根本不进
`execute`，它们由 agent 循环在更外层按名字拦截分派。也就是说分发早就裂成
两层了，那本身就是「这里该有一张表」的信号。

> 半个月里这张表唯一的变化是「6 个」变成「9 个」，而两处硬编码一处没动 ——
> 这恰好印证了下面第 2 条的结论：**它挡不住任何东西**。这段时间里加了
> 十几个工具，每一个都是照着这三处抄一遍，没有一次因为「没有注册表」
> 而做不下去。

> ⚠️ **上面这张表是 2026-08-15 上午的现状，下午第 1 条就做完了。**
> `builtin_specs` 与 `execute` 那两处硬编码**照旧**（第 2 条没做），
> 但工具目录已经不只有内置的了 —— 见下面第 1 条。

~~**更要紧的一条：我们不是 MCP 客户端。**~~ **2026-08-15 起是了。**

此前 `rmcp` 只用来借 `Tool` 这个类型，没有任何 client transport，于是
第三方 MCP server 一个都接不进来 —— 而我们信这套原语信到把核心资产
（记忆）按它组织成 server 交出去。**只当供给方不当消费方**，那是不对称的。

#### 两家的做法

**[DeepSeek Harness](https://github.com/deepseek-ai/deepseek-harness)（dsh）
—— 一切皆插件。** Cordis 内核只管挂载 / 卸载 / 依赖，其余**全部**是插件：
模型、工具、skills、会话、沙箱、存储、循环、调度、UI。核心概念是
**Capability Seam**：Service Definition 声明接口（`ctx.llm`、`ctx.fs`），
Service Provider 实现它（`llm-deepseek`、`fs-local`），Consumer 消费它
（通常就是面向模型的工具，如 `tool-bash` 消费 shell 能力）。
**没有需要打补丁的特权核心** —— 扩展方式是在别的插件旁边挂一个新的，
注册是副作用，卸载时干净回滚。配置层用 profile / bundle 分层组装
（`dsh-base` → `dsh-web-app` / `dsh-headless`），运行模式里那个
**Minimal 只留 shell 与文件编辑器**，专门用来给模型做 benchmark。

**[pi](https://github.com/badlogic/pi-mono)（Mario Zechner）—— 反方向的
同一件事。** 核心极简到 ~150 词系统提示 + 四个工具（read / write / edit /
bash），**刻意没有** sub-agent、plan mode、MCP、后台 bash，然后靠扩展把这些
全加回来。扩展是 TypeScript Extension / Skill / Prompt Template / Theme，
打成「Pi Package」用 npm 或 git 分发，支持热重载。它在 TerminalBench 上
排第二 —— 也就是说**砍到只剩四个工具并不掉分**，值得记住。

#### 对我们意味着什么 —— 三条，按确定性排序

**1（最确定）· 接 MCP 客户端。—— 已完成（2026-08-15）**

`cortex-mcp`：stdio 与 HTTP 两种传输、配置与 Claude Code 通用、设置页
（列表 / 详情 / 粘贴添加 / 官方注册表）、连接失败逐台可见。

**这条直接改变了 F 的做法**：文档生成不用自己写，挂一个 Office 类
MCP server 就有；图片生成仍要自己写，因为难点（选哪家、谁付钱、key 存哪儿）
MCP 一点都没省下来。

做的过程里撞出两个 bug，都是「不报错」那种，值得记下来：

- **`chat_turn` 是启动时建好的 `Turn::sealed()`**，外来工具只并进了绑了
  工作区那条路。于是「配好 MCP + 开个没绑工作区的会话」= 一个外来工具都
  没有，而模型只是表现得不会用那个功能。治法不是给它也并一次（那就是第三处
  装配），是**删掉那个字段、两条路都现搭**
- **连接一处超时都没有。** `serve()` 与 `list_all_tools()` 都可能永不返回
  （子进程起来了但在等 stdin、HTTP 端点接了连接就沉默），而逐台连接是串行
  的 —— 后果是**整个 agent 起不来**，桌面端卡在「正在启动 agent」，日志最后
  一行指向那台 server，看起来像它的问题。`McpHub` 文档里「一台起不来其余
  照常」那句承诺，没有这道超时就是假的

**2（中等）· `builtin_specs` + `execute` 换成注册表。** 纯机械重构，
收益是「加工具不改 match」。

> **它不再是第 1 条的前置了。** 原先的判断是「从 MCP 来的工具得能和内置
> 工具进同一张表」，实际做下来更简单：`ToolSpec` 加一个 `source` 字段
> （`Builtin` / `External`），分发按**类型**分岔而不是按 match 的兜底顺序。
> 也就是说这条现在是纯粹的内部整洁，没有功能挡在它后面 —— 优先级该降。

**3（最不确定）· 要不要走到 dsh 那种「循环 / 存储 / UI 都是插件」。**
Rust 没有 dsh 那种 ESM 动态挂载的运行时便利，做成动态库插件是另一个量级
的事（ABI 稳定性、崩溃隔离、版本矩阵）。我们的 seam 更可能长成
**「进程内注册表 + 进程外 MCP」两层**，而不是一个通用内核。
**这一条要有明确需求再动** —— 别为了对齐一个架构名词，付编译期与
安全边界上的真钱。

#### 一条必须写在前面的：权限是我们已有的 seam

`ExecEnvironment` + `Risk` + `ApprovalPolicy::decide` 是按**爆炸半径**
组织的，而任何从外部挂进来的工具**必须落进同一个闸门**。

这是接第三方工具时第一个要想清楚的问题，而两家都没有替我们回答：
dsh 的审批策略放在 `dsh-base` 里（是插件，也就是可以被换掉），
pi 干脆不谈这件事。

具体要定的是：**`ToolSpec.risk` 由谁声明？** 一个外来的 MCP server
自称它的工具是 `Risk::Safe`，我们该信吗？照现在的类型，
`decide` 只在 `risk < confirm_at` 时放行 —— 让调用方自报风险等级，
等于把闸门的钥匙交给被闸的人。

#### 这个问题在 2026-08-15 定了

**不信对端，信用户。** 三条，写在 `ToolSpec::external` 与 `config::Trust` 上：

1. **外来工具一律 `Risk::Execute`**（最高档），构造函数里写死，不读对端的
   `annotations.readOnlyHint` —— 那个字段是**服务端自报**的
2. **唯一能降档的地方是用户自己的配置文件**里的 `trust`（`ask` / `write` /
   `trusted`）。字段名刻意不叫 `risk`：配置要表达的是「我多信它」，
   不是「它有多危险」—— 后者我们判断不了
3. **名字强制加前缀** `mcp__{server}__{tool}`，构造函数里拼，外来工具因此
   **不可能**顶掉一个内置工具的名字

设置页把第 2 条摆在明面上（三档 + 一句为什么默认是「每次询问」）。
两家参考实现都没回答这个问题：dsh 把审批策略放进一个可以被换掉的插件，
pi 干脆不谈。

### ~~I · 与四家 harness 的差距 / J · 设计稿里有产品里没有的~~ —— 两节 15 条全部做完

2026-08-24 起的两份逐项清单（I：对照 Claude Code / Codex / Grok Build /
goose 核过代码的 10 条；J：对照设计稿逐屏核过的 5 条），**2026-08-25
全部落地**，连同「三样不差反超」与每条的取件来源一起搬去了
[roadmap-done.md](roadmap-done.md)。

四件**明确不做**：`AGENTS.md` 的 `@import` 展开、上下文的 0.8 阈值主动
压缩、资料库的 pdf/docx 正文提取、MCP 的 `prompts`。理由逐条记在
[roadmap-done.md](roadmap-done.md#决定不做--不排期--归档-2026-08-25)，
**别当成漏项重开**。

## 不做的事 —— 都在 roadmap-done 里

**这里不再留「不排期」的清单。** 一份长期不动的待办与一份「决定不做」
读起来一模一样，而两者的下一步完全相反：前者等时机，后者已经有答案了。
混在一起的代价是每次读 roadmap 都要重新判断一遍每一行属于哪种。

所以决定与理由整块搬去了
[roadmap-done.md](roadmap-done.md#决定不做--不排期--归档-2026-08-25)，
按**归哪个仓库**分了组 —— 搬的时候才发现那张表里大半根本不是这一侧的事
（检索质量、cross-encoder、`relates_to`、多主同步……全在 Cormex）。

这一侧还活着的三条，一句话版本：

| | |
|---|---|
| **ASR** | 缺口在上游 —— goose 的 `MessageContent` 没有 Audio 变体。补齐约等于「一个变体 + 一个 `convert_audio`」，等上游或等一个明确需求 |
| **移动端** | 桌面与 Web 先跑通。移动端是采集端，可后置 |
| **HA** | 单机自托管，恢复期间服务是停的，RTO 就是停机时间 |

---

## 获客钩子（v1 前值得做）

**① 外部对话一键导入 —— ✅ 三端全部落地**（解析器
`crates/cortex-import`、CLI `cortex import`、桌面端与 Web 端界面）。
落地经过与那条「线全接好了、roadmap 没跟上」的教训搬去了
[roadmap-done.md](roadmap-done.md)。

**② 带走自己的数据**：现在完全没有（只有 `pg_basebackup` 那套运维备份，
那是给运维恢复用的，不是给用户带走数据用的）。一个以「永不丢失」为承诺的
产品拿不走自己的数据是自相矛盾的，GDPR 的可携带权也写在
[memory-content.md](memory-content.md) 里。工作量小，**该早做**。

⚠️ **这一条拆分之后是两半，别当成一件事排期**：
**记忆的导出在 Cormex**（facts / entities / 时间轴都在那边的库里），
要开那个仓库的会话去做；**这一侧能做的是会话导出** —— 消息、附件、项目，
`cortex-store` 里都有。两边各做各的，判据还是 CLAUDE.md 那句
「这张表离开记忆能力还有没有意义」。

**③ 让别的 agent 用我们的记忆 —— 已落地，且现在整个在 Cormex**
（`/mcp`：工具 `memory_search` / `remember`，resource `cortex://profile`）。
它同时是一条走通了的**战略岔路**：我们自己的 agent 与第三方 agent 走同一个
API，「不能发布一个自己都不吃的 API」从形状上成立而不靠自律。经过搬去了
[roadmap-done.md](roadmap-done.md)。

---

## 并行策略

非冲突任务交子 agent 并行。冲突面按**目录**划，不按功能划——
功能会互相牵扯，目录不会。

拆分之后这个仓库的天然切面是四块，互不重叠：

| 目录 | 谁在这儿干活 |
|---|---|
| `crates/cortex-agent` + `cortex-local` | 循环、工具、沙箱、权限 |
| `crates/cortex-agentd` + `cortex-store` + `migrations/` | 编排、持久层、路由 |
| `app/` | Flutter（桌面 + Web 一套） |
| `scripts/` + `ansible/` + `.github/` | 运维与发版 |

**加一个工具会同时落在前两块**（spec 在 `cortex-agent`，宿主实现在
`cortex-local`，路由在 `cortex-agentd`）—— 那种活**串行做或者一个 agent
包圆**，别拆。判据不是「改了几个文件」，是「有没有一处漏改是编译器发现
不了的」：`Caps::any()` 那次就是漏在两块之间。

> 上一轮四个 agent 的边界写的是 `cortexd`，那个 crate 2026-08 拆走了。
> 留这一句是因为**并行边界会随架构漂**，照着旧边界分工的第一个症状是
> 两个 agent 同时改一个不存在的目录。

---

## 演示场景（做完什么算「能给别人看」）

> ⚠️ **这一节 2026-08-28 整个换掉了。** 原先三条全是记忆的演示
> （「我们上次定的对象存储是什么」「三个月前我以为用什么」「那张截图里
> 有哪些组件」），还写着「前两条已经能跑」—— 而记忆 2026-08-17 就整个
> 拆去了 Cormex，这个仓库连一条召回都不做。
>
> 也就是说：**这份 roadmap 的验收标准，量的是另一个产品。** 照它去准备
> 演示，会在第一句话上就翻车。那三条现在归 Cormex 那边的 roadmap。

Cortex 这一侧真正拿得出手的三条，都验过：

1. **咖啡馆场景。** 笔记本绑 `127.0.0.1`、没有公网地址、agentd 在容器里
   够不到它 —— 在手机浏览器上发一句，agent 在**那台笔记本**的目录里跑
   `list_dir` / `write_file` 并流式回答；工具确认弹回浏览器，批准之后
   文件真的落在笔记本的磁盘上。2026-08-28 在生产上端到端验过。
2. **关掉浏览器，活还在干。** 发一轮长任务，中途掐断 SSE —— 重新打开，
   那一轮已经跑完，结果在历史里。轮次与响应是解耦的。
3. **同一份 agent 循环，三个地方跑。** 桌面端、云沙箱、别人机器上的
   worker，是同一个 `cortex-local` 二进制，差别只有一个
   `--exec-env`。演示的价值在于「行为完全一致」—— 而不是「我们有三种模式」。

**Windows 上的成色（2026-08-28 起）**：进程级沙箱有了 —— 沙箱里的命令
读不到、写不进用户目录。但**网络还没有被这一层挡住**，所以「把工作区里
的东西发出去」这条路在三个平台上都还开着（Linux 侧靠 `cortex-egress-proxy`，
Windows 侧是阶段 2）。演示时这一点要说清楚，别说成「全封住了」。
